# VISNDT 重新设计工程方案（假设从零开始）

**性质说明**：这是一份思想实验性质的工程设计文档——假设抛开现有代码，
只保留"要解决什么问题"这个出发点，重新设计一遍。目的不是要你真的
推倒重来（第十一节会说明现有项目哪些部分应该保留），而是用这个视角
反过来检验现有设计哪里站得住、哪里是历史包袱。

---

## 一、竞品全景：这个赛道现在长什么样

**国内**：
- **领翼NDT商城**（chinandtmarket.com）——最直接的同类对标，定位是
  "无损检测设备&耗材一站式采购平台"，本质是垂直电商（购物车/下单
  模式），没有"结构化参数匹配"这个层次，是"人找货"的传统电商货架
  逻辑
- **ndt.org.cn（无损检测网）**——行业门户+交易市场混合体，带有
  "诚信通""担保交易""订单保证金"这类阿里巴巴式的信任机制，说明
  这个行业的采购方对"钱货两清的信任保障"有明确需求，不只是"找到
  参数匹配的设备"就够了
- **仪表网**——更大类目（覆盖仪器仪表全品类，NDT只是其中一小块），
  靠"垂直搜索+客服人工撮合+短视频/展会资源"驱动流量和转化，续费率
  号称 85%，说明**人工撮合环节在这个行业目前仍然是转化率的关键
  杠杆**，纯自动化匹配可能还不足以让重决策的工业采购完成闭环
- **1688/慧聪网**——通用工业品 B2B，NDT 设备只是长尾品类之一，
  没有针对性的参数体系

**国外**：
- **DirectIndustry / Thomasnet / IndustryNet / Engineering360**——
  工业黄页型平台，靠展会资源和目录聚合驱动，参数化程度普遍不深，
  更像"带联系方式的产品图册"
- **NDTSupply.com**——美国的 NDT 品类垂直电商，直接卖货（含耗材），
  介于"电商"和"技术选型工具"之间，参数展示以规格表形式呈现，
  没有智能匹配

**结论**：**目前没有一家真正做到"用结构化工程参数做需求-设备智能
匹配"这件事**——这恰好印证了 VISNDT 最初的差异化判断是对的，不是
臆想出来的空白点。但所有对照案例都提示了两个 VISNDT 现在还没有的
东西：**人工撮合/客服介入的转化环节**、**交易信任机制**（担保交易/
认证体系）。这两点会体现在下面的重新设计里。

---

## 二、现有项目哪些判断值得原样保留——不是全部推倒

在往下写"如果重新设计"之前，先说清楚：**以下几点是正确判断，重新
设计也不会改**，避免这份文档被误读成"之前都做错了"：

- **`Product`（平台标准产品）→ `SupplierProduct`（供应商型号）→
  `Organization`** 三层结构——对照所有竞品，没有一家做出了这个层次，
  这是 VISNDT 相对于货架式电商和黄页型平台真正的结构性优势，重新
  设计会保留并强化，不会推倒
- **参数标准化驱动匹配**（`ParameterGroup`/`ParameterDefinition` +
  加权评分 + 必填硬性淘汰）——方向正确，重新设计会保留算法思路，
  但会在数据模型上更早地把"参数字典"当作一等公民而不是后来才长出来
  的模块（见第四节）
- **JWT 自研鉴权、不绑定第三方 OAuth**——考虑到"中国可访问、无 ICP
  依赖第三方美国服务商"这个约束，这个选择依然正确
- **中国可访问/无 ICP 备案/自托管**这个部署约束——不会变

---

## 三、如果重新设计，产品分层怎么定

结合竞品缺口（智能匹配没人做）和竞品优势（人工撮合、信任机制），
建议把产品从"一个撮合网站"升级成三层：

```
① 自助发现层（对标现有 VISNDT 已经在做的：搜索/分类/参数比对/RFQ）
② 人工撮合层（对标仪表网的客服撮合——高价值订单需要人工介入，
   纯自动化在这个客单价的行业走不通，这一层现有项目完全没有设计）
③ 信任与履约层（对标 ndt.org.cn 的担保交易/认证——解决"素未谋面
   的买卖双方敢不敢下单"这个问题，现有项目也完全没有设计）
```

现有 VISNDT 只做了①，这不是错，是**合理的 MVP 取舍**——先把结构化
数据这个真正的护城河做扎实，再往外扩两层。但如果要"从零规划"，
建议从第一天起就在架构里为②③预留位置（哪怕先不实现），而不是等
用户量上来了发现"合同都是线下签的，平台一分钱看不见"才回头补。

---

## 四、领域模型重新设计——保留结构，调整生长顺序

现有项目的问题不是模型错，是**模型是在开发过程中逐步长出来的**，
导致：`ParameterDefinition` 后来才被追认成"平台工程参数权威"、
`Capability` 概念一度成为独立实体又被收回、`Offer` 承担了它本不该
承担的"供给判断"职责（详见架构报告 4.1 节）。

如果从零设计，建议**第一天就明确五个核心对象的权威边界**（不是
后来补一张权责矩阵，是从建表那一刻就按这个来）：

```
Product          → 平台是唯一事实来源（谁都不能绕过平台定义一个新产品）
ParameterDefinition → 平台是唯一事实来源（供应商只能在平台定义的参数
                       字典内声明数值，不能自造参数名）
SupplierProduct  → 供应商声明 + 平台审核批准（两方共同权威，供应商
                    负责真实性，平台负责规范性）
Offer            → 供应商单方权威（这是商业响应，不该被匹配引擎拿来
                    当"是否有供给"的判断依据——这个职责应该由
                    SupplierProduct.status=PUBLISHED 单独承担）
Organization     → 平台审核 + 组织自治（谁是谁、是否可信由平台把关，
                    组织内部治理归组织自己）
```

**关键改动**：匹配候选逻辑从"Product ACTIVE + 有 ACTIVE Offer"改成
"Product ACTIVE + 有 PUBLISHED 状态的 SupplierProduct"——把"是否
有供给"这件事从"是否报过价"这个偶然信号，改成"是否通过审核、
声明在卖"这个确定性信号。Offer 只在真正进入 RFQ 商务流程后才出现，
不参与匹配阶段的候选筛选。

---

## 五、技术架构重新设计——吸取本项目的教训，不是推翻技术选型

**技术栈本身评价合理，不需要换**（NestJS+Prisma+PostgreSQL 的组合
对这个体量合适，Next.js+Vite+AntD 三端分离也合理）。但从这个项目
的实际经历里，有三条工程纪律值得在"从零开始"时就写进团队规范，
而不是事后补救：

1. **权限判断从第一天起用统一装饰器，不允许每个 service 手写
   `checkXxxAccess` 私有方法**——本项目的跨组织越权漏洞正是因为
   每个模块各写一遍访问控制逻辑，写错了没人能一眼看出来。从零
   设计应该先写好 `@RequireOrgAccess()` 这类统一装饰器，再开始写
   业务代码，而不是等业务代码写完了才发现漏洞
2. **schema 变更必须自带"影响面清单"（测试 fixture、审计策略、
   前端类型）联动更新，不能只改一个文件**——本项目 `categoryId`
   改非空导致 e2e 大面积失败就是反例
3. **执行记录默认追加式，不默认新建文件**——不要等到 900 份报告
   堆起来才建立治理规则，从第一次写执行记录起就用"追加进几个固定
   文件"的模式

**新增一条本项目完全没有的设计**：**为②③层（人工撮合、信任机制）
预留数据模型接口**，哪怕暂不实现——比如 `Organization` 表现在就
留一个 `verificationLevel` 字段的位置（哪怕先只有一个默认值），
比"火烧眉毛才加认证体系，还要考虑给现有几万条数据补字段"要轻松
得多。

---

## 六、核心功能优先级重排（如果是 MVP 从零规划）

对照竞品缺口，建议的优先级顺序（跟本项目实际的开发顺序做对比）：

```
① 参数字典 + Product 目录（本项目已经做了，正确）
② 供应商入驻 + SupplierProduct 审核（本项目已经做了，正确）
③ 搜索/分类/发现（本项目已经做了）
④ 结构化匹配引擎（本项目已经做了，是核心差异化）
⑤ 【本项目缺失】轻量级人工客服介入通道——不是完整的 CRM，只是
   "匹配结果出来后，用户可以一键请求人工顾问协助"这个动作，
   对标仪表网"客服主动跟进"这个高转化率的关键环节
⑥ RFQ/Offer 商务流程（本项目已经做了）
⑦ 【本项目缺失】基础信任标识——不需要一开始就做完整担保交易，
   先做"供应商资质认证展示"（营业执照/行业认证上传+平台核验后
   打标），成本低，能直接对标 ndt.org.cn 的诚信通逻辑，解决"敢不
   敢下单"的心理门槛
⑧ 知识库/内容（本项目已经做了）
⑨ AI/Embedding 语义搜索（本项目已经做了，属于锦上添花，不是
   MVP 必需——对照竞品，没有一家做了这个，属于真正的领先投入，
   但优先级应该低于⑤⑥⑦这类直接影响成交转化率的功能）
```

**这份重排的核心信息是**：本项目在"结构化数据"这个真正的护城河上
投入是对的、甚至领先于所有竞品，但在"促成成交的临门一脚"（人工
撮合、信任标识）上是空白的，而这恰恰是几乎所有参照竞品都在做、
且被验证有效的部分。**如果要往下投入资源，建议优先级给⑤和⑦，
而不是继续深挖 AI 语义搜索这类竞品都还没做的领先功能**——护城河
挖得再深，用户不敢下单也没用。

---

## 七、商业模式设计——结合竞品参照，给出比上一份文档更具体的方向

上一份《商业化产品设计方案》里，商业模式部分是开放式提案。结合
这次竞品调研，可以给出更具体的方向参考（依然是提案，不是结论）：

- **仪表网模式**：广告曝光位+客服人工撮合服务，按年费收供应商，
  不直接抽成交易——这个模式跟"匹配分数不受付费影响"这条护栏
  完全兼容，可以作为优先参考模型
- **ndt.org.cn 模式**：担保交易抽成——技术实现成本高（涉及资金
  监管、对公账户合规），且这个行业客单价高（工业设备动辄数万到
  数十万），大额资金监管的合规门槛比小商品电商更高，**不建议作为
  第一阶段的方向**
- **领翼NDT商城模式**：直接自营电商——这跟 VISNDT"能力发现+撮合"
  的定位冲突（平台变成了经销商），不建议采用

**初步建议方向**（仍是提案）：优先做"年费会员+曝光位+人工客服
坐席"这个仪表网验证过的模式，规避担保交易的合规复杂度，也不偏离
"撮合平台"这个定位。

---

## 八、非功能性需求（从零设计会补的部分）

- **信任标识体系**：资质认证/行业认证展示，从数据模型第一天就
  预留字段
- **人工客服工单系统**：轻量级，不需要重型 CRM，一个"请求顾问协助"
  的表单+内部工单流转即可起步
- **多语言预留**（如果未来有出海计划，虽然当前定位是中国大陆市场，
  但工业设备的供应链天然有跨境属性，NDT 品牌本身很多是国际品牌
  如奥林巴斯/Evident，预留 i18n 结构成本很低，事后补成本很高）

---

## 九、分阶段路线图（从零开始会怎么排）

```
Phase 1：参数字典 + Product/SupplierProduct + 基础发现（对标现有
         项目已完成部分，顺序一致）
Phase 2：匹配引擎（用 SupplierProduct 而非 Offer 作为候选判断依据，
         这是与现有实现的关键差异点）+ 基础信任标识（资质认证展示）
Phase 3：RFQ/Offer 商务闭环 + 轻量人工客服介入通道（这两个并行，
         现有项目是先做了 RFQ 没做客服通道）
Phase 4：知识库/内容 + AI 语义搜索（现有项目投入顺序基本一致）
Phase 5：视情况评估是否需要更重的信任机制（担保交易），大概率
         不需要，除非交易规模验证了这个必要性
```

---

## 十、与现有项目的关系——这不是一份"推倒重来"的文档

对照下来，**现有项目在 Phase 1、4 上跟"从零设计"的结论高度一致**，
说明当初的方向判断基本正确，不需要因为这份文档去否定已经做的工作。
真正值得采纳的是三点增量调整：

1. **匹配候选逻辑从 Offer 改为 SupplierProduct**（第四节，架构改动，
   有实际代码工作量，但不涉及大范围重构）
2. **补齐轻量级信任标识**（第六节⑦，成本低，建议尽快排期）
3. **补齐人工客服介入通道**（第六节⑤，成本低，建议尽快排期）

这三点不需要"从零重来"就能落地，可以直接排进现有的
《项目合理化提升改造建议方案》的 Phase 2/3。

---

## 十一、参数级内联释义系统（ⓘ悬浮/点开小窗）——设计与价值评估

**先纠正一个前提**：这个功能目前项目里**完全不存在**，不是"已经做了
一部分"——直接搜索确认全仓库零命中 `Tooltip`/`Popover` 组件，
`Content.type = INSIGHT` 是"一篇完整的洞察类文章"，跟"参数旁边
一个ⓘ点开小窗解释"是两个不同粒度的东西，容易混淆，先说清楚。

### 11.1 这个功能的真实价值——两个方向都成立

- **服务用户**：工业采购决策者不一定是技术专家（可能是采购部门、
  中间商），"景深ⓘ"点开显示"景深是什么、常见取值范围、为什么这个
  参数会影响你的检测场景选择"，能直接降低决策门槛，减少用户因为
  看不懂参数而流失或者去问客服的情况
- **SEO/AI 引流**：如果这些释义内容是服务端渲染、可被搜索引擎和
  AI 工具抓取的（不是纯前端 JS 弹窗），"景深是什么意思""内窥镜
  景深多少合适"这类长尾搜索词/AI 问答，都可能把流量导向 VISNDT
  的产品页——这跟第四节提到的"结构化数据是护城河"是同一个逻辑的
  延伸：**参数字典不只是给匹配引擎用的，也是内容资产**

### 11.2 建议的实现方式——复用知识库，不新建一套系统

不建议把这个做成独立的新模块，建议**给 `ParameterDefinition` 加一个
可选的关联字段，指向 `KnowledgeEntry`**（现有知识库的最小颗粒度，
本项目已有域/分类/条目三层结构）。理由：
- 知识库的内容管理后台、审核流程已经存在，不用重新做一套编辑界面
- 一条 `KnowledgeEntry` 既可以支撑参数旁边的小窗（取一小段摘要），
  也可以支撑独立的知识库文章页（取完整内容），一份内容两种展示，
  不需要重复维护

**具体交互设计**：
```
参数名旁边一个 ⓘ 图标
  → 点击/悬浮展开一个小卡片，包含：
    - 一句话解释"是什么"
    - 常见取值范围/可选项列表（如果是 ENUM 类型，直接读
      ParameterDefinition 现有的 options 字段，不用额外维护）
    - "为什么重要"的一到两句工程说明
    - 可选的"查看完整说明"链接，跳到对应的知识库文章（如果有）
```

**服务端渲染要求**：这个小窗内容必须是页面初始 HTML 的一部分（哪怕
默认视觉上折叠），不能是纯客户端异步加载后才有内容——否则达不到
"被搜索引擎/AI 抓取"这个引流目的，这一点是这个功能能不能达到第二个
价值（SEO/AI 引流）的关键实现细节，做的时候要注意。

### 11.3 优先级建议

内容成本是主要瓶颈（每个参数的释义需要人工撰写，不是纯技术工作）。
建议**先给使用频率最高的 10-20 个核心参数写释义**（比如探头直径、
工作长度、视场角、分辨率这类几乎每个产品都有的参数），验证有没有
带来搜索流量或者降低客服咨询量，再决定要不要覆盖全部参数字典。
不建议一开始就要求"所有参数都要有释义"，会变成内容产出的负担，
拖慢其他优先级更高的工作（第六节提到的人工客服通道、信任标识）。

---

## 十二、其他可以进一步完善项目的功能/设计补充

### 12.1 产品对比表——已有基础，建议强化关联

直接读仓库确认 `apps/web/src/app/products/compare/page.tsx`
（407 行）已经是真实实现，不是空页面。**建议的强化方向**：让参数
对比表里的每个参数名也挂上 11 节的ⓘ释义——用户在对比两个产品的
"分辨率"时，正是最需要"这个参数为什么重要"这个信息的场景，两个
功能放在一起做，价值会比分开做更大。

### 12.2 检测标准/规范标签——贴合 NDT 行业特性的功能

无损检测行业高度依赖标准规范（比如 GB/T、ASTM、ISO 系列标准，
不同标准对同一检测方法的验收要求不同）。建议给 `Product` 或
`ParameterDefinition` 增加一个可选的"适用标准"标签（比如"符合
ASTM E165 渗透检测标准"），这跟第七节调研发现的行业特性直接相关——
工业采购方在选型时经常是"先确定要满足什么标准，再倒推需要什么参数
的设备"，反过来把标准也变成一个可检索/可筛选的维度，能覆盖这类
"从标准出发"的搜索路径，是参数字典体系的自然延伸，不需要新建
独立系统，加一个字段+一个筛选维度即可。

### 12.3 应用场景内容与产品的双向关联——强化现有 SOLUTION 内容类型

现有 `Content.type = SOLUTION` 已经存在，建议确认（如果还没有）
这类内容跟具体 `Product`/`ProductCategory` 的关联是双向可见的——
不只是"知识库里有一篇讲航空发动机叶片检测的文章"，而是"用户在看
某个内窥镜产品详情页时，能看到'常见应用场景：航空发动机叶片检测'
这条链接，点进去就是对应的 SOLUTION 内容"。这个如果现有
`ProductCategoryKnowledgeMapping` 结构已经支持，只是前端还没做
双向展示，那是低成本的补充；如果结构本身不支持，再评估要不要加。

### 12.4 商业模式补充想法：内容驱动的免费流量入口

结合 11 节的参数释义、12.2 的标准标签、知识库现有内容，这些其实
都是"免费获取搜索引擎/AI 流量"的入口，不需要额外营销预算。这跟
第七节建议的"仪表网模式"（年费会员+曝光位）可以形成配合：**免费
内容吸引流量和用户信任，付费会员获得在这批流量里的曝光优先权**，
不冲突，是同一个漏斗的两端。这不需要现在就设计出完整方案，只是
提醒你：11、12.1、12.2、12.3 这几个"内容型"功能不要孤立看待，
它们共同构成的是获客成本更低的自然流量入口，价值可能比继续投入
AI 语义搜索这类竞品都还没做的领先功能更直接。

---

## 十三、面向"小而精、人力资金有限"的补充方案——把已有 AI 基础设施用在减轻自己负担上

前面几节的功能大多是"给用户用"的，这一节的几条思路是反过来的：
**用现成的 embedding/RAG 基础设施（`apps/api/src/ai/` 已经存在，
不是要新建）去降低你自己 1-2 人团队的人工成本**，这比继续给用户
加功能更直接地解决你现在的约束。

### 13.1 AI 辅助结构化录入——解决你最大的瓶颈

你提到产品信息由平台统一录入，1-2 人要维护一个不断增长的目录，
**手工录入速度是硬瓶颈**。建议：供应商提供的规格书（PDF/图片/自由
文本描述）先丢给 AI 做一次"预提取"，AI 按 `ParameterDefinition` 
字典尝试解析出"探头直径=4.0mm、工作长度=1000mm……"这样的结构化
草稿，人工只需要**核对+修正**而不是从零手打。这不是新建功能，是把
现有 `ai/` 模块的能力从"对外的语义搜索"延伸到"对内的录入辅助"，
复用度很高，投入产出比目前所有补充建议里最高——直接缓解你说的
"最初完成、后期少量新品也能人工录入"这个目标的实际执行难度。

### 13.2 AI 辅助询价初筛——延续第六节"人工客服通道"，但省人力

第六节建议加一个"请求人工顾问协助"的轻量入口，这里补充一步：**AI
先做第一轮分类和草拟**——买方提交询价后，AI 自动判断这条询价属于
哪个产品分类、匹配度大概如何、草拟一条初步回复，人工只需要**审核
后发送**而不是从头写。这跟仪表网"客服人工跟进转化率高"这个经验
不冲突——**转化环节还是人在把关，只是人不用做机械性的初稿工作**，
对 1-2 人团队来说，这个差别决定了"能不能撑住询价量增长"。

### 13.3 自动化数据健康检查——让目录"少人也能自转"

不需要专门配一个人巡查产品目录有没有过期/缺字段/供应商联系方式
失效，建议做一个轻量的**定时健康检查任务**：
- 标记"缺关键参数"的产品（比如某个 ENUM 参数字典更新了，但已有
  产品还在用旧值）
- 标记"长期未更新"的供应商型号（比如超过 6 个月没有变动，可能已经
  停产或者信息过时）
- 汇总成一个"待处理清单"，你打开后台看这一个列表就知道该做什么，
  不用凭印象去翻几百条数据

这个投入很小（一个定时任务+一个汇总视图），但对"最小人力自运转"
这个目标是直接命中的——**把"要不要检查数据"从人的记忆负担，变成
系统主动推给你的待办**。

### 13.4 轻量信任标识——用公开数据源自动化第一层，而不是人工审核

第六节提到的"资质认证展示"，如果完全靠人工审核营业执照/证书，
对 1-2 人团队是额外负担。建议分两层：
- **基础层（自动）**：接入企业公开信用信息查询（比如企查查/天眼查
  这类第三方数据接口，通常有免费或低成本的 API 方案），自动核验
  "这家公司是否真实存在、经营状态是否正常"，打一个基础认证标——
  这一步几乎不占用你的人工时间
- **进阶层（人工，可选）**：行业资质证书这类需要人工判断真实性的
  认证，作为更高等级的标识，量少的时候你自己审，量大了再考虑要不要
  外包或者招人

这样"信任标识"这个此前建议里成本较高的部分，被拆成了"自动能覆盖
大部分场景，人工只处理少数高价值案例"，符合你的资源约束。

### 13.5 不建议现在做的部分——明确排除项，帮你守住"小而精"

为了不让这份文档变成"什么都建议做"，明确写几条**现在不建议投入**
的方向，避免精力被拉散：
- **完整 CRM 系统**——13.2 的轻量询价初筛够用，不需要重型客户关系
  管理系统
- **担保交易/资金监管**——第七节已经分析过，合规成本高，现阶段
  不建议
- **多端 App（移动端原生应用）**——网页响应式足够覆盖当前阶段，
  原生 App 开发/维护成本对 1-2 人团队不成比例
- **多语言国际化的完整实现**——第八节建议"预留结构"（成本低），
  不等于"现在就要做完整多语言"，两者不要混淆

---
