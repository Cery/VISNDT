# 362_M14.2.6_Workspace_Final_Integrity_Audit_Report

## 1. Execution Summary

- 任务模式：Audit Only
- Git 根目录：`F:\Desktop\VISNDT`
- 业务目录：`F:\Desktop\VISNDT\VISNDT`
- 分支：`main`
- 审计目标：验证 M14.2.5 Workspace Runtime 系列任务完成后的整体完整性，重点覆盖 Workspace 路由、Dashboard 命名空间迁移、Sidebar 映射、访问控制边界、API Client 调用链与构建完整性
- 审计边界：只读检查 `apps/web/src/app/workspace/**`、`apps/web/src/components/workspace/**`、`apps/web/src/components/layout/**`、`apps/web/src/lib/api/**`、`apps/web/src/services/**`，后端仅只读查看 `apps/api/src/**`

### 1.1 git status --short

```text
 M VISNDT/apps/web/src/app/dashboard/page.tsx
 M VISNDT/apps/web/src/app/login/page.tsx
 M VISNDT/apps/web/src/app/register/page.tsx
 M VISNDT/apps/web/src/app/workspace/rfqs/[id]/page.tsx
 M VISNDT/apps/web/src/components/layout/PublicHeader.tsx
 M VISNDT/apps/web/src/components/workspace/WorkspaceSidebar.tsx
 M VISNDT/apps/web/src/lib/api/rfqs.ts
 M VISNDT/apps/web/src/lib/constants.ts
 M VISNDT/apps/web/src/services/rfq.service.ts
 M docs/_review/M_Series_Development_Control_Plan.md
?? VISNDT/apps/web/src/app/workspace/dashboard/
?? docs/_review/359_M14.2.5_RFQ_Detail_Response_Integrity_Fix_Report.md
?? docs/_review/360_M14.2.5_Workspace_Dashboard_Namespace_Decision_Audit_Report.md
?? docs/_review/361_M14.2.5_Workspace_Dashboard_Namespace_Migration_Report.md
?? docs/_review/361_M_Series_Control_Plan_Temporary_Task_Insert_Report.md
```

说明：当前仓库存在既有修改与未跟踪文件。本次审计未覆盖、未回滚、未修改上述业务代码，仅基于当前工作树状态进行只读核验。

## 2. Route Integrity Matrix

### 2.1 Buyer Workspace

| Route | Page Exists | Build Included | Status |
| --- | --- | --- | --- |
| `/workspace/dashboard` | Yes | Yes (`○`) | PASS |
| `/workspace/demands` | Yes | Yes (`○`) | PASS |
| `/workspace/demands/create` | Yes | Yes (`○`) | PASS |
| `/workspace/demands/[id]` | Yes | Yes (`ƒ`) | PASS |
| `/workspace/rfqs` | Yes | Yes (`○`) | PASS |
| `/workspace/rfqs/create` | Yes | Yes (`○`) | PASS |
| `/workspace/rfqs/[id]` | Yes | Yes (`ƒ`) | PASS |
| `/workspace/matches` | Yes | Yes (`○`) | PASS |
| `/workspace/notifications` | Yes | Yes (`○`) | PASS |
| `/workspace/settings` | Yes | Yes (`○`) | PASS |

### 2.2 Supplier Workspace

| Route | Page Exists | Build Included | Status |
| --- | --- | --- | --- |
| `/workspace/supplier` | Yes | Yes (`○`) | PASS |
| `/workspace/supplier/rfqs` | Yes | Yes (`○`) | PASS |
| `/workspace/supplier/rfqs/[id]` | Yes | Yes (`ƒ`) | PASS |
| `/workspace/supplier/responses` | Yes | Yes (`○`) | PASS |

### 2.3 Additional Workspace Route

| Route | Page Exists | Build Included | Status |
| --- | --- | --- | --- |
| `/workspace` | Yes | Yes (`○`) | PASS |

结论：任务要求中的全部 Workspace 路由均存在，且均进入 `pnpm --filter @visndt/web build` 输出。

## 3. Dashboard Migration Verification

### 3.1 `/dashboard -> /workspace/dashboard`

- `apps/web/src/app/dashboard/page.tsx` 当前实现为：
  - `redirect('/workspace/dashboard')`
- `apps/web/src/app/workspace/dashboard/page.tsx` 已存在，且保留 Buyer Dashboard 业务逻辑：
  - 统计数据：`getDemands(1, 1)`、`getMyInquiries(1, 1)`、`getUnreadCount()`
  - 快捷入口：`/workspace/demands`、`/workspace/rfqs`、`/workspace/matches`
  - 权限包裹：`AuthGuard + RoleGuard roles={['BUYER']}`

### 3.2 默认角色补偿检查

已执行搜索：

- `workspaceRole ??`
- `workspaceRole ||`

结果：在 `apps/web/src` 范围内未发现默认角色补偿写法。

### 3.3 `/dashboard` 全局引用结果

`apps/web/src` 范围内，旧 `/dashboard` 业务导航已完成迁移；当前仅保留：

1. `apps/web/src/app/dashboard/page.tsx` 兼容跳转页

以下位置已改为 `/workspace/dashboard`：

1. `WorkspaceSidebar.tsx`
2. `PublicHeader.tsx`
3. `login/page.tsx`
4. `register/page.tsx`
5. `lib/constants.ts`

其余 `/dashboard` 命中主要位于：

1. 历史审计/报告文档
2. `apps/admin` 的 `/admin/dashboard/*` 管理后台上下文

结论：Dashboard Namespace 迁移整体完成，旧 `/dashboard` 已降级为兼容 redirect。

## 4. Sidebar Mapping Audit

### 4.1 BUYER

| Label | Href | Real Page Exists | Result |
| --- | --- | --- | --- |
| 仪表盘 | `/workspace/dashboard` | Yes | PASS |
| 我的需求 | `/workspace/demands` | Yes | PASS |
| 询价单 | `/workspace/rfqs` | Yes | PASS |
| 匹配结果 | `/workspace/matches` | Yes | PASS |
| 通知中心 | `/workspace/notifications` | Yes | PASS |
| 设置 | `/workspace/settings` | Yes | PASS |

### 4.2 SUPPLIER

| Label | Href | Real Page Exists | Result |
| --- | --- | --- | --- |
| 供应商工作台 | `/workspace/supplier` | Yes | PASS |
| RFQ响应 | `/workspace/supplier/rfqs` | Yes | PASS |
| 我的响应 | `/workspace/supplier/responses` | Yes | PASS |
| 通知中心 | `/workspace/notifications` | Yes | PASS |
| 设置 | `/workspace/settings` | Yes | PASS |

### 4.3 null

| Role | Sidebar Behavior | Result |
| --- | --- | --- |
| `workspaceRole = null` | 不渲染导航项，显示“当前账号未映射到可用工作区”提示 | PASS |

结论：Sidebar 配置与真实页面文件已对齐，未发现 Sidebar href 指向不存在页面的情况。

## 5. Access Control Audit

### 5.1 Buyer / Shared Route Guard Matrix

| Route | AuthGuard | RoleGuard | Result |
| --- | --- | --- | --- |
| `/workspace` | Yes | `['BUYER', 'SUPPLIER']` | PASS |
| `/workspace/dashboard` | Yes | `['BUYER']` | PASS |
| `/workspace/demands` | Yes | `['BUYER']` | PASS |
| `/workspace/demands/create` | Yes | `['BUYER']` | PASS |
| `/workspace/demands/[id]` | Yes | `['BUYER']` | PASS |
| `/workspace/rfqs` | Yes | `['BUYER']` | PASS |
| `/workspace/rfqs/create` | Yes | `['BUYER']` | PASS |
| `/workspace/rfqs/[id]` | Yes | `['BUYER']` | PASS |
| `/workspace/matches` | Yes | `['BUYER']` | PASS |
| `/workspace/notifications` | Yes | `['BUYER', 'SUPPLIER']` | PASS |
| `/workspace/settings` | Yes | `['BUYER', 'SUPPLIER']` | PASS |

### 5.2 Supplier Route Guard Summary

| Route | AuthGuard | Supplier Permission Logic | Result |
| --- | --- | --- | --- |
| `/workspace/supplier` | Yes | `RoleGuard roles={['SUPPLIER']}` 位于内容内部 | PASS WITH RISK |
| `/workspace/supplier/rfqs` | Yes | `RoleGuard roles={['SUPPLIER']}` 位于内容内部 | PASS WITH RISK |
| `/workspace/supplier/rfqs/[id]` | Yes | `RoleGuard roles={['SUPPLIER']}` 位于内容内部 | PASS WITH RISK |
| `/workspace/supplier/responses` | Yes | `RoleGuard roles={['SUPPLIER']}` 位于内容内部 | PASS WITH RISK |

### 5.3 关键审计结论

1. Buyer 页面权限边界满足 `AuthGuard + RoleGuard roles={['BUYER']}` 要求。
2. Notifications 页面允许 `BUYER` / `SUPPLIER`，并通过 `RoleGuard` 拒绝 `workspaceRole = null`。
3. 未发现 `workspaceRole ?? 'BUYER'`、`workspaceRole || 'BUYER'`、`workspaceRole ?? 'SUPPLIER'`、`workspaceRole || 'SUPPLIER'`。
4. Supplier 页面虽然存在 `RoleGuard`，但多处将数据加载逻辑放在 `RoleGuard` 外层；即页面先触发 `useEffect` 请求，再在渲染层做角色拒绝，权限边界一致性弱于 Buyer 页面。

## 6. API Client Integrity Matrix

| Page | Frontend API | Backend Endpoint | Result |
| --- | --- | --- | --- |
| `/workspace/dashboard` | `getDemands` + `getMyInquiries` + `getUnreadCount` | `GET /demands/my` + `GET /inquiries/mine` + `GET /notifications/unread-count` | PASS |
| `/workspace/demands` | `getDemands` | `GET /demands/my` | PASS |
| `/workspace/demands/create` | `createDemand` | `POST /demands` | PASS |
| `/workspace/demands/[id]` 读取链路 | `getDemand` + `getDemandMatches` | `GET /demands/:id` + `GET /demands/:id/matches` | PASS |
| `/workspace/demands/[id]` 动作链路 | `publishDemand` + `closeDemand` + `deleteDemand` | `POST /demands/:id/publish` + `POST /demands/:id/close` + `DELETE /demands/:id` | HIGH RISK: 删除按钮暴露给 Buyer，但后端 `DELETE /demands/:id` 为 `ADMIN only` |
| `/workspace/rfqs` | `getRfqs` | `GET /rfqs/mine` | PASS |
| `/workspace/rfqs/create` | `getMyDemands` + `createRfq` | `GET /demands/my` + `POST /rfqs` | PASS |
| `/workspace/rfqs/[id]` 读取链路 | `getRfq` + `getRfqResponses` | `GET /rfqs/:id` + `GET /rfqs/:id/responses` | PASS |
| `/workspace/rfqs/[id]` 动作链路 | `publishRfq` + `closeRfq` + `deleteRfq` | `POST /rfqs/:id/publish` + `POST /rfqs/:id/close` + `DELETE /rfqs/:id` | HIGH RISK: 删除按钮暴露给 Buyer，但后端 `DELETE /rfqs/:id` 为 `ADMIN only` |
| `/workspace/matches` | `getAllMatches` + `updateMatchStatus` + `rematchDemand` | `GET /demands/:id/matches` + `PATCH /demands/:id/matches/:matchId` + `POST /demands/:id/rematch` | PASS |
| `/workspace/notifications` | `getNotifications` + `getUnreadCount` + `markAsRead` + `markAllAsRead` | `GET /notifications` + `GET /notifications/unread-count` + `PATCH /notifications/:id/read` + `PATCH /notifications/read-all` | PASS |
| `/workspace/supplier/rfqs` | `getAvailableRfqs` | `GET /rfqs/available` | PASS WITH RISK: 数据请求发生在 `RoleGuard` 渲染校验之前 |
| `/workspace/supplier/rfqs/[id]` | `getRfq` + `createRfqResponse` | `GET /rfqs/:id` + `POST /rfqs/:id/responses` | PASS WITH RISK: `getRfq` 在 `RoleGuard` 之前触发 |
| `/workspace/supplier/responses` | `getMyRfqResponses` | `GET /rfq-responses/mine` | PASS WITH RISK: 数据请求发生在 `RoleGuard` 渲染校验之前 |

### 6.1 额外断链检查

发现以下前端导航目标不存在对应页面：

| Source Page | Navigation Target | Route File Exists | Risk |
| --- | --- | --- | --- |
| `/workspace/demands/[id]` | `/workspace/demands/${id}/edit` | No | HIGH |
| `/workspace/rfqs/[id]` | `/workspace/rfqs/${id}/edit` | No | HIGH |

结论：

1. Demand / RFQ / Match / Notification / Dashboard 主数据链路基本正确。
2. RFQ Detail 响应来源已使用 `GET /rfqs/:id/responses`，未再使用 `GET /rfq-responses/mine` 作为 Buyer RFQ Detail 响应来源。
3. 存在两个明确的前端断链与两个明确的删除权限映射不一致问题。

## 7. Build Result

### 7.1 Command

```text
pnpm --filter @visndt/web build
```

### 7.2 Result

- Build：`PASS`

### 7.3 Workspace Routes in Build Output

```text
├ ○ /workspace
├ ○ /workspace/dashboard
├ ○ /workspace/demands
├ ƒ /workspace/demands/[id]
├ ○ /workspace/demands/create
├ ○ /workspace/matches
├ ○ /workspace/notifications
├ ○ /workspace/rfqs
├ ƒ /workspace/rfqs/[id]
├ ○ /workspace/rfqs/create
├ ○ /workspace/settings
├ ○ /workspace/supplier
├ ○ /workspace/supplier/responses
├ ○ /workspace/supplier/rfqs
└ ƒ /workspace/supplier/rfqs/[id]
```

### 7.4 Dashboard Build Verification

```text
├ ○ /dashboard
├ ○ /workspace/dashboard
```

说明：`/dashboard` 仍存在于构建输出，但当前职责为兼容 redirect；`/workspace/dashboard` 已成功进入构建输出。

## 8. Warning Inventory

本次构建存在以下已知 warning，未在本任务处理：

1. `src/app/layout.tsx`
   - `@next/next/no-page-custom-font`
2. `src/components/products/ManufacturerInfo.tsx`
   - `@typescript-eslint/no-unused-vars`

## 9. Risk List

### Critical

- None

### High

1. Buyer Demand Detail 页面暴露了“编辑”入口，但 `/workspace/demands/[id]/edit` 对应路由文件不存在，点击后将形成前端断链。
2. Buyer RFQ Detail 页面暴露了“编辑”入口，但 `/workspace/rfqs/[id]/edit` 对应路由文件不存在，点击后将形成前端断链。
3. Buyer Demand Detail 页面暴露了删除动作，但前端调用的 `DELETE /demands/:id` 在后端为 `ADMIN only`，Buyer 用户会遭遇权限不一致的硬失败。
4. Buyer RFQ Detail 页面暴露了删除动作，但前端调用的 `DELETE /rfqs/:id` 在后端为 `ADMIN only`，Buyer 用户会遭遇权限不一致的硬失败。

### Medium

1. Supplier 路由（`/workspace/supplier/rfqs`、`/workspace/supplier/rfqs/[id]`、`/workspace/supplier/responses`）的 `RoleGuard` 位于内容内部，数据请求在 `RoleGuard` 渲染校验前就会触发，导致权限边界与请求时机不完全一致。

### Low

- None

## 10. Final Assessment

### PASS WITH RISKS

理由：

1. 任务要求中的 Workspace 路由已全部存在，并已进入构建输出。
2. Dashboard Namespace 迁移已完成，旧 `/dashboard` 已收敛为兼容 redirect。
3. Sidebar 映射与真实页面基本一致，未发现 Sidebar 自身的 href 断链。
4. 构建通过，且未发现 `workspaceRole` 默认角色补偿。
5. 但当前仍存在用户可见的高风险断链与删除权限映射不一致问题，尚不能判定为无风险通过。

### 下一阶段建议

1. 以单独任务处理 Buyer Demand / RFQ Detail 的“编辑”断链问题，明确是补齐路由还是移除入口。
2. 以单独任务处理 Buyer Delete 动作与后端 `ADMIN only` 删除接口的契约不一致问题。
3. 以单独任务收敛 Supplier 页面权限边界，使 `RoleGuard` 在数据请求之前生效，避免“页面被拒绝但请求已发送”的不一致。
