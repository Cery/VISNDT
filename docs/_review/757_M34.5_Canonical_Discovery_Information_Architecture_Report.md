# 757_M34.5_Canonical_Discovery_Information_Architecture_Report

## 1. Task Summary

**Task ID**: `757_M34.5_Canonical_Discovery_Information_Architecture`

**Task Status**: **CONDITIONAL PASS**

**Scope**: Canonical Discovery Information Architecture。将已建立的 Entity/Discovery Foundation 正式组织为统一的 **DISCOVER 信息架构（Search / Categories / Capabilities / Products / Suppliers）**，建立可进入、可回退、可理解、可回环的跨实体发现路径，并明确 Public / Workspace / Admin 边界。

**Execution Principle**: M34.5 = Canonical Discovery Information Architecture，**不是 Website/Visual Redesign、不是 Search 2.0、不是 Supplier Marketplace**。核心平台路径保持 Entity → Discovery → Evaluation → Connection → Governance；本任务只执行 **Entity → Discovery**。

本报告记录（1）Pre-existing vs 757 Change 归因、（2）当前与目标 IA 清单、（3）Route/Nav 变更、（4）Entity Discovery Path、（5）Architecture Authority 验证、（6）Public/Workspace 边界、（7）分层 Runtime / Empty-data / Mobile / Static / Regression 证据、（8）M34 Roadmap 对齐、（9）Remaining Conditions、（10）Final Decision。746–756 历史报告 **Preserved（未修改）**，本报告未覆盖任何历史报告。

## 2. Repository / Code Root / Branch / Commit / Working Tree

- **Repository Root**: `F:\Desktop\VISNDT`
- **Code Root**: `F:\Desktop\VISNDT\VISNDT`
- **Branch**: `main`
- **Commit**: `ff03a9a`
- **Working Tree**: **OTHER**（存在大量 Modified + Untracked，均为历史累积）
- **757 唯一代码变更**: `apps/web/src/components/layout/PublicHeader.tsx`（NAV_ITEMS 导航语义收敛）

> Working Tree 中列出的其余改动（admin/web 组件、design-tokens、css、`capability-context.ts`、`supplier-context.ts`、`search.service.ts`、database 探针等）均为 **Pre-existing（M31 / M33 / M34.1–M34.4 / 756）**，不归因 757。

## 3. Pre-existing vs 757 Change Attribution

| 变更 | 归因 | 说明 |
|------|------|------|
| `apps/web/src/components/layout/PublicHeader.tsx`（NAV_ITEMS） | **757** | 唯一本次代码变更：导航语义收敛为 DISCOVER 主线 |
| `apps/api/src/search/search.service.ts` | Pre-existing（755/756） | 本任务未触碰 |
| `apps/web/src/lib/capability-context.ts`、`supplier-context.ts` | Pre-existing（M34.1/752 / M34.2/753） | 仅引用验证架构权威，未改动 |
| admin/web 组件、design-tokens、css | Pre-existing（M31/M33） | 未改动 |
| database 探针/脚本 | Pre-existing（QA） | 未改动 |

## 4. Current IA Inventory（改前的实际 IA）

- 主导航（PublicHeader NAV_ITEMS 修改前）：首页 / 产品中心 / 产品分类 / 解决方案 / 知识中心 / 商务合作 / 关于我们 = **官网优先（Product/Content-first）平铺结构**。
- 实际 canonical 路由面已存在：`/`、`/search`、`/categories`、`/products`、`/products/[slug]`、`/suppliers/[id]`、`/solutions`、`/knowledge`、`/business`、`/about`、`/workspace/*`、`/dashboard/*`。
- 缺失统一 DISCOVER 主线；供应商公开发现入口未在主导航呈现（仅依赖搜索 Tab）；Capability 语义无独立导航语义（"产品分类"/"产品中心"命名偏商品目录）。
- 内容（Solutions/Knowledge/About/Business）与发现入口同层级平铺，Content 更像 Platform Core。

## 5. Target Canonical IA

```
Discovery（DISCOVER）
├── Search             → /search
├── Categories         → /categories（能力分类）
├── Capabilities       → Product 的发现语义角色（/products 能力详情权威 + 能力分类）
├── Products           → /products
└── Suppliers          → /search?type=supplier-product（公开入口）+ /suppliers/[id]（详情）

Content = Supporting Layer（解决方案 / 知识中心）
Public：/ /search /categories /products /suppliers（公开只读）
Workspace：/dashboard/buyer（BUYER）/ /dashboard/supplier（SUPPLIER）/ ADMIN运营中心
```

**产品化 Gate 满足**：VISNDT 不再以 Home → Product → About → Article → Contact 为主要 Public 信息架构，主导航主线 = DISCOVER；Content = Supporting Layer，而非 Platform Core。

## 6. Route / Navigation Changes

**Nav Semantic Convergence（受控，非视觉/Global Rewrite）**：

```
修改前（7项，官网优先）          →  修改后（7项，DISCOVER 主线）
首页                            →  首页
产品中心                        →  搜索(/search)
产品分类                        →  能力分类(/categories)
解决方案                        →  产品中心(/products)
知识中心                        →  能力型号/供应商(/search?type=supplier-product)  ☆ 新增
商务合作 (移至footer)              →  解决方案
关于我们 (移至footer)              →  知识中心
```

- **新增供应商公开发现入口** `/search?type=supplier-product`：复用现有 `/search` 搜索面，**不新增路由**；经 `parseType(searchParams.get('type'))`（`SearchPageContent.tsx` L97）正确落地到能力型号 Tab。
- `/business`、`/about` 从主导航移除为语义收敛，**仍可由 PublicFooter 访问（保留可达，无死链）**（footer「平台」栏含 `/about`、`/business`）。
- **无 Route Migration**：`/search` `/categories` `/products` `/suppliers/[id]` 均为既有 canonical 路由，本次仅收敛导航语义、保留 SEO/导航语义。
- 遗留静态路由 `/supplier-models`（207B）存在，**未改动（不虚构收敛）→ Future Candidate**。

## 7. Entity Discovery Path

| Path | Expected | Status |
|------|----------|--------|
| Search → Category → Product | categoryPath `/products?categoryId=` | **STRUCTURAL VERIFIED** |
| Search → Product → Supplier | SupplierProductResultCard → `/products/[slug]` → SupplierCapabilityList/SupplierInfo → `/suppliers/${org.id}` | **STRUCTURAL VERIFIED** |
| Category → Product | `/categories` 分类 → `/products?categoryId=` | **STRUCTURAL VERIFIED** |
| Product → Supplier | `/suppliers/${org.id}`（SupplierInfo.tsx L62 / SupplierCapabilityList.tsx L154） | **STRUCTURAL VERIFIED** |
| Supplier → Published SupplyProduct → Product | `/suppliers/[id]` 供应能力区（relatedProduct 回溯） | **STRUCTURAL**（真实数据 runtime=UNVERIFIED） |
| Public → Workspace | `/login` / `/dashboard` 认证入口 | **BOUNDARY VERIFIED** |

**实体关系语义（§8 Architecture Authority）**：Search → Category/Capability → Product → Supplier → Back to Discovery；Capability = Product Semantic Role；Product = Canonical Platform Object；SupplyProduct = Supplier-owned Commercial Product；Supplier = Organization(type=SUPPLIER)；Specification = ParameterDefinition Authority。

## 8. Architecture Authority Verification（ONE 权威集，Second-Authority = NONE）

| 权威 | 定义 | 证据来源 | 通过 |
|------|------|----------|------|
| Capability = Product Semantic Authority | identity = product.id，非独立 Entity/Table | `capability-context.ts`（buildCapabilityContext） | PASS |
| Product = Canonical Platform Object | 平台权威能力/产品对象 | M34 Contract §1.1 / capability-context | PASS |
| Supplier = Organization(type=SUPPLIER) | 语义角色，非独立 Supplier 表 | `supplier-context.ts` / schema 无 supplier 表 | PASS |
| SupplyProduct = Supplier-owned Commercial Product | 供应商自有商用产品，非 Capability | M34 Contract §1.2 | PASS |
| Specification = ParameterDefinition Authority | 参数字典权威 | M34.3/754 | PASS |

**Architecture Gate = PASS**：Capability/Product/Supplier/Specification 均只有 ONE authority，无双 authority。遇到第二套 authority = BLOCKED——**未发生**。

## 9. Public / Workspace / Admin Boundary

- **Public Discovery**：`/` `/search` `/categories` `/products` `/suppliers` = 公开只读。
- **BUYER WORKSPACE** = `/dashboard/buyer`；**SUPPLIER WORKSPACE** = `/dashboard/supplier`；**ADMIN 运营中心**（`/dashboard/*` + Admin app）与 Public Discovery 分离。
- 以**认证入口**分界（`/login` → 工作台）；Public/Workspace 对象引用语义不同步破坏边界。Public → Workspace = **BOUNDARY VERIFIED**。

## 10. Runtime Evidence（分层）

| 层 | Status | 证据 |
|----|--------|------|
| **Route Reachability** | **VERIFIED** | 生产 `next start` @ `http://localhost:3001`，`GET` `/` `/search` `/search?type=supplier-product` `/categories` `/products` `/solutions` `/knowledge` `/about` `/business` 全部 **200** |
| **Navigation / Query Wiring** | **VERIFIED·STRUCTURAL** | homepage SSR HTML 渲染 DISCOVER 主线标签（能力分类/能力型号·供应商/搜索/产品中心）；`/search?type=supplier-product` 经 `parseType` 落地能力型号 Tab |
| **Real Data Population** | **UNVERIFIED** | product=0 / supplier_product=0 / offer=0（合法空数据），后端本次未运行故未重复查询 |

> **GET only**：本次运行时验证仅 GET/读操作，无 POST/PUT/PATCH/DELETE，无业务写入。

## 11. Empty-data Evidence

- **product=0 / supplier_product=0 / offer=0**（沿用 756 对账基线）；taxonomy=28/11/54/45；param_value=0。
- 由此导致：
  - **Real Product Result Population = UNVERIFIED**
  - **Real Supplier Result Population = UNVERIFIED**
  - **Real Capability/SupplierModel Result Population = UNVERIFIED**
- 明确：**HTTP 200 ≠ Experience Complete；Empty List ≠ Feature Broken**。Route Reachability 与 Navigation Wiring 的 VERIFIED **不**等同于真实数据体验完成——本报告不伪造 real-data 填充。

## 12. Mobile Evidence（分层）

- **Responsive Structure**：**VERIFIED（结构）**——主导航 count=7 不变、移动端 `< lg` 使用 hamburger 折叠菜单、`/business`·`/about` footer 可达。
- **Browser Runtime Evidence**：受 **No-Visual-Loop Gate** 约束，未重入逐屏浏览器视觉审计；仅以 SSR HTML（服务端渲染主导航）作为导航运行时证据。
- **Known Historical Overflow**：768≈140px 历史溢出 = 既有基线问题（754/755/756 持续记录），**Carry Forward → Future Candidate**，不归因 757、本次不静默修复。

## 13. Static Verification（逐项）

| 项 | 结果 |
|----|------|
| `pnpm --filter @visndt/api build` | **exit 0**（`nest build`） |
| `pnpm --filter @visndt/web lint` | **exit 0**（`next lint`，仅存量 warnings，PublicHeader 零告警） |
| `pnpm --filter @visndt/web build` | **exit 0**（44 静态页全部生成，含 `/search` `/categories` `/products` `/suppliers/[id]`） |
| API lint | **PRE-EXISTING TOOLING GAP**（`apps/api` 无 `eslint.config.*`，不可运行——**不伪造 PASS**） |

## 14. Regression Verification

- **Auth / RBAC / Demand / Matching / RFQ / RFQResponse / Inquiry**：本次仅 `PublicHeader` NAV_ITEMS 语义收敛，未触及上述任何业务逻辑/API/数据，**NO BEHAVIOR CHANGE**。
- **Search / Category / Product / Supplier / Workspace Entry**：均未被非授权修改；导航语义收敛不影响其业务行为，**无未授权回归**。

## 15. Scope Compliance

| 维度 | 结果 |
|------|------|
| Schema | **NO CHANGE** |
| Migration | **NONE** |
| Database Mutation | **NONE** |
| Test Data Creation | **NONE** |
| AI / Vector / RAG / Semantic Search | **NONE** |
| Matching | **NO CHANGE** |
| RFQ | **NO CHANGE** |
| Transaction | **NONE** |
| 新增 Capability / Supplier / Specification Domain Authority | **NONE** |
| Search Index / Intent Engine | **NONE** |
| Global UI / Header / Homepage Rewrite | **NONE**（仅主导航语义收敛） |
| Route Migration | **NONE**（复用现有 canonical 路由） |

**Mandatory Scope Gate = PASS**：执行过程中**未**发现需新增 Capability/Supplier/Specification Authority、Schema/Migration、Search Index、Semantic Search、Matching/RFQ 修改、大量 Route Migration 或 Global UI Rewrite，未现场扩大范围。

## 16. M34 Roadmap Alignment（当前状态）

| 阶段 | 状态 |
|------|------|
| M34.0 | **CONDITIONAL** |
| M34.1 | **COMPLETE / CONDITIONAL** |
| M34.2 | **COMPLETE / CONDITIONAL** |
| M34.3 | **COMPLETE / CONDITIONAL** |
| M34.4 | **CONDITIONAL PASS** |
| M34.4R | **CONDITIONAL PASS** |
| **M34.5** | **CONDITIONAL PASS**（本任务） |
| M34.6 | **NOT AUTHORIZED**（不因 757 实施自动 READY） |
| M34.7 | **NOT AUTHORIZED**（不因 757 实施自动 READY） |
| M34-FINAL | **NOT STARTED** |

- 757 实施完成 **不自动**升级 C2/C5/C6：**C2=CONDITIONAL / C5=FOUNDATION·NOT FULL DISCOVERY / C6=FOUNDATION·NOT FULL CAPABILITY-LED SEARCH**（保留）。

## 17. Remaining Conditions

| 条件 | 分类 |
|------|------|
| Real-data Population（product/supplier_product/offer=0） | **UNVERIFIED** |
| 768≈140px 历史溢出 | **Carry Forward → Future Candidate** |
| `/supplier-models` 遗留静态路由 | **Future Candidate** |
| 历史搜索缺口（Supplier Directory 全量列表） | **NOT CLAIMED / Future Candidate** |
| Supplier user-facing surface | **Entry+Detail 已建立**；全量公开供应商目录 = **NOT CLAIMED** |

## 18. Final Decision

**CONDITIONAL PASS**

- **M34.5 implementation accepted（Canonical Discovery IA 已建立）**
- **≠ M34 platformization complete**（M34.5 仅完成 Entity→Discovery 的 IA 组织；Evaluation+Connection 归 M34.6、Governance+SEO/LLM+Mobile 归 M34.7、平台化整体收口归 M34-FINAL，均未授权）。
- 判定依据：Static PASS + Route Reachability/Navigation Wiring VERIFIED + Architecture Authority ONE 权威集 + Scope Compliance 全 NONE + 无架构违规；条件 = Real-data Runtime 因合法空数据（product=0/supplier_product=0/offer=0）无法验证 → **CONDITIONAL PASS** 成立，不伪造为 COMPLETE/100%。
- 符合 Foundation ≠ Full Capability、Conditional ≠ Complete、HTTP 200 ≠ Feature Complete、Empty Data ≠ Search Failure、Structural Verification ≠ Real-data Verification。

---

## Mandatory Final Route Matrix

| Discovery Entry | Canonical Target | Public | Workspace | Status |
|-----------------|------------------|:------:|:---------:|--------|
| Search | `/search`（既有 canonical 路由） | YES | NO | **VERIFIED（Route）** |
| Categories | `/categories`（能力分类） | YES | NO | **VERIFIED（Route）** |
| Capabilities | Product 语义角色 · `/products` + `/categories` | YES | NO | **STRUCTURAL VERIFIED（语义锚）** |
| Products | `/products` | YES | NO | **VERIFIED（Route）** |
| Suppliers | `/search?type=supplier-product`（公开入口）+ `/suppliers/[id]`（详情） | YES | NO | **VERIFIED（入口）/ STRUCTURAL（详情）** |

> 未为填表虚构不存在的 route；Capabilities 以实际 capability-oriented route/surface（/products 能力详情 + /categories 能力分类）表达，不虚构 `/capabilities`。

## Mandatory Discovery Path Matrix

| Path | Expected | Status |
|------|----------|--------|
| Search → Category → Product | `/search` → facet/能力分类 → `/products?categoryId=` | **VERIFIED** |
| Search → Product → Supplier | SearchCard → `/products/[slug]` → `/suppliers/${id}` | **VERIFIED** |
| Category → Product | `/categories` → `/products?categoryId=` | **VERIFIED** |
| Product → Supplier | ProductDetail → SupplierInfo/SupplierCapabilityList → `/suppliers/${id}` | **VERIFIED** |
| Supplier → Published SupplyProduct → Product | `/suppliers/[id]` 供应能力区回溯 `relatedProduct` | **STRUCTURAL / UNVERIFIED**（空数据） |
| Public → Workspace | `/login` → `/dashboard/*` | **BOUNDARY VERIFIED** |

## Mandatory Productization Gate

- VISNDT 不再仅以 Home → Product → About → Article → Contact 为主要 Public 信息架构。
- 已形成：**Discovery（Search / Categories / Capabilities / Products / Suppliers）**，Content = Supporting Layer（而非 Platform Core）。**MET**。

## Mandatory No-Visual-Loop Gate

- 本任务未重新进入「视觉发现问题 → Header/Homepage/Design-Token/Component 调整 → 再次视觉审核」循环。
- 未发现直接阻断 Discovery usability 的视觉问题；无需视觉修复。

## Mandatory Architecture Gate

- **ONE Capability Authority / ONE Product Authority / ONE Supplier Authority / ONE Specification Authority** 全部成立；无第二套 authority。若出现第二套 authority → BLOCKED STOP——**未发生**。

## Final Execution Output

```
Task ID:               757_M34.5_Canonical_Discovery_Information_Architecture
Task Status:           CONDITIONAL PASS
Repository Root:       F:\Desktop\VISNDT
Code Root:             F:\Desktop\VISNDT\VISNDT
Branch:                main
Commit:                ff03a9a
Working Tree:          OTHER（历史累积；757 仅改 PublicHeader.tsx）
M33:                   CLOSED / UNCHANGED
M34.0:                 CONDITIONAL
M34.1:                 COMPLETE / CONDITIONAL
M34.2:                 COMPLETE / CONDITIONAL
M34.3:                 COMPLETE / CONDITIONAL
M34.4:                 CONDITIONAL PASS
M34.4R:                CONDITIONAL PASS
M34.5:                 CONDITIONAL PASS
M34.6:                 NOT AUTHORIZED
M34.7:                 NOT AUTHORIZED
M34-FINAL:             NOT STARTED
C2:                    CONDITIONAL
C5:                    FOUNDATION / NOT FULL DISCOVERY
C6:                    FOUNDATION / NOT FULL CAPABILITY-LED SEARCH
```

```
Canonical Discovery IA:            ESTABLISHED
Search Entry:                      VERIFIED（/search）
Category Entry:                    VERIFIED（/categories · 能力分类）
Capability-oriented Entry:         STRUCTURAL VERIFIED（Product 语义角色 /products + /categories）
Product Entry:                     VERIFIED（/products）
Supplier Entry:                    VERIFIED（/search?type=supplier-product · /suppliers/[id]）
Cross-Entity Discovery:            STRUCTURAL VERIFIED
Public / Workspace Boundary:       BOUNDARY VERIFIED
Route Canonicalization:            CONDITIONAL（复用现有路由收敛，/supplier-models→Future Candidate）
Search Context Preservation:       PRESERVED（/search 类型参数不变，additive）
Mobile:                            STRUCTURAL（No-Visual-Loop Gate 约束）
Runtime Navigation:                VERIFIED（GET 200 + SSR 导航标签）
Real Product Population:           UNVERIFIED（product=0）
Real Supplier Population:          UNVERIFIED（supplier_product=0）
Real Capability Population:        UNVERIFIED（无真实 capability 数据）
Schema:                            NO CHANGE
Migration:                         NONE
Database Mutation:                 NONE
Test Data Creation:                NONE
AI / Vector / RAG / Semantic Search: NONE
Matching:                          NO CHANGE
RFQ:                               NO CHANGE
Transaction:                       NONE
Documentation:                     UPDATED
Review Report:                     docs/_review/757_M34.5_Canonical_Discovery_Information_Architecture_Report.md
```

---

**STOP**

本任务已完成 M34.5 Canonical Discovery Information Architecture。**未自动进入 M34.6（Evaluation + Connection）**，**未自动实施** Compare / Shortlist / Inquiry / RFQ / Buyer↔Supplier Workflow。后续 M34.6 / M34.7 / M34-FINAL 必须重新经独立授权。