# M13.4.1 补充 — Blueprint 对齐审计结论

**Date:** 2026-08-01  
**Phase:** M13.4.1 → M13.4.2 过渡  
**Type:** Read-Only 对比分析  
**Source:** `docs/_review/temp_claude_0801.md` + `docs/VISNDT-Blueprint/600_Frontend/`  
**Status:** ✅ 分析完成 — 发现 5 项需修正的差异

---

## 0. 核心发现

**Blueprint 600_Frontend 文档确实存在且已冻结 (FINAL)，共 10 份、~150K+ 行：**

| # | 文件 | 大小 | 主题 |
|---|------|------|------|
| 601 | `Frontend_Architecture.md` | 14.7K | 前端架构 |
| 602 | `Page_Information_Architecture.md` | 16.5K | 页面信息架构 |
| 603 | `Component_Design_System.md` | 18.3K | 组件设计系统 |
| 604 | `State_Management.md` | 19.2K | 状态管理 (Zustand) |
| 605 | `API_Integration.md` | 14.2K | API 集成 (TanStack Query) |
| 606 | `Product_Center_UI.md` | 15.7K | 产品中心 UI |
| 607 | `Requirement_Center_UI.md` | 15.5K | 需求中心 UI |
| 608 | `Public_Page_UI.md` | 18.3K | 公共页面 UI |
| 609 | `Frontend_Deployment_UI.md` | 13.9K | 部署方案 (Docker) |
| 610 | `Supplier_Workflow_UI.md` | 4.1K | 供应侧工作流 UI |

**242 报告在生成时未引用 Blueprint 文档。**

---

## 1. 与 M13.4 报告 (242-251) 的差异分析

### 1.1 技术栈差异 (3 项需修正)

| # | 决策项 | 244 报告 (Trae) | Blueprint 冻结版 | 修正 |
|---|--------|----------------|-----------------|------|
| 1 | 客户端状态管理 | 纯 Context | **Zustand** (appStore/userStore/productStore/searchStore/requirementStore) | ✅ 采用 Zustand |
| 2 | 服务端数据缓存 | 纯 Context | **TanStack Query** | ✅ 采用 TanStack Query |
| 3 | JWT 存储 | 未明确 | **localStorage + Zustand persist** (与 Admin 一致) | ✅ 沿用 Admin 模式 |

> **结论:** 244 报告的"纯 Context"方案过于简单，Blueprint 的 Zustand + TanStack Query 方案更成熟。M13.4.2 应以此为准。

### 1.2 路由命名差异 (6 项需修正)

| # | 244 报告 (Trae) | Blueprint 冻结版 | 修正 |
|---|----------------|-----------------|------|
| 1 | `/categories/[slug]` | `/products/categories/[slug]` | ✅ 采用 Blueprint |
| 2 | `/workspace/*` | `/dashboard/*` | ✅ 采用 Blueprint |
| 3 | `/profile` | `/account/profile` | ✅ 采用 Blueprint |
| 4 | `/demands` (Buyer) | `/account/demands` | ✅ 采用 Blueprint |
| 5 | `/notifications` | `/account/messages` | ✅ 采用 Blueprint |
| 6 | `/workspace/offers` | `/dashboard/offers` | ✅ 采用 Blueprint |

> **结论:** 244 报告的路由命名与 Blueprint 不一致。Blueprint 的命名更清晰地划分了公共区 (`/products`) 和用户区 (`/account`, `/dashboard`)。M13.4.2 路由应以 Blueprint 为准。

### 1.3 页面遗漏 (5 项)

| # | 页面 | 244 报告 | Blueprint | 状态 |
|---|------|---------|-----------|------|
| 1 | `/solutions/[slug]` | ❌ 缺失 | ✅ 规划 | 新增 (MDX) |
| 2 | `/applications/[slug]` | ❌ 缺失 | ✅ 规划 | 新增 (MDX) |
| 3 | `/contact` | ❌ 缺失 | ✅ 规划 | 新增 |
| 4 | `/account/favorites` | ❌ 缺失 | ✅ 规划 | 延后 (无 DB 模型) |
| 5 | `/dashboard/rfqs/[id]/respond` | ❌ 缺失 | ✅ 规划 | 新增 |

> **结论:** 244 报告遗漏了 3 个营销内容页面和 1 个供应侧操作页面。Contact 和 respond 页面应纳入 M13.4.2+，Solutions/Applications/Knowledge 作为可选 MDX 页面延后。

### 1.4 目录结构差异

| 层级 | 244 报告 | Blueprint 冻结版 |
|------|---------|-----------------|
| App Router 分组 | 无 | `(marketing)`, `(demand)`, `(account)`, `(dashboard)`, `(auth)` |
| 状态管理 | 无明确目录 | `stores/` (Zustand) |
| 数据缓存 | 无 | `hooks/` (TanStack Query) |
| 服务层 | `services/` | `services/` (Admin 同款拦截器) |

> **结论:** Blueprint 的分组布局 (Route Groups) 更清晰，M13.4.2 应采用。

### 1.5 部署方案差异

| 决策项 | 242 报告 | Blueprint 冻结版 | 现状 |
|--------|---------|-----------------|------|
| 部署平台 | Vercel | **Docker (Provider 无关)** | ✅ 245 已修正 |
| Auth 方案 | next-auth | **Admin 同款 JWT 拦截器** | ✅ 245 已修正 |

> **结论:** 245 报告已修正部署和 Auth 方案，与 Blueprint 一致。无需额外修改。

---

## 2. M13.4.2 路由修正方案

### 2.1 修正后的完整路由表 (以 Blueprint 为准)

```
公共营销区 (marketing) — SSG/ISR:
  /                                          # 首页
  /products                                  # 产品列表
  /products/[slug]                           # 产品详情
  /products/categories/[slug]                # 分类页
  /about                                     # 关于我们
  /contact                                   # 联系我们 (新增)
  /solutions/[slug]                          # 解决方案 (MDX, 可选)
  /applications/[slug]                       # 应用场景 (MDX, 可选)
  /knowledge/[slug]                          # 知识中心 (MDX, 可选)

需求撮合区 (demand) — ISR + CSR:
  /demands                                   # 需求列表 (公开)
  /demands/[id]                              # 需求详情
  /demands/new                               # 发布需求 (多步表单)
  /demands/[id]/matches                      # 匹配结果

用户中心 (account) — CSR:
  /account/profile                           # 个人资料
  /account/demands                           # 我的需求
  /account/messages                          # 消息中心
  /account/favorites                         # 收藏夹 (延后)

供应侧工作台 (dashboard) — CSR:
  /dashboard                                 # 工作台首页
  /dashboard/profile                         # 机构资料
  /dashboard/offers                          # Offer 管理
  /dashboard/rfqs                            # RFQ Inbox
  /dashboard/rfqs/[id]/respond               # 响应处理 (新增)

认证 (auth) — CSR:
  /login
  /register
```

### 2.2 关键变更 vs 244 报告

| 变更 | 说明 |
|------|------|
| `/workspace/*` → `/dashboard/*` | 供应侧工作台命名对齐 Blueprint |
| `/categories/*` → `/products/categories/*` | 分类页嵌套在产品路径下 |
| `/profile` → `/account/profile` | 用户中心统一 `/account` 前缀 |
| `/demands` (买方) → `/account/demands` | 买方需求放在账户下 |
| `/notifications` → `/account/messages` | 消息中心 |
| 新增 `/contact` | 联系我们 |
| 新增 `/dashboard/rfqs/[id]/respond` | RFQ 响应处理 |
| 新增 `/solutions`, `/applications` | 营销内容 (MDX, 可选) |

---

## 3. M13.4.2 技术栈修正

| 层级 | 原 244 方案 | 修正后 (Blueprint) |
|------|-----------|-------------------|
| 状态管理 | Context | **Zustand** (5 个 store) |
| 数据缓存 | Context | **TanStack Query** (hooks) |
| JWT 存储 | 未明确 | **localStorage + Zustand persist** |
| API 客户端 | 自行设计 | **Admin 同款 fetch 拦截器** |
| 部署 | Vercel (已修正) | **Docker** (已修正) |

---

## 4. M13.4.1 完成状态确认

| 端点 | 244 规划 | 实际完成 | Blueprint 需求 |
|------|---------|---------|---------------|
| `GET /organizations/:id` (Public) | ✅ | ✅ Batch 1 | ✅ 满足 |
| `GET /demands/mine` | ✅ | ✅ Batch 1 | ✅ 满足 |
| `GET /rfqs/mine` | ✅ | ✅ Batch 2 | ✅ 满足 |
| `GET /rfq-responses/mine` | ✅ | ✅ Batch 2 | ✅ 满足 |
| `POST /inquiries` | ✅ | ✅ Batch 3 | ✅ 满足 Contact 表单需求 |

> **结论:** M13.4.1 已完成的 5 个 API 端点完全满足 Blueprint 的 Phase 0 需求。无需额外后端开发。

---

## 5. M13.4.2 建议调整

### 5.1 原 M13.4.2 规划 (244)

```
M13.4.2: Web Foundation
  - 页面布局 (Header, Footer)
  - 路由系统 (/app 目录结构)
  - 基础组件 (Loading, Error, Empty)
  - API Client
  - 认证 HOC
```

### 5.2 建议调整 (对齐 Blueprint)

```
M13.4.2: Web Foundation (对齐 Blueprint Phase 1)

  **必做:**
  1. Next.js 项目骨架 (已有 apps/web/)
  2. Tailwind CSS + shadcn/ui 接入
  3. 目录结构 (Route Groups + stores/ + hooks/ + services/)
  4. Zustand 5 个 store 初始化
  5. TanStack Query 接入
  6. API Client (Admin 同款 JWT 拦截器)
  7. 认证 HOC (AuthGuard)
  8. Docker 化 (Dockerfile)

  **调整:**
  9. 路由以 Blueprint 为准 (修正 6 项命名)
  10. 目录结构以 Blueprint 为准 (Route Groups)
```

### 5.3 后续阶段建议 (对齐 Blueprint Phase 2-7)

| 阶段 | 内容 | 对应 Blueprint |
|------|------|---------------|
| M13.4.2 | Web Foundation | Phase 1 |
| M13.4.3 | 产品中心 (首页 + 产品列表/详情/分类) | Phase 2 (606) |
| M13.4.4 | 需求中心 (需求列表/详情/发布/我的需求) | Phase 3 (607) |
| M13.4.5 | 供应侧工作台 (Dashboard + Offers + RFQ + Respond) | Phase 4 (610) |
| M13.4.6 | 用户中心 + 登录注册 | Phase 5 |
| M13.4.7 | 营销内容页 (MDX, 可选) | Phase 6 (608) |
| M13.4.8 | SEO 收尾 + Closeout | Phase 7 |

---

## 6. 待决策项

| # | 决策 | 建议 | 影响 |
|---|------|------|------|
| 1 | Solutions/Applications/Knowledge 内容策略 | MVP 用 MDX 静态内容，不建 CMS 模型 | M13.4.7 可选 |
| 2 | Favorites 是否纳入首期 | 延后到核心闭环之后 | 不影响 M13.4 |
| 3 | Contact 表单 | 直接用 `POST /inquiries` (已在 M13.4.1 实现) | ✅ 已解决 |
| 4 | `/dashboard` vs `/workspace` 命名 | 以 Blueprint `/dashboard` 为准 | 需修正 244 路由 |
| 5 | 路由命名体系 | 以 Blueprint 冻结版为准 | 需修正 6 项 |

---

## 7. 结论

1. **Blueprint 文档是本项目的权威前端设计参考**，M13.4.2 必须对齐它，而不是继续沿用 244 报告的独立设计。

2. **M13.4.1 已完成的后端 API 完全满足 Blueprint Phase 0 需求**，无需额外后端开发。

3. **M13.4.2 需要修正 6 项路由命名 + 3 项技术栈选择**，以 Blueprint 冻结版为准。

4. **不需要重新设计** — Blueprint 已经把信息架构、组件体系、状态管理方案都定好了。M13.4.2 的工作是"实现"，不是"设计"。

5. **建议 M13.4.2 开始前，先阅读 Blueprint 604 (State Management) 和 605 (API Integration) 两份文档**，确保 Zustand + TanStack Query 方案正确落地。

---

*Report generated by M13.4.1 Blueprint Alignment Audit*  
*No code was modified.*  
*Next: M13.4.2 Web Foundation (对齐 Blueprint Phase 1)*