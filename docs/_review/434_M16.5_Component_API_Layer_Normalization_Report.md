# 434_M16.5_Component_API_Layer_Normalization_Report

## 1. Task Overview

| 项 | 值 |
| --- | --- |
| Task ID | 434 |
| Task Type | Refactor / Architecture Cleanup |
| Stage | M16 业务深化前稳定化（收尾阶段） |
| Objective | 扫描 `apps/web/src/components`，将组件层运行时直接调用 `@/lib/api/*` 的 API 调用迁移至 Service 层，统一前端数据访问架构，保持页面行为一致 |
| Scope | 仅组件层 API 调用规范化；不含业务功能开发、CMS、内容模型、SEO 管理、Notification/Audit 重构、新增业务模型 |

## 2. Repository Verification

| 项 | 值 |
| --- | --- |
| Repository Root | `F:\Desktop\VISNDT` |
| Code Root | `F:\Desktop\VISNDT\VISNDT` |
| Branch | `main` |
| 前置报告 | 429 / 430 / 431 / 432 / 433 报告均存在（`docs/_review/`） |
| M16.4 状态 | 已完成（延迟内容系统至 M17） |
| 历史工作区修改 | `apps/api`（7 文件）、`apps/web/src/app`（页面）、`services/rfq.service.ts`、`types/product.ts` 为 M15.2/M16.1/M16.2/M16.3 遗留未提交修改，非本任务引入 |

## 3. Modified Files

### 代码文件（本任务）

| 文件路径 | 修改目的 |
| --- | --- |
| `apps/web/src/components/products/InquiryForm.tsx` | 将 `createInquiry` 调用从 `@/lib/api/inquiries` 迁移至 `@/services/inquiry.service` |
| `apps/web/src/components/home/CategorySection.tsx` | 将 `getCategories` 调用从 `@/lib/api/categories` 迁移至 `@/services/category.service` |
| `apps/web/src/components/home/FeaturedProductsSection.tsx` | 将 `getProducts` 调用从 `@/lib/api/products` 迁移至 `@/services/product.service` |

> 仅修改 import 路径；复用已存在的 Service 方法（`inquiry.service`/`category.service`/`product.service`），未新增 Service 方法。

### 文档文件（本任务）

| 文件路径 | 同步内容 |
| --- | --- |
| `docs/project-management/PROJECT_STATUS.md` | 新增 M16.5 阶段记录，更新风险与下一步 |
| `docs/project-management/PROJECT_ROADMAP.md` | M16 行与阶段说明补记 M16.5 |
| `docs/project-management/MODULE_COMPLETION_MATRIX.md` | Public Website/Buyer Workspace 行与 Overall Judgment 补记 M16.5 |
| `docs/project-management/BUSINESS_CAPABILITY_MAP.md` | 新增 M16.5 Note 段 |
| `docs/project-management/CONTENT_MANAGEMENT_PLAN.md` | Rollout Order 补记 M16.5 |

## 4. Architecture Impact

### Frontend

- 组件层 3 处运行时直接调用 `@/lib/api/*` 已全部迁移至 Service 层。
- 前端数据访问架构统一为 `Component → Service → API Wrapper → Backend API`。
- 组件层剩余 `@/lib/api/*` 引用为 5 处 `import type`（`RfqItem`/`RfqDetailItem`/`RfqResponseItem`/`DemandItem`/`DemandDetailItem`），均属编译期类型引用、非运行时数据访问，无 `fetch`/`client` 直连，列为低风险可后续清理项。

### Backend

- 未修改 `apps/api`，无任何后端改动。

### Domain

- 业务逻辑、页面行为、用户流程完全保持，无变化。

## 5. Database Impact

| 维度 | 结论 |
| --- | --- |
| Schema | No Change |
| Migration | No Change |

## 6. API Impact

| 维度 | 结论 |
| --- | --- |
| Endpoint Changes | 无 |
| Contract Changes | 无 |
| Compatibility | 完全兼容（仅前端 import 调整，未触碰 API 封装逻辑） |

结论：**No API Contract Change**。

## 7. Security Impact

| 维度 | 结论 |
| --- | --- |
| Auth (JWT) | 无变化 |
| CSRF | 无变化 |
| RBAC / Permission | 无变化 |

## 8. Build Verification

| 项 | 结论 |
| --- | --- |
| Build Status | `apps/web` `pnpm build` 通过，exit code 0 |
| Test Status | 未执行自动化测试（本任务为纯 import 路径重构，无行为变更，构建验证充分） |
| 未执行原因 | 无运行时行为修改，构建为准入标准 |

## 9. Freeze Compatibility Check

| 项 | 结论 |
| --- | --- |
| 是否影响冻结模块（WorkflowEvent/Notification/AuditLog/Auth/Schema） | 否 |
| 是否扩大需求范围 | 否 |
| 是否提前实施 M17 内容能力 | 否 |

## 10. Project Status Update

五个状态文件均已同步记录 M16.5：

- `PROJECT_STATUS.md` ✅
- `PROJECT_ROADMAP.md` ✅
- `MODULE_COMPLETION_MATRIX.md` ✅
- `BUSINESS_CAPABILITY_MAP.md` ✅
- `CONTENT_MANAGEMENT_PLAN.md` ✅

## 11. Next Recommendation

- M16 主体任务（M16.1-M16.5）已完成：询价链路、前端 API 层规范化、SEO 基础、内容管理评估、组件层 API 规范化均已收口。
- M16 是否完成：主要稳定化目标已达成，建议执行 **M16 收尾审计**（复核稳定化目标达成情况）后宣告 M16 关闭。
- 是否允许进入 M17：内容域推迟至 M17，需先完成 M16 收尾审计；M16.5 已为 M17 内容组件开发提供统一数据访问基线。
- 下一任务建议：M16 收尾审计（建议编号 435）。

## Final Execution Output

- **Modified Files**：3 个组件文件（`InquiryForm.tsx`、`CategorySection.tsx`、`FeaturedProductsSection.tsx`）
- **Documentation Updates**：5 个 `docs/project-management` 状态文件
- **Review Report**：`docs/_review/434_M16.5_Component_API_Layer_Normalization_Report.md`
- **Build Result**：`apps/web` build 通过（exit code 0）；`apps/api` No Change；`apps/admin` No Change
- **Risk Summary**：组件层剩余 5 处 `import type` 类型引用（低风险，编译期类型，非运行时数据访问）；`SITE_URL` 依赖生产 `NEXT_PUBLIC_SITE_URL` 配置；公共内容体系仍为静态展示壳
- **Next Step**：M16 收尾审计（435）→ M17 内容域立项