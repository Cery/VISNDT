# 464 M19.1.3 Product Supplier Capability & Inquiry Experience Enhancement Report

## 1. Task Overview

- **Task**: 464_M19.1.3_Product_Supplier_Capability_And_Inquiry_Experience_Enhancement
- **Task Type**: Development / Frontend Enhancement
- **Stage**: M19.1 Product Center V2 Development Execution
- **Objective**: 基于 459-463 冻结结论，完善 Product Center V2 供应商能力与询价体验——ManufacturerInfo 接入 offers.organization 数据、SupplierCapabilityList 展示增强、Inquiry 交互流程完善，确保 Product → Offer → Inquiry 业务链路完整。

## 2. Repository Verification

| Item | Value | Verified |
| --- | --- | --- |
| Repository Root | `F:\Desktop\VISNDT` | ✅ |
| Code Root | `F:\Desktop\VISNDT\VISNDT` | ✅ |
| Branch | `main` | ✅ |
| Git Status | 本次修改均为 `apps/web/src` 与项目管理文档；M18.5.3 SEO 未提交文件与 `docs/_review/453-463` 历史报告未触碰 | ✅ |

## 3. Modified Files

### 修改（apps/web/src）

| 文件 | 修改内容 |
| --- | --- |
| `VISNDT/apps/web/src/components/products/ManufacturerInfo.tsx` | 新增 `offers` prop，从 `offers` 中推导制造商信息（取第一个 ACTIVE/SUBMITTED offer 的 organization），统一制造商信息与供应商能力数据源；空状态文案引导用户使用供应商列表 |
| `VISNDT/apps/web/src/components/products/SupplierCapabilityList.tsx` | 增强空状态（图标+引导文案"如果您是该产品的供应商，请联系我们加入平台"）、供应商卡片增加 organization.type 标签、选中状态增加「已选择供应商」指示器、提示文案根据是否选中动态切换 |
| `VISNDT/apps/web/src/components/inquiry/SupplierInquirySection.tsx` | 选中供应商后展示选中横幅（"询价对象：XXX"）+「切换供应商」按钮；InquiryForm 传入 organizationName |
| `VISNDT/apps/web/src/components/inquiry/InquiryForm.tsx` | 新增 `organizationName` prop，选中供应商时显示"向 XXX 询价 YYY" |
| `VISNDT/apps/web/src/app/products/[slug]/page.tsx` | ManufacturerInfo 改为传入 `offers={product.offers}` 替代 `organization={null}` |

### 文档

| 文件 | 修改内容 |
| --- | --- |
| `docs/project-management/PROJECT_STATUS.md` | 新增 M19.1.3 章节，更新 Current M stage 与 Current Phase Judgment |
| `docs/project-management/PROJECT_ROADMAP.md` | 更新 M19 行，记录 M19.1.3 完成 |
| `docs/project-management/MODULE_COMPLETION_MATRIX.md` | Supplier Display / Inquiry Flow 状态更新为 M19.1.2-3 Implementation Completed |
| `docs/project-management/BUSINESS_CAPABILITY_MAP.md` | 更新 Supplier Capability Display / Inquiry Flow 前端状态为 M19.1.2-3 |

## 4. Architecture Impact

- **Product Global Catalog 保持**：产品仍为标准平台目录，未引入 `Product.organizationId` / `ProductFamily` / `ProductModel` / `SupplierOffering`。
- **Offer Supplier Display 保持**：供应商能力通过 `Offer.organization` 展示，ManufacturerInfo 从 `offers` 推导制造商，统一数据源。
- **No Supplier Product Ownership**：未引入 Supplier 产品管理、Storefront、Price、Inventory、Transaction。

## 5. API Impact

- **API Change = None**。
  - 复用 `GET /products/:id`（已有 `offers` 含 `organization` 数据）。
  - 复用 `POST /inquiries`（已有 `offerId` + `organizationId` 参数）。
  - 未新增后端接口，未修改 API Contract。

## 6. Database Impact

- **Schema Change = None**。
- **Migration = None**。

## 7. Functional Verification

### Build / Type Check

- `apps/web` `next build`：**exit code 0**，全阶段成功（编译 + 类型校验 + `Generating static pages (28/28)`）。
- `/products/[slug]` 路由正常生成（4.34 kB，dynamic）。
- 仅既有非阻断 lint 警告（custom font / img / ManufacturerInfo productName unused）。

### Supplier Capability - 功能场景

| 场景 | 预期 |
| --- | --- |
| 多供应商展示 | 每个 Offer 显示 organization.name + type 标签 + status 徽章 |
| Offer 选择 | 点击选中，卡片高亮（primary 边框+背景），显示 checkmark + 展开 description |
| 切换供应商 | 点击已选中的取消选中，点击其他切换到新供应商 |
| 空状态 | 无 Offer 时显示图标 + "暂无供应商" + 引导文案 |
| ManufacturerInfo 联动 | 有 Offer 时显示第一个供应商的 organization 信息；无 Offer 时显示引导文案 |

### Inquiry - 功能场景

| 场景 | 预期 |
| --- | --- |
| 未选 Offer 不可提交 | 未选择供应商时，InquiryForm 不显示 |
| 选中后显示供应商 | 选中横幅"询价对象：XXX"+"切换供应商"按钮 |
| InquiryForm 提交带 supplier | 表单描述"向 XXX 询价 YYY"，提交时携带正确的 offerId + organizationId |
| 提交成功 | 显示成功状态，可"提交另一个询价" |
| 切换供应商后重新询价 | 点击"切换供应商"回到列表，重新选择 |

### Regression - 功能场景

| 场景 | 预期 |
| --- | --- |
| 产品详情整体布局 | Overview / Media Center / Technical Specification / Documents 布局不变 |
| ProductGallery | 图片正常展示 |
| Breadcrumb | 面包屑导航正常 |
| ProductParameters | 参数按 ParameterGroup 分组展示正常 |
| 产品搜索列表 | `/products` 路由正常（5.46 kB） |

> 说明：本环境未能启动实时后端进行浏览器端到端实测；上述验证基于构建通过 + 后端既有 API 逻辑与前端实现的一致性确认。建议在联调环境按上表做真实页面测试。

## 8. Risk Assessment

| 风险 | 等级 | 说明 |
| --- | --- | --- |
| Offer 数据为空 | Low | ManufacturerInfo 空状态与 SupplierCapabilityList 空状态均正确处理，引导用户联系平台 |
| Inquiry 状态管理 | Low | `SupplierInquirySection` 使用 `useState<Offer | null>` 管理选中状态，切换/取消逻辑清晰，无 fallback 导致非法提交 |
| Supplier 信息缺失 | Low | Organization 字段若缺失，渲染时仅显示已有字段，不会崩溃 |
| 无 High / Medium 风险 | — | 本任务仅前端展示增强与交互优化，未触碰后端/数据库/权限 |

## 9. Final Status

**Completed**（M19.1.3 Product Supplier Capability & Inquiry Experience Enhancement 完成）。

---

## Change Summary

| 类别 | 状态 |
| --- | --- |
| Code Change | Yes（仅 `apps/web/src`，5 修改） |
| Schema Change | None |
| Migration | None |
| API Change | None |

## Next Recommendation

进入 **M19.1.4**（Product Center V2 后续开发）。460 实施规划中剩余 P0-P2 任务依优先级推进，保持「Backend Capability First，前端消费现有能力，零 Schema 变更」原则。