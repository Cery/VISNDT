# 762 M34.6 Controlled Real-data Onboarding Preparation Assessment Report

> Task: **762_M34.6_Controlled_Real_Data_Onboarding_Preparation_Assessment**
> Stage: **M34.6 Evaluation + Connection — Controlled Real-data Preparation**
> Mode: **Architecture Audit / Data-flow Verification / Readiness Assessment（READ-ONLY）**
> Prepared: 2026-08-31

**Final Decision: DATA PREPARATION = STRUCTURALLY READY / INSTANCE NOT READY（需受控真实数据落地）；M34.6 AUTHORIZATION = STILL PENDING（重查）**

> 本任务**不创建任何数据、不修改任何代码/Schema/Migration/Search/Discovery/Workflow**。仅核验既有合法业务管理链路是否能在不扩域、不引入 M34.6 业务模型、不制造 Synthetic Data 的前提下，合法产生 M34.6 最小真实数据集，并明确后续真实数据准备所需的实际业务步骤、数据条件、发布条件、关系条件与参数条件。

---

## 0. 执行摘要

- **结论**：现有 ONE Authority（Product / SupplierProduct / Organization(SUPPLIER) / ProductCategory / ParameterDefinition / KnowledgeEntry）+ 既有 Admin 管理链路，**可合法产生 M34.6 Core Dataset 的全部要素**，**无需新增 Schema / Domain Authority / Search / Discovery / Workflow 修改**。
- **当前实例状态**：Core/Extended Dataset **INSTANCE NOT READY**（Product=0、SupplierProduct=0、ProductParameterValue=0、Discoverable Supplier=0）；Workflow Dataset **CONDITIONAL**；Offer **OPTIONAL**。
- **差距**：数据未落地。需一个**受控真实数据准备（Controlled Real-data Onboarding）独立任务**将实例落地；本任务不执行。
- **M34.6 = NOT AUTHORIZED / PENDING RECHECK**。

---

## 1. Repository Verification

- 仓库根 `F:\Desktop\VISNDT`（`git rev-parse --show-toplevel` = `F:/Desktop/VISNDT`）✅
- 代码根 `F:\Desktop\VISNDT\VISNDT`，实际存在 `apps/ database/ docs/ packages/`，以及 `apps/api` `apps/web` `apps/admin` ✅

## 2. Working Tree Classification

- Branch = `main`（`git branch --show-current`）✅
- HEAD = `ff03a9a`，与 761 基线完全一致，**BASELINE DRIFT = NONE** ✅
- Working Tree = `git status --short` **164 行**，全部为**既往历史任务产生的未提交改动（M33/M34.1-759/761 等）**，762 **未引入任何生产代码改动**。
- 因 762 为只读审计，无可归属的 762 写入变更。既有改动不归属于 762。

## 3. Baseline Verification（759/760/761）

- 761 声明基线 **全部重新实证核验，无漂移**：

| Entity | 761 声明 | 762 实测 | 一致 |
|---|--:|--:|:-:|
| Product | 0 | 0 | ✅ |
| SupplierProduct | 0 | 0 | ✅ |
| SUPPLIER Organization | 8 | 8 | ✅ |
| Discoverable Supplier | 0 | 0 | ✅ |
| ProductParameterDefinition | 0 | 0 | ✅ |
| ProductParameterValue | 0 | 0 | ✅ |
| SupplierProductParameterValue | 0 | 0 | ✅ |
| ParameterDefinition | 54 | 54 | ✅ |
| ParameterGroup | 11 | 11 | ✅ |
| ParameterOption | 45 | 45 | ✅ |
| Inquiry | 0 | 0 | ✅ |
| Demand | 3 | 3（DRAFT1 / CLOSED2） | ✅ |
| RFQ | 2 | 2（DRAFT1 / OPEN1） | ✅ |
| RFQResponse | 1 | 1（SUBMITTED） | ✅ |
| Offer | 0 | 0 | ✅ |

## 4. ONE Authority Verification

- `Product` = Capability Authority（`product.status` String @default("DRAFT")；SQL 无 enum 约束）
- `SupplierProduct` = Supplier-owned Capability/Model Authority（`SupplierProductStatus` enum：DRAFT/SUBMITTED/REVIEWING/APPROVED/PUBLISHED/REJECTED）
- `Organization(type=SUPPLIER)` = Provider Authority（`organization.organization_type` String = "SUPPLIER"）
- `ProductCategory` = Taxonomy Authority
- `ParameterDefinition` = Specification Dictionary Authority（`dataType` enum、`required @default(false)`、`code` unique）
- `KnowledgeEntry` = Knowledge Authority
- 既有 `Demand / DemandMatch / Inquiry / RFQ / RFQResponse / Offer` = Workflow Authority
- **未新增任何一级 Domain Model。** 禁止项（Capability/Supplier/Evaluation/Comparison/Shortlist/Specification/Application/DetectionObject/Standard/Certification/Evidence）均未创建 ✅

### 3.2 必需关系（Schema 实证）

```text
Product(categoryId → ProductCategory, FK non-null)
  ↑ platform_product_id (FK, onDelete: Restrict)
SupplierProduct(organizationId → Organization FK non-null)
  ↓ brand/series/modelNumber + status + publishedAt
Organization(organization_type='SUPPLIER')
Product → ProductParameterValue(parameterDefinitionId) → ParameterDefinition
Product → ProductParameterDefinition(displayOrder) → ParameterDefinition
SupplierProduct → SupplierProductParameterValue(parameterDefinitionId) → ParameterDefinition
```

## 5. Product Lifecycle Assessment

- **创建**：`POST /products`（ADMIN only）→ [products.controller.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/products/products.controller.ts#L66-L73)；`ProductsService.create` 默认 status=DRAFT，`categoryId` 为必填 FK（28 个 ProductCategory 现存）。
- **编辑/发布**：`PATCH /products/:id`（ADMIN）、`POST /products/batch-status`（ADMIN）可设任意 status 字符串。
- **可见/可发现条件**：统一检索 [search.service.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/search/search.service.ts#L243-L252) `searchProducts` 以 `status:'ACTIVE'` 为唯一发布边界；`findRelatedProducts` 同样以 `status:'ACTIVE'` 过滤。**即「Published/Discoverable Product」= status='ACTIVE'**。
- **管理 UI**：admin `ProductList/ProductEdit/ProductDetail/ProductForm` 存在 ✅
- **判断**：现有链路**可合法产生 Published Product**（创建 → 设 status='ACTIVE'）。**无需 Schema 修改。**

## 6. SupplierProduct Lifecycle Assessment

- **状态机（实证）**：`DRAFT → SUBMITTED → REVIEWING → APPROVED → PUBLISHED / ↘ REJECTED`（[supplier-products.service.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/supplier-products/supplier-products.service.ts#L194-L247)），逐级 guard 源状态（`transition`）。
- **控制器**：`admin/supplier-products`（ADMIN），含 `create/submit/review/approve/reject/publish/findAll/findOne`（[supplier-products.controller.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/supplier-products/supplier-products.controller.ts#L30-L155)）。
- **organizationId**：由认证 admin 的 `user.organizationId` 服务端派生，**非请求体**提供。**无运行时校验 org.type='SUPPLIER'**（control note）。
- **platformProductId**：`createDraft` 经 `ensurePlatformProductExists` 强制校验平台 Product 必须存在（能力绑定）；FK `onDelete: Restrict`。
- **发布条件**：`publish` = APPROVED→PUBLISHED（记 `publishedAt`）。
- **管理 UI**：admin `SupplierProductList/SupplierProductDetail` + `api/supplier-product.service.ts`（调 admin/supplier-products）✅
- **判断**：现有链路**可合法产生 Published SupplierProduct**。**无需 Schema 修改。**

## 7. Supplier Provider Assessment

- `Organization(type='SUPPLIER')` = **8**；其中拥有 **≥1 Published SupplierProduct** = **0**；拥有 **任意 SupplierProduct** = **0**。
- 可发现 Supplier 判定（[search.service.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/search/search.service.ts#L534-L551)）：`SupplierProduct.status=PUBLISHED AND organization.type='SUPPLIER'`，按 organization 聚合。
- **Provider Exists = 8；Provider Discoverable = 0。** 8 个 SUPPLIER 组织 ≠ 8 个 Discoverable Supplier。
- 既有路径存在：SUPPLIER 组织下的 admin 走 SupplierProduct 治理 → PUBLISH，即可进入 earchSuppliers。

## 8. Parameter Lifecycle Assessment

- **ParameterDefinition/Group**：ADMIN 可创建/编辑/删除（[parameter-definitions.controller.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/parameter-definitions/parameter-definitions.controller.ts#L32-L68)）；admin UI 有 `ParameterDefinitionList/Create/Edit/Detail`、`ParameterGroupList/Detail` ✅
- **Product 参数**：`POST /products/:id/parameters`（ADMIN）按 `productId+parameterDefinitionId` upsert `ProductParameterValue`（[product-parameters.service.ts](file:///f:/Desktop/VISNDT/VISNDT/apps/api/src/product-parameters/product-parameters.service.ts#L19-L43)）；`GET` 公开读取。
- **Runtime-readable**：`ProductParameterValue → ParameterDefinition` 即构成运行时可读 Parameter Profile（search 参数 facet、enrichment 均读 `parameterValues`）。
- **Required 规则**：`ParameterDefinition.required` 存在但**无任何运行时强制**（创建/设参均不校验）。→ **Q7 = UNDEFINED**。
- **gap 记录**：`ProductParameterDefinition`（关联/displayOrder 策划表）在 `product-parameters.controller` 无独立写接口；运行时评估依据 `ProductParameterValue` 即可，关联表为策划层（非阻断）。

## 9. Publication / Discoverability Assessment

| 目的 | 发布/可发现条件 | 证据 |
|---|---|---|
| Product · 检索可发现 | `status='ACTIVE'` | search.service.ts#L244 |
| Product · 关联产品 | `status='ACTIVE'` | products.service.ts#L348 |
| SupplierProduct | `status=PUBLISHED` | search.service.ts#L303 |
| Supplier（组织聚合） | `status=PUBLISHED AND org.type='SUPPLIER'` | search.service.ts#L534-536 |
| Product 删除保护 | offers/inquiries/demandMatches | products.service.ts#L442 |

- 无 `deletedAt`/`visible` 字段于 Product/SupplierProduct；可见性由 status 决定（Product 用自由字符串 'ACTIVE'，SupplierProduct 用 enum PUBLISHED）。

## 10. Database Read-only Verification（READ-ONLY ✅）

容器 `visndt-postgres`（healthy），全部只读 SELECT/COUNT/GROUP BY/JOIN。计数见 §3 表。关系完整性：

| 关系 | 结果 |
|---|---|
| SupplierProduct.platformProductId → Product | 0 条（无实例） |
| SupplierProduct.organizationId → Organization(SUPPLIER) | 0 条（无实例） |
| PUBLISHED 完整链（SP→Product→Org(SUPPLIER)） | 0 条 |
| ProductParameterValue / ProductParameterDefinition / SupplierProductParameterValue | 各 0 |

## 11. Core Dataset Definition

**冻结定义**（M34.6 最小真实评估链）：

```text
≥1 Published Product（status='ACTIVE'，含合法 categoryId）
+ ≥1 Published SupplierProduct（status=PUBLISHED）
+ ≥1 SUPPLIER Organization
+ 合法 Product ↔ SupplierProduct（platform_product_id）
+ 合法 SupplierProduct ↔ SUPPLIER Organization（organization_id）
+ ≥1 合法 Product Parameter Profile（ProductParameterValue → ParameterDefinition）
```

- **结构性满足**：全部元素可由既有 Authority + Admin 链路落地，无需扩域/Schema。
- **实例满足**：当前不满足（全 0）。**不能以「架构可用」冒充「Runtime Ready」。**

## 12. Extended Dataset Definition

```text
≥2 Products；≥2 SupplierProducts；≥2 Discoverable Suppliers；≥2 Parameter Profiles
+ same Product + multiple SupplierProducts + different Suppliers
+ different Products + different Parameter Profiles
```

- 结构性可满足（重复受控落地即可）；当前 INSTANCE NOT READY。**非架构失败。**

## 13. Workflow Dataset Assessment

| Entity | 实测 | 判定 |
|---|--:|:--:|
| Demand | 3（DRAFT1/CLOSED2） | 存在 |
| Inquiry | 0 | SPARSE |
| RFQ | 2（DRAFT1/OPEN1） | 存在 |
| RFQResponse | 1（SUBMITTED） | 存在 |

- **Workflow Dataset = CONDITIONAL / SPARSE**，与 Core Discovery Data **独立判断**，不混为一 Gate。

## 14. Commercial / Offer Classification

- Offer = 0。**无任何 M34.6 核心验收场景强依赖 Offer 的代码/架构证据** → **Offer = OPTIONAL / NOT CORE BLOCKER**（保持 761 归类）。

## 15. Existing Real-data Source Assessment

**SOURCE EXISTS（既有合法 Onboarding 链路，未在本任务创建任何数据）：**

| 链路 | 能否产生 | 证据 |
|---|---|---|
| Admin Product Mgmt（create/status） | 能 → Published Product | products.controller/service、admin ProductList/Edit |
| Admin SupplierProduct Governance（create→submit→review→approve→publish） | 能 → Published SupplierProduct | supplier-products.controller/service、admin SupplierProductList |
| Organization Mgmt（type=SUPPLIER） | 能 → Provider Organization | organizations.controller（ADMIN） |
| Parameter Mgmt（definitions/groups/product-value） | 能 → Parameter Profile | parameter-definitions、product-parameters controller、admin parameter/* |

**Synthetic Data Prohibition**：762 **未**将 Demo/Mock/Fixture/Seed 当作真实就绪；未创建任何数据。✅

## 16. Onboarding Path Assessment

**Product**：Admin 登录 → `POST /products`（必填 categoryId+name）→ DRAFT →（设参数 Profile：`POST /products/:id/parameters`）→ `PATCH`:id / batch-status 设 `status='ACTIVE'` → 检索可发现。
**SupplierProduct**：SUPPLIER 组织下 Admin → `POST /admin/supplier-products`（绑 platformProductId_product+品牌/型号）→ DRAFT → submit → review → approve → publish → 检索（`status=PUBLISHED AND org.type=SUPPLIER`）可发现。
**Parameter Profile**：Admin → `POST /products/:id/parameters`（parameterDefinitionId+value[+valueNumber]）→ 运行时 `ProductParameterValue→Definition` 可读。

## 17. Gaps / Blockers

- **G1（control）**：SupplierProduct.create 不校验 `org.type='SUPPLIER'`（仅 FK）；可发现性由 searchSuppliers 的 org.type 过滤兜底。
- **G2（control）**：Product 发布用自由字符串 `status='ACTIVE'`（非 enum）；公开列表端点 `GET /products` 默认不过滤状态，检索 `/search` 才强制 `ACTIVE`。
- **G3（gap，非阻断）**：`ProductParameterDefinition`（关联/displayOrder）无独立写接口；运行时 Profile 依赖 `ProductParameterValue`。
- **G4（gap）**：Parameter `required` 无运行时强制 → 必填规则 **UNDEFINED**（Q7）。
- 以上均为记录项，**不在本任务修复**；皆不影响通过既有 API 完成合法落地。
- **核心 Block**：**无（架构/链路可落地）；唯一 Gap = 真实数据实例为 0**。

## 18. Architecture Decision Requirements

- **Shortlist Persistence Boundary = ARCHITECTURE DECISION REQUIRED**（Session vs Persistent User Evaluation State；若持久化需新表 → 独立 Schema Assessment + Migration；762 不裁决、不实施）。
- 可选：G1 的 org-type 校验属未来治理决策（非 Core 阻断）。

## 19. Expansion Gate

保持 OUT OF SCOPE：Compare/Shortlist/Evaluation/Inquiry/RFQ/Matching/Search/Discovery/Marketplace/Storefront/Cart/Checkout/Order/Payment/Commission/Transaction/SEO/LLM/Embedding/Vector/RAG/Search 2.0/Mobile/Accessibility/Homepage/M34.7/M34-FINAL。**未实施任何项。**

## 20. Mobile Carry Forward

- 762 不进行视觉/Mobile 修改；M34.6 Runtime 保持 Desktop+Mobile；`768≈140px overflow` = **CARRY FORWARD**（不借 762 修复）。

## 21. Documentation Synchronization

- 本报告：`docs/_review/762_M34.6_Controlled_Real_Data_Onboarding_Preparation_Assessment_Report.md`
- 同步：`docs/project-management/PROJECT_STATUS.md`、`PROJECT_ROADMAP.md`、`MODULE_COMPLETION_MATRIX.md`、`docs/_architecture/M34_Platform_Architecture_Contract.md`（另文进行）

## 22. Final Readiness Decision（四维）

| 维度 | 判定 | 依据 |
|---|:--:|---|
| Core Data Readiness | **NOT READY（INSTANCE）／STRUCTURALLY READY（PATH）** | 实例全 0；链路可落地 |
| Extended Evaluation Readiness | **NOT READY** | 需 2+；非架构失败 |
| Workflow Scenario Readiness | **CONDITIONAL** | Demand/RFQ/RFQResponse 存在，Inquiry=0 |
| Commercial Readiness | **OPTIONAL** | Offer=0；无场景依赖 |

**Data Preparation = STRUCTURALLY READY（需受控真实数据落地）/ INSTANCE NOT READY**
**M34.6 Authorization = STILL PENDING（PENDING RECHECK）**

### Q1–Q15

- **Q1** 能否合法产生 Published Product？ **YES**（创建→status='ACTIVE'）
- **Q2** 能否合法产生 Published SupplierProduct？ **YES**（治理 PUBLISH）
- **Q3** SupplierProduct 正确绑定 Product？ **YES**（platform_product_id FK + ensurePlatformProductExists）
- **Q4** SupplierProduct 正确绑定 SUPPLIER Org？ **FK YES / 发现性需 org.type='SUPPLIER'**（无 create 时 org.type 运行时校验→control note G1）
- **Q5** 8 个 SUPPLIER Org 是否有既有路径成为 Discoverable Supplier？ **有路径**（治理→PUBLISH），**当前无人可达**（0 Published SP）
- **Q6** Parameter Profile 能否既有链路合法建立？ **YES**（POST /products/:id/parameters upsert value）
- **Q7** 明确 Required/Optional/Runtime-readable 规则？ **UNDEFINED**（required 字段存在但无强制；Runtime-readable=YES）
- **Q8** Core Dataset 最小条件能否现有系统满足？ **结构 YES，实例 NO**
- **Q9** Extended 能否满足？ **结构 YES，实例 NO**
- **Q10** 需新 Schema 完成 Onboarding？ **NO**
- **Q11** 需改 Search/Discovery？ **NO**
- **Q12** 需改 Inquiry/RFQ/Matching？ **NO**
- **Q13** 需新建 Domain Authority？ **NO**
- **Q14** Shortlist 持久化边界仍保持 ARCHITECTURE DECISION REQUIRED？ **YES**
- **Q15** 下一步？ **受控真实数据准备（Controlled Real-data Onboarding / Preparation）独立任务**；就绪后重跑 M34.6 Authorization Recheck（762）。无需新增架构/代码改造任务。

## 23. Mandatory STOP

完成 762 后 **STOP**。未创建任何数据（Product/SupplierProduct/Parameter/Seed/Demo/Mock/Fixture/Synthetic=**NONE**）；未实施 Compare/Shortlist/Evaluation/Inquiry/RFQ 修改；未创建 Schema/Migration；未修改 Search/Discovery/Marketplace；未实施 SEO/LLM/Embedding/Vector/RAG/Mobile。M34.6 = **PENDING RECHECK**（非 AUTHORIZED）。

**762 = Controlled Real-data Onboarding **PREPARATION** Assessment（NOT Data Creation / NOT M34.6 Implementation / NOT M34.6 Authorization）**