# M13.7.3 RFQ Create Flow Report

**Date:** 2026-08-03  
**Task:** M13.7.3 RFQ Create Flow  
**Type:** New feature — Web user RFQ (Request for Quotation) creation

---

## 1. Root Path Verification

| Item | Path | Status |
|------|------|--------|
| Project Root | `F:\Desktop\VISNDT\VISNDT\` | ✅ |
| Target Directory | `apps/web/` | ✅ |
| Report Output | `F:\Desktop\VISNDT\docs\_review\` | ✅ |

---

## 2. Modified Files

| # | File | Change | Type |
|---|------|--------|------|
| 1 | [lib/api/rfqs.ts](file:///F:/Desktop/VISNDT/VISNDT/apps/web/src/lib/api/rfqs.ts) | Added `createRfq()` + `CreateRfqParams` | Modified |
| 2 | [app/workspace/rfqs/create/page.tsx](file:///F:/Desktop/VISNDT/VISNDT/apps/web/src/app/workspace/rfqs/create/page.tsx) | New RFQ Create page | Added |

**Total: 1 modified, 1 added.**

---

## 3. API Reuse

### 3.1 Backend Endpoint

| Item | Detail |
|------|--------|
| **Endpoint** | `POST /rfqs` (JWT) |
| **Auth** | `@UseGuards(JwtAuthGuard)` |
| **DTO** | `CreateRfqDto` — only `demandId: string` |
| **Controller** | `RfqsController.create(@Body() dto, @CurrentUser() user)` |
| **Response** | `ApiResponse.ok(rfq, 'RFQ created')` |

### 3.2 Frontend API Client

| Item | Detail |
|------|--------|
| **Module** | `lib/api/rfqs.ts` (existing) |
| **New Function** | `createRfq(params: { demandId: string })` |
| **HTTP Method** | `POST` with `body: JSON.stringify(params)` |
| **CSRF** | ✅ Auto-attached by `apiClient` |
| **Auto-refresh** | ✅ 401 → refresh → retry |
| **New API Client?** | ❌ No — added to existing module |

### 3.3 Demand API (for Demand Selection)

| Item | Detail |
|------|--------|
| **Module** | `lib/api/demands.ts` (existing) |
| **Function** | `getMyDemands()` — already existed |
| **Endpoint** | `GET /demands/my` (JWT) |
| **Purpose** | Populate demand dropdown in RFQ form |

---

## 4. Form Mapping

### 4.1 Backend DTO → Form

| Backend Field | Form Element | Required | Validation |
|---------------|-------------|----------|------------|
| `demandId` | `<select>` dropdown | ✅ Yes | Non-empty selection |

### 4.2 Field NOT in DTO (not implemented)

| Field | Reason |
|-------|--------|
| `title` | Not in `CreateRfqDto` — RFQ title is derived from demand |
| `description` | Not in `CreateRfqDto` |
| `productId` | Not in `CreateRfqDto` |
| `quantity` | Not in `CreateRfqDto` |
| `quantityUnit` | Not in `CreateRfqDto` |
| `expectedDeliveryDate` | Not in `CreateRfqDto` |

> **Note:** The task specified "优先实现: title, description, productId（如果 DTO 需要）". Since `CreateRfqDto` only contains `demandId`, only `demandId` is implemented — strictly aligned with backend DTO.

### 4.3 Form Layout

```
┌──────────────────────────────────────┐
│  Create RFQ                          │
│  ─────────────────────────────────   │
│  Select Demand *              [▼   ] │
│  ┌────────────────────────────────┐  │
│  │ Selected: "Need industrial..." │  │
│  └────────────────────────────────┘  │
│  ─────────────────────────────────   │
│  [Create RFQ]  [Cancel]              │
└──────────────────────────────────────┘
```

---

## 5. Page Flow

### 5.1 Route

| Item | Detail |
|------|--------|
| **Route** | `/workspace/rfqs/create` |
| **Next.js Type** | Static (○) |
| **First Load JS** | 121 kB |

### 5.2 User Flow

```
┌──────────────────┐
│  /workspace      │  Workspace overview
│  /rfqs           │  RFQ list
│       │
│       ▼  "Create RFQ" button
│  /rfqs/create     │  ← NEW PAGE
│       │
│       ▼  Select demand → Submit
│  POST /rfqs       │  API call
│       │
│       ▼  Success
│  /rfqs            │  Redirect back to list
└──────────────────┘
```

### 5.3 Edge Cases

| Scenario | Handling |
|----------|----------|
| **No demands** | 🟡 Info banner + "Create a demand →" link to `/workspace/demands/create` |
| **Demands loading** | ⏳ Spinner + "Loading demands..." |
| **Demands error** | 🔴 Red error banner |
| **No demand selected** | 🔴 Validation: "Please select a demand" |
| **API error** | 🔴 Red error banner with message |
| **Submit success** | → Redirect to `/workspace/rfqs` |

---

## 6. Auth Handling

| Aspect | Implementation |
|--------|---------------|
| **Page-level guard** | `AuthGuard` wraps the entire page |
| **Redirect** | `router.replace('/login')` if not authenticated |
| **API auth** | `credentials: 'include'` → HttpOnly JWT cookie |
| **CSRF** | `X-CSRF-Token` auto-attached by `apiClient` |
| **Token refresh** | Auto-retry on 401 via `tryRefreshToken()` |

---

## 7. State Handling

| State | Hook | Visual |
|-------|------|--------|
| **Demands loading** | `useQuery` → `isLoading` | ⏳ Spinner |
| **Demands error** | `useQuery` → `isError` | 🔴 Red banner |
| **Demands empty** | `data.length === 0` | 🟡 Amber banner + link |
| **Validation error** | `selectedDemandId === ''` | 🔴 "Please select a demand" |
| **Mutation loading** | `useMutation` → `isPending` | ⏳ Spinner + "Creating..." + disabled |
| **Mutation error** | `useMutation` → `isError` | 🔴 Red banner |
| **Mutation success** | `useMutation` → `onSuccess` | → Redirect to `/workspace/rfqs` |

---

## 8. Freeze Verification

| Directory | Modified? | Status |
|-----------|-----------|--------|
| `apps/web/` | ✅ 2 files | Target |
| `apps/api/` | ❌ 0 files | ✅ Frozen |
| `database/` | ❌ 0 files | ✅ Frozen |
| `packages/` | ❌ 0 files | ✅ Frozen |
| `apps/admin/` | ❌ 0 files | ✅ Frozen |

---

## 9. Build Verification

| Build | Command | Exit Code | Output |
|-------|---------|-----------|--------|
| Web Frontend | `pnpm build` | 0 ✅ | 20 routes (1 new), 0 errors |

### New Route

| Route | Type | Size | First Load JS |
|-------|------|------|---------------|
| `/workspace/rfqs/create` | Static | 1.98 kB | 121 kB |

### All Routes (`/workspace/*`)

```
/workspace               4.41 kB   110 kB
/workspace/demands       3.84 kB   109 kB
/workspace/demands/[id]  4.23 kB   110 kB  (dynamic)
/workspace/demands/create 1.96 kB  115 kB  (M13.7.2)
/workspace/matches       4.15 kB   109 kB
/workspace/rfqs          3.77 kB   109 kB
/workspace/rfqs/[id]     4.26 kB   110 kB  (dynamic)
/workspace/rfqs/create   1.98 kB   121 kB  ← NEW (M13.7.3)
/workspace/settings      3.29 kB   109 kB
```

---

## 10. Final Status

| Item | Status |
|------|--------|
| RFQ Create API client | ✅ `createRfq()` added to existing `lib/api/rfqs.ts` |
| RFQ Create page | ✅ `/workspace/rfqs/create` |
| Form fields | ✅ 1 field (`demandId`) — aligned with `CreateRfqDto` |
| Demand selection | ✅ Dropdown populated via `getMyDemands()` |
| No demands flow | ✅ Info banner + link to create demand |
| Validation | ✅ demandId required |
| CSRF | ✅ Auto-attached |
| JWT auth | ✅ AuthGuard + credentials: 'include' |
| Token refresh | ✅ Auto-retry on 401 |
| Loading (demands) | ✅ Spinner |
| Loading (submit) | ✅ Spinner + disabled button |
| Error (demands) | ✅ Red banner |
| Error (API) | ✅ Red banner |
| Empty (demands) | ✅ Amber banner + link |
| Success redirect | ✅ `router.push('/workspace/rfqs')` |
| No new API client | ✅ Reused existing module |
| No useEffect | ✅ TanStack Query `useQuery` + `useMutation` |
| No direct fetch/axios | ✅ Through `apiClient` |
| No payment/chat/upload/approval/AI | ✅ |
| No database changes | ✅ |
| Build | ✅ Exit Code 0 |
| Freeze | ✅ Only apps/web modified |

### Summary

| Metric | Value |
|--------|-------|
| Files Modified | 1 |
| Files Added | 1 |
| API Functions Added | 1 (`createRfq`) |
| Form Fields | 1 (`demandId`) |
| States Handled | 7 (demands loading/error/empty + validation/submit/pending/error/success) |

---

**Overall Status: ✅ COMPLETE**

**Next:** 等待审核。