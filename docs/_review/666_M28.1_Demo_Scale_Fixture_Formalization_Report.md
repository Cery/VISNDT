# 666 M28.1 Demo Scale Fixture Formalization — Review Report

- **Task**: 666_M28.1_Demo_Scale_Fixture_Formalization
- **Stage**: M28.1（首个任务）
- **Status**: **PASS**
- **Review Date**: 2026-08-23
- **Goal**: 将 662 / 662.1 已验证的 SupplierProduct Demo + Scale 数据形式化为 **M28 Demo Scale Fixture = Controlled / Reproducible / Non-Production Dataset**，为后续 Buyer/Supplier/Admin E2E、Search Regression、SupplierProduct Comparison、Product Experience Audit、M28.1 Final Validation 提供稳定、可重复、可识别、可回滚的 Demo 数据基础设施。
- **Execution Principle**: Demo Data Is Product Experience Infrastructure / Fixture Before Repeated E2E / Reproducibility Before Optimization / Idempotency Before Scale / Isolation Before Automation / Observed Scale != Projected Scale / Core Demo Data Must Be Preserved / Existing Architecture Before New Architecture / No Schema Expansion / No Business Expansion / No Scope Expansion / Documentation Before Next Stage。

---

## 1. Repository Verification

| Item | Value | Result |
|------|-------|--------|
| 仓库根目录（Git root） | `F:\Desktop\VISNDT` | VERIFIED |
| 代码根目录 | `F:\Desktop\VISNDT\VISNDT` | VERIFIED |
| 分支 | `main` | VERIFIED |
| 任务 666 变更范围 | Database Fixture 层 only | VERIFIED |

任务 666 实际改动（仅 Seed / Fixture Code + Package Script + Demo Data + Documentation）：

- `VISNDT/database/fixture/index.ts`（新增）：M28 Fixture master runner（seed / clean / reset 三模式）
- `VISNDT/database/package.json`（修改）：新增 `seed:m28-fixture` / `seed:m28-fixture:clean` / `seed:m28-fixture:reset` 脚本
- `VISNDT/database/seed_supplier_product.ts` + `seed_scale_6621.ts`：既有 seed（662 已落地），本任务**未改写其业务逻辑**，仅由 master runner 驱动
- 验证脚本（临时，不入产品代码）：`_666_counts.mjs` / `_666_ident.mjs` / `_666_matrix.mjs` / `_666_verify.mjs` / `_666_regression.mjs`

## 2. Baseline Verification

| Baseline Task | Status | Frozen Confirmation |
|---------------|--------|---------------------|
| 665 M28.0 Platform Productization E2E Experience Audit | PASS | M28.0 Productized Baseline = READY |
| 664 M28.0 Supplier Product Management Scaling | PASS | — |
| 663 M28.0 Admin IA and Governance Optimization | PASS | — |
| 662.1 M28.0 Three Role Scale Validation | PASS | — |

冻结确认：**Hybrid Model C = FROZEN**、**Unified Search = FROZEN**（本任务保持，不做任何运行时激活/架构改动）。

## 3. Existing Seed / Fixture Reality Scan

| Seed / Fixture | 是否写 SupplierProduct | 可识别前缀 | 角色 |
|----------------|------------------------|------------|------|
| `seed_demo.ts` | **否**（不创建 SupplierProduct） | — | Core Demo（Users / Organizations / Platform Products / Offers / Content / Knowledge / Demand / RFQ / Inquiry / Notifications） |
| `seed_supplier_product.ts` | 是 | `mingshi-` / `ruishi-` / `zhongke-`（+ `scale-`） | Base Demo 13 models |
| `seed_scale_6621.ts` | 是 | `scale-` | Scale +8 DRAFT（662.1 受控数据） |

扫描结论：全部 21 条 SupplierProduct 均来自两个既有 seed，`seed_demo.ts` 不写 SupplierProduct → **表中无非 Fixture 数据**。该事实是 Fixture 可完全识别的根基。

## 4. Fixture Classification

| Fixture 类别 | 定义 | 本任务归属 |
|--------------|------|------------|
| Core Demo Data | 平台基线演示数据（业务闭环各实体），须永久保留 | Users / Organizations / Platform Products（7）/ Offers（7）/ Inquiry（9）/ RFQ（12）/ RFQ Response（12）/ Content / Knowledge / Demand |
| Demo Fixture（Base） | SupplierProduct 基础演示数据，可回滚可重建 | 13 models（mingshi/ruishi/zhongke 前缀） |
| Scale Fixture | 规模压测数据，可回滚可重建 | +8 DRAFT（scale- 前缀） |
| Controlled Demo Fixture（合成） | Base + Scale，作为单一可复现受控入口 | M28 Demo Scale Fixture（本任务产物） |

**正式定义**：**M28 Demo Scale Fixture = Controlled / Reproducible / Non-Production Dataset**。

## 5. Fixture Architecture Decision

**Decision: Option C — 独立 Fixture 目录 + master runner（确认）**

```
database/fixture/
    index.ts          <- master runner（seed / clean / reset）
```

- **seed**（默认）：`seed_supplier_product.ts`（Base Demo 13）+ `seed_scale_6621.ts`（Scale +8 DRAFT）
- **clean**（`--clean`）：仅删除 Fixture SupplierProduct（级联 Media / ParameterValue）+ 解除 Offer 绑定，保留 Core Demo
- **reset**（`--reset`）：clean + seed

驱动方式为 spawn 既有已验证 seed（**零业务逻辑改写**），保证 Fixture 与既有数据口径一致。Package Script：`npm run seed:m28-fixture` / `seed:m28-fixture:clean` / `seed:m28-fixture:reset`。

## 6. Fixture Identification Strategy

**唯一策略：`supplier_product.slug` 前缀族 `(scale|mingshi|ruishi|zhongke)-` = M28 Demo Fixture 唯一可识别标记**

- DB 实测：`total=21 / identifiable=21 / missing=[]`（`_666_ident.mjs`）
- `seed_demo.ts` 不写 SupplierProduct → 表中 100% 数据均可通过前缀识别
- 该前缀族同时用于 `clean`（`delete where slug ~ '(scale|mingshi|ruishi|zhongke)-'`），确保只删 Fixture、不碰任何非 Fixture 数据

## 7. Idempotency Verification

| Stage | supplier_product | PUBLISHED | offer_with_sp |
|-------|------------------|-----------|---------------|
| Before | 21 | 6 | 6 |
| First Seed | 21 | 6 | 6 |
| Second Seed | 21 | 6 | 6 |

- 两个 seed 均按自然键 **upsert** → 重复执行 count 不变（**A == B**）
- SupplierProductMedia 按 supplierProductId **先删后建** → 幂等
- **结果：Idempotency = PASS**（第二次执行不产生重复业务数据）

## 8. Rollback / Cleanup Strategy

`--clean` 实测（`fixture/index.ts`）：

1. `update offer set supplier_product_id = NULL where supplier_product_id in (select id from supplier_product where slug ~ '...')` → 解除 Offer 绑定 **6**
2. `delete from supplier_product where slug ~ '...'` → 删除 Fixture SupplierProduct **21**（级联 Media / ParameterValue）

清理后：`supplier_product=0 / media=0 / parameter_value=0 / offer_with_sp=0`

**Core Demo 全保留（未被误删除）**：`offer=7 / inquiry=9 / rfq=12 / rfq_response=12`（清理前后不变）

清理后 `seed` 可完整还原 → **FINAL_RESTORED = 21 / 6 / 6**（当前 DB 状态即还原后状态，`_666_counts.mjs FINAL_REPORT` 复核）

- **结果：Rollback / Cleanup = SAFE**
- 生产防护：`NODE_ENV=production` 时禁止运行 clean（进程退出）

## 9. SupplierProduct Coverage Matrix

**3 Supplier Orgs × 6 Platform Capabilities × 21 Models**

| Organization | 覆盖 Capability | Models | 状态 |
|--------------|-----------------|--------|------|
| 明视（D5reg） | 3DSCAN / FB-3000 / MIC-5000 / US-200 / US-800 / VX-6000 | 11 | DRAFT / SUBMITTED / REVIEWING / APPROVED / PUBLISHED |
| 锐视 | 3DSCAN / FB-3000 / MIC-5000 / US-200 / VX-6000 | 6 | DRAFT / REJECTED / PUBLISHED |
| 中科 | FB-3000 / US-200 / US-800 / VX-6000 | 4 | DRAFT / SUBMITTED / PUBLISHED |

**状态全覆盖**：DRAFT 10 / SUBMITTED 2 / REVIEWING 1 / APPROVED 1 / PUBLISHED 6 / REJECTED 1

**Integrity**：`sp_total=21 / with_real_org=21 / with_real_product=21`；Offer 绑定 `bound_offers=6 / cross_org_bad=0 / cross_cap_bad=0`

## 10. Published Boundary Verification

- 公开 `/search?type=supplier-product` 仅返回 **PUBLISHED** SupplierProduct
- 实测：`total=6 / status={PUBLISHED:6} / onlyPublished=true`
- DRAFT / SUBMITTED / REVIEWING / APPROVED / REJECTED **均不进公共发现**（服务端强制 Published Gate）
- **结果：Published Boundary = PASS**

## 11. Supplier Isolation Verification

| Supplier | Runtime n | brand | crossVisible |
|----------|-----------|-------|--------------|
| s1（明视） | 11 | [明视] | 0 |
| s2（锐视） | 6 | [锐视] | 0 |

- 每个 Supplier 仅能看到自己组织的 SupplierProduct，**无跨组织泄漏**
- **结果：Supplier Isolation = PASS**

## 12. Admin Governance Verification

- `/admin/supplier-products` pool：`n=21` 跨 3 组织可见（Admin = Platform Governance Center）
- detail：200（hasOrg / hasProduct）
- status=PUBLISHED 过滤：`n=6 / allMatch=true`
- **结果：Admin Governance = PASS**

## 13. Buyer Discovery Verification

| Query | 结果 |
|-------|------|
| Keyword q=内窥镜 | total=2，全 PUBLISHED |
| Brand=明视 | total=2 allMatch |
| Series=精密扫描系列 | total=1 allMatch |
| hasOffer=true | total=6 allHaveOffer |
| 分页 | page1=2 + page2=2 distinct=4 total=6 |

- **Observed Scale（Buyer 可见）= 6**（仅 PUBLISHED）；DB 全部 = 21
- **结果：Buyer Discovery = PASS**

## 14. Inquiry Verification

- s1 PUBLISHED 型号 inquiry-context：200（含 supplierProductId / platformProductName / inquiries / total）
- 型号维度 Inquiry 上下文链路正常
- **结果：Inquiry = PASS**

## 15. Multi-Supplier Verification

DB 级多组织同 Capability 覆盖（`_666_matrix.mjs`）：

| Capability | Orgs | Models |
|------------|------|--------|
| VX-6000 高清视频内窥镜 | 3 | 7 |
| FB-3000 光纤内窥镜 | 3 | 3 |
| US-200 便携式超声波探伤仪 | 3 | 3 |
| 3DSCAN-Pro 结构光三维扫描仪 | 2 | 3 |
| MIC-5000 工业金相显微镜 | 2 | 3 |
| US-800 超声波探伤仪 | 2 | 2 |

- **Observed Scale = 3 orgs**（真实数据）
- **Projected Scale = 50+ orgs 能力 = CONTRACT / QUERY CAPABILITY VERIFIED**（非真实运行数据，绝不作 REAL RUNTIME VERIFIED）
- **结果：Multi-Supplier = PASS**

## 16. Media Fixture Verification

| media_type | document_type | n |
|------------|---------------|---|
| IMAGE | — | 4 |
| SPEC_SHEET | SPEC_SHEET | 1 |

- Media 5 条，覆盖 5 型号，均绑定真实 SupplierProduct
- **结果：Media = PASS**

## 17. Parameter Fixture Verification

- ParameterValue 共 **13** 条，覆盖 13 型号参数，均绑定真实 SupplierProduct
- **结果：Parameter = PASS**

## 18. Runtime Validation Matrix

| # | 维度 | 结果 |
|---|------|------|
| 1 | 幂等性（Before == First == Second） | 21/6/6 == 21/6/6 == 21/6/6 PASS |
| 2 | 清理回滚（clean） | SAFE，Core Demo 保留 |
| 3 | 清理后还原（seed） | FINAL_RESTORED = 21/6/6 PASS |
| 4 | 识别（slug 前缀） | 21/21 命中 PASS |
| 5 | Published Boundary | total=6 onlyPublished PASS |
| 6 | Supplier Isolation | s1=11 / s2=6 / cross=0 PASS |
| 7 | Admin Governance | pool=21 / detail=200 / filter=6 PASS |
| 8 | Buyer Discovery | Keyword/Brand/Series/hasOffer/分页 全 PASS |
| 9 | Inquiry context | 200 PASS |
| 10 | Multi-Supplier | 6 Capability 多组织 PASS |
| 11 | Media | 5（IMAGE 4 + SPEC_SHEET 1）PASS |
| 12 | Parameter | 13 PASS |

## 19. Data Count Comparison

| 表 | Before | First Seed | Second Seed | After Cleanup |
|----|--------|------------|-------------|---------------|
| supplier_product | 21 | 21 | 21 | 0 |
| supplier_product_media | 5 | 5 | 5 | 0 |
| supplier_product_parameter_value | 13 | 13 | 13 | 0 |
| offer | 7 | 7 | 7 | **7**（保留） |
| inquiry | 9 | 9 | 9 | **9**（保留） |
| rfq | 12 | 12 | 12 | **12**（保留） |
| rfq_response | 12 | 12 | 12 | **12**（保留） |
| sp_published | 6 | 6 | 6 | 0 |
| offer_bound_to_sp | 6 | 6 | 6 | 0 |

- **Core Demo Data 未被误删除**：offer / inquiry / rfq / rfq_response 在 After Cleanup 后保持不变
- **Fixture Data 是否可完全识别**：**是**，21/21 命中 slug 前缀族，`seed_demo.ts` 不写 SupplierProduct → 无非 Fixture 数据，可 100% 识别与移除

## 20. Regression Verification

**Build（exit 0）**：`apps/api build`（nest）= 0 / `apps/web build`（next）= 0 / `apps/admin build`（tsc+vite）= 0

**Runtime（当前复核，全 200）**：

| 端点 | 结果 |
|------|------|
| GET /products（公开） | 200 first=true |
| GET /products/:id（公开） | 200 name=… |
| GET /offers（公开） | 200 total=7 |
| GET /search?q=内窥镜（Unified Search） | 200 products=2 sp=2 |
| Buyer login + /inquiries/mine | 201 / 200 |
| Buyer /demands | 200 total=11 |
| Buyer /rfqs/mine | 200 total=11 |
| Supplier login + /workspace/supplier/rfqs | 201 / 200 total=3 |
| Admin login + /admin/matching/stats | 201 / 200 |
| Admin /admin/dashboard/stats | 200 |

**无下列回归**：Search Regression（Unified Search 正常返回 Product + SupplierProduct 双维度）/ Permission Regression（Buyer/Supplier/Admin 登录与授权正常）/ Cross Organization Exposure（Supplier Isolation 无泄漏）/ Published Gate Regression（公开搜索仅 PUBLISHED）/ Offer Binding Regression（Offer 绑定关系正常）

**结果：Regression = PASS**

## 21. Future Candidate Trigger Assessment

| Candidate | 触发条件 | 当前状态 | 结论 |
|-----------|----------|----------|------|
| F1 Series Entity | 同 Supplier + 同 Series ≥ 5 Models，或 Series 明显阻碍管理 | 最密 同 Supplier+同 Series 未达 ≥5 | **未触发** |
| F2 Supplier Product Pool | 单 Supplier ≥ 50 SupplierProducts | 单供应商最多 11 < 50 | **未触发** |
| D8 Batch Operation | 审核队列 ≥ 15，或人工审核成本显著上升 | 审核队列 3 < 15 | **未触发** |
| F3 Media Folder | entityType/entityId/organizationId 不足以治理媒体 | 5 Media 可治理 | **未触发** |

**结论：F1 / F2 / D8 / F3 = FUTURE CANDIDATE（保持）**。本任务只重新确认，不实现。

## 22. Architecture Impact

| 维度 | 状态 |
|------|------|
| Database | UNCHANGED |
| Schema | UNCHANGED |
| Migration | NONE |
| Product | UNCHANGED |
| SupplierProduct | UNCHANGED |
| Offer | UNCHANGED |
| Search | **FROZEN**（保持） |
| Matching | UNCHANGED |
| RFQ | UNCHANGED |
| Inquiry | UNCHANGED |
| API | UNCHANGED |
| Hybrid Model C | **FROZEN**（保持） |

允许改动范围：Seed / Fixture Code、Package Script、Demo Data、Documentation。**未触发** Schema / API / Business Logic 变更（如遇即 STOP / REPORT / DO NOT IMPLEMENT）。

## 23. Documentation Synchronization

| 文档 | 状态 |
|------|------|
| `docs/project-management/PROJECT_STATUS.md` | SYNCED（666 节，PASS） |
| `docs/project-management/PROJECT_ROADMAP.md` | SYNCED（666 节，PASS） |
| `docs/project-management/MODULE_COMPLETION_MATRIX.md` | SYNCED（666 行，PASS） |

明确记录：

```
665  M28.0 Productized Baseline READY
        ↓
666  Demo Scale Fixture Formalization
```

正式定义：**M28 Demo Scale Fixture = Controlled / Reproducible / Non-Production Dataset**。

## 24. Final Decision

| 判定项 | 要求 | 实测 |
|--------|------|------|
| Fixture = Reproducible | 可重复执行 | PASS |
| Idempotency | PASS | PASS（21/6/6 == 21/6/6） |
| Rollback / Cleanup | SAFE | SAFE（Core Demo 保留，seed 可还原） |
| Published Boundary | PASS | PASS（仅 PUBLISHED=6） |
| Supplier Isolation | PASS | PASS（crossVisible=0） |
| Buyer Discovery | PASS | PASS |
| Admin Governance | PASS | PASS |
| Regression | PASS | PASS（build 0 + runtime 全 200） |
| P0 | 0 | **0** |
| P1 | 0 | **0** |
| P2 | 允许 | 1（Fixture command 尚需进一步封装：README / CLI 参数校验 / 多环境 DATABASE_URL 隔离） |
| P3 | 允许 | 0 |

所有判定项满足、P0=0 / P1=0 → **666 = PASS**，授权进入 667。

---

## Final Execution Output

```
Task:
666_M28.1_Demo_Scale_Fixture_Formalization

Status:
PASS

Repository:
VERIFIED

Fixture Architecture:
Option C（database/fixture/ master runner：seed / clean / reset）

Fixture Identification:
slug 前缀族 (scale|mingshi|ruishi|zhongke)-（21/21 命中，missing=0）

Core Demo Data:
PRESERVED（offer=7 / inquiry=9 / rfq=12 / rfq_response=12 清理后不变）

SupplierProduct Fixture:
VERIFIED（21 Models / 3 Orgs / 6 Capabilities / 6 PUBLISHED / 6 Offer-bound）

Scale Fixture:
VERIFIED（+8 DRAFT，idempotent）

Idempotency:
PASS（Before == First Seed == Second Seed = 21/6/6）

Rollback:
SAFE（--clean 仅删 Fixture，Core Demo 保留；seed 可还原 FINAL_RESTORED=21/6/6）

Supplier Isolation:
PASS（s1=明视 n=11 / s2=锐视 n=6 / crossVisible=0）

Published Boundary:
PASS（公开 /search 仅 PUBLISHED=6，onlyPublished=true）

Admin Governance:
PASS（pool n=21 跨 3 组织 / detail=200 / PUBLISHED 过滤 n=6）

Buyer Discovery:
PASS（Keyword / Brand / Series / hasOffer / Pagination 全验证）

Inquiry:
PASS（inquiry-context=200）

Multi-Supplier:
PASS（DB 级 6 Capability 多组织；Observed=3 orgs REAL，Projected=50+ QUERY-CAPABILITY）

Media:
PASS（5 Media：IMAGE 4 + SPEC_SHEET 1）

Parameter:
PASS（13 ParameterValues / 13 型号覆盖）

Regression:
PASS（api/web/admin build exit 0；Unified Search/Product/Offer/Inquiry/Demand/RFQ/Matching/Dashboard 全 200）

Observed Scale:
21 Models（REAL DATA VERIFIED；public discovery=6 PUBLISHED）

Projected Scale:
50+ Models（CONTRACT / QUERY CAPABILITY VERIFIED）

P0:
0

P1:
0

P2:
1（Fixture command 尚需进一步封装：README / CLI 参数校验 / 多环境 DATABASE_URL 隔离）

P3:
0

Hybrid Model C:
FROZEN

Unified Search:
FROZEN

Database:
UNCHANGED

Schema:
UNCHANGED

Migration:
NONE

API:
UNCHANGED

Matching:
UNCHANGED

RFQ:
UNCHANGED

Documentation:
SYNCED

Review Report:
docs/_review/666_M28.1_Demo_Scale_Fixture_Formalization_Report.md

Next:
667_M28.1_SupplierProduct_Comparison_Experience
```
