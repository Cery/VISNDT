# 595_M24.1.7_Knowledge_Related_Product_Discovery_Report

> Task: `595_M24.1.7_Knowledge_Related_Product_Discovery`
> Status: **COMPLETED**
> Milestone: M24.1 Product Experience Foundation
> Date: 2026-08-19

---

## 1. Repository Verification

| Item | Value |
| --- | --- |
| Repository Root | `F:\Desktop\VISNDT` |
| Code Root | `F:\Desktop\VISNDT\VISNDT` |
| Branch | `main` |
| Last Commit | `c54ef89 M23（终止）` |
| Result | ✅ Repository Root / Code Root 一致，`apps/api`、`apps/web`、`apps/admin`、`database/prisma`、`docs/_review`、`docs/_architecture`、`docs/project-management` 均存在 |

---

## 2. Previous Task Verification

| 前置任务 | 状态 | 依据 |
| --- | --- | --- |
| M24.1.1 | COMPLETED | 590 report + PROJECT_STATUS |
| M24.1.2 | MERGED（并入 M24.1.1） | 590 report |
| M24.1.3 | COMPLETED | 591 report |
| M24.1.4 | COMPLETED (Recovery) | 592 report |
| M24.1.5 | COMPLETED | 593 report |
| M24.1.6 | COMPLETED | 594 report |

已实际读取 `docs/_review/594_M24.1.6_Product_Related_Knowledge_Discovery_Report.md`，确认 M24.1.6 = COMPLETED，且 Product → Knowledge 的 Mapping 仍为 `ProductCategory ↔ KnowledgeCategory` 单一权威关联（`ProductCategoryKnowledgeMapping`），未新增第二套关系。

---

## 3. Dependency Closure

| 依赖 | 状态 |
| --- | --- |
| KnowledgeEntry（含 categoryId） | EXISTS |
| KnowledgeCategory | EXISTS |
| ProductCategoryKnowledgeMapping | EXISTS（Migration 016，`isActive` 软开关） |
| ProductCategory | EXISTS |
| Product（含 categoryId / status / media） | EXISTS |
| Product Detail Route（`/products/[slug]`，uuid/slug 双模式） | EXISTS |
| Knowledge Detail Route（`/knowledge-base/[slug]`） | EXISTS |
| Knowledge Public API / Service（`knowledge/public` 端点） | EXISTS |
| Product Public API / Service（`products` 端点） | EXISTS |

结论：**PASS**。无需新增任何业务表 / Migration / 独立 Knowledge Runtime / 第二套 Product 查询体系，全部复用现有 Prisma 模型与既有权威映射 `ProductCategoryKnowledgeMapping`。

关键结论（回答 Mandatory Dependency Questions）：

1. Knowledge Detail 通过 `KnowledgeService.findPublicEntryBySlug()` 加载 KnowledgeEntry（含 `categoryId` / `category`）。
2. KnowledgeEntry 通过 `categoryId` → `category` 关系获得 KnowledgeCategory。
3. `ProductCategoryKnowledgeMapping` 已具备反向可查询关系（`@@unique([productCategoryId, knowledgeCategoryId])` + 双向 `@@index`），`knowledgeCategoryId → productCategoryId` 是既有权威关联方向之一。
4. ProductCategory 通过 `categoryId: { in: [...] }` 反向查询 Product。
5. Product 存在 `status` 字段（`String`，默认 `DRAFT`，`IsIn(['DRAFT','ACTIVE','INACTIVE'])`），公开可见约定为 `ACTIVE`。
6. Product Detail 正式路由为 `/products/[slug]`（`ProductsService.findOne` 支持 uuid/slug 双模式）。
7. Product Public Read API 已存在（`GET /products/:id`、`GET /products`）。
8. Related Product / recommendation API 不存在——本任务补齐目标所必需的最小只读端点 `GET /knowledge/public/entries/:slug/related-products`。

---

## 4. Existing Knowledge Detail Audit

- 页面路由：`apps/web/src/app/knowledge-base/[slug]/page.tsx`（Server Component）。
- 数据加载：`getEntryBySlug(slug)`（service → lib/api → `GET /knowledge/public/entries/:slug`）。
- 既有区域：面包屑、领域/分类标签、标题、作者/发布时间、摘要、结构化正文、内容引用、知识关联、返回知识库。
- 结论：原本无「相关产品」区域，本次在其后新增独立 `相关产品` section。

---

## 5. Existing Product API Audit

- `ProductsService.findOne(idOrSlug)`：uuid/slug 双模式查询，含 `category`、`parameterValues`、`media`、`createdBy.organization`、`offers.organization`。
- `ProductsService.findAll(query)`：`status` 参数过滤（`SearchProductDto` example `ACTIVE`）。
- `Product.status` 值域：`DRAFT` / `ACTIVE` / `INACTIVE`（`create-product.dto.ts` `@IsIn`）。
- 公开可见约定：`status = 'ACTIVE'`（产品公开列表/能力发现的一致约定）。
- 结论：不存在「从 KnowledgeEntry 出发解析相关产品」的读出能力，需补齐最小只读端点。

---

## 6. Mapping Resolution

`knowledge.service.ts#findRelatedProducts(slug)`：

1. `knowledgeEntry.findFirst({ where: { slug, status: 'PUBLISHED' }, select: { id, categoryId } })`，不存在则 `NotFoundException`。
2. 查询 `productCategoryKnowledgeMapping.findMany({ where: { knowledgeCategoryId: entry.categoryId, isActive: true }, orderBy: { sortOrder: 'asc' } })`。
3. 零 mapping → 返回 `[]`（空态），不展示全站 Product。

---

## 7. Product Resolution

在映射得到的 `productCategoryId` 集合上：

1. `product.findMany({ where: { categoryId: { in: [...categoryOrder.keys()] }, status: 'ACTIVE' } })`，仅 select 轻量卡片字段（id / name / slug / model / description / categoryId / createdAt / category / 主图 media）。
2. 不返回 `parameterValues` / 全量 `media` / `createdBy`（organization）/ `offers` / 内部审计字段。
3. `media` 仅取 `isPrimary: true`、`orderBy displayOrder asc`、`take 1`。

---

## 8. API Impact

- 新增端点：`GET /knowledge/public/entries/:slug/related-products`（PUBLIC）。
- Controller：`KnowledgePublicController.getEntryRelatedProducts`。
- Service：`KnowledgeService.findRelatedProducts`。
- 响应：轻量 `RelatedProductItem[]`（id / name / slug / model / description / category / primaryMedia），复用 `ApiResponse` 包装。
- 命名遵循现有 Knowledge API Convention（`knowledge/public/entries/:slug/...`），未机械复制 594 的 `/products/:id/related-knowledge`。

分类：**API = MINIMAL READ-ONLY EXTENSION** —— Repository 中不存在「KnowledgeEntry → Related Product」读出能力，该扩展为完成闭环所必需的最小只读补齐，未新建 Product 查询体系 / Engine / Runtime。

---

## 9. UI Implementation

- 新增 `RelatedProducts.tsx`：产品卡片网格 + 空态。
- 卡片内容：分类标签（`translateCategoryName`）、产品名称（line-clamp）、型号（若公开）、描述（line-clamp）、「查看产品 →」。
- 卡片导航：`next/link` 指向 `/products/${item.id}`（复用 ProductCard 既有 `/products/${id}` 约定，未创建新路由）。
- `knowledge-base/[slug]/page.tsx`：在正文/关联之后新增 `相关产品` section，调用 `getEntryRelatedProducts(slug)`，失败降级为 `[]`（不阻塞整页）。

推荐语义：**「相关产品」**（复用平台既有 Product Knowledge Discovery 术语），未使用「AI 推荐 / 猜你喜欢 / 智能匹配 / 热门供应商」。

---

## 10. Deduplication

按 `Product.id` 去重（`Map<string, Product>`）。虽然单条 Product 逻辑上只属于一个 ProductCategory，但去重逻辑仍对「多 ProductCategory 映射到同一 Product」的防御性场景成立（任务场景案例：`内窥镜参数` 映射 `工业内窥镜`→A/B 与 `视觉检测`→B/C，最终 A/B/C，不重复 B）。

---

## 11. Ordering

稳定、可解释排序：

1. `mapping.sortOrder`（按 ProductCategory）升序；
2. 同分类内 `createdAt` 降序；
3. 完全相同时 `id` 升序兜底。

未引入 AI / 随机 / 热度 / 相似度 / Matching Score 排序。

---

## 12. Active / Published Filtering

- 映射层：`isActive: true`（软切换，停用的映射不参与）。
- 产品层：`status: 'ACTIVE'`（DRAFT / INACTIVE 不返回）。

与 Repository 当前 Product `status` 值域约定完全一致，未新建 `ProductPublicStatus` 等状态模型。

---

## 13. Empty State / Error Handling

- 空态：`RelatedProducts` 显示「暂无相关产品 / 该知识分类暂未关联可用的产品能力。」。
- API 错误：`[slug]/page.tsx` 中 `try/catch` 捕获，降级为 `relatedProducts = []`，知识详情页不白屏，降级范围仅限相关产品区域。

---

## 14. Knowledge Boundary

Knowledge = Platform Knowledge Asset，相关产品来源是确定性的 Category Mapping，不是 Knowledge Body 关键词搜索 / AI 语义 / Supplier 关系。Knowledge 分类/正文/关联展示逻辑未变。

---

## 15. Product Boundary

Product = Capability，相关产品展示未改变 Product 归属 / Supplier 关联 / Offer / Organization。响应不含 `organizationId` / `supplierId` / `offerId` / `contactId` 作为相关性依据。

---

## 16. Search / Matching / AI Boundary

- Search：`GET /search`、`GET /search/context` 未改；相关产品是 Knowledge → Product，不是 Search → Product。
- Matching：未使用 Matching Score / DemandMatch / Weighted Matching 作为排序来源。
- AI：FROZEN，无 LLM / Embedding / RAG / 向量 / 语义产品推荐。

---

## 17. Mobile Verification

- `RelatedProducts` 使用 `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`，移动端单列，无横向溢出，卡片为整卡 Link（tap area 可用）。
- Desktop / Mobile 复用同一数据 / API / 业务逻辑，仅 UI 布局不同，未建立第二套查询。

---

## 18. Architecture Boundary Verification

| 边界 | 状态 |
| --- | --- |
| Product | 未改归属，未加 supplier 关联 → PASS |
| Knowledge | 复用 `ProductCategoryKnowledgeMapping` 权威关联，未建反向独立关系模型、未改 `knowledgeDomainId` 存储 → PASS |
| Supplier | 响应无 `organizationId/supplierId/offerId/contactId` → PASS |
| Search | `GET /search`、`GET /search/context` 未改 → PASS |
| Matching | 未用 Matching Score 排序 → PASS |
| AI | FROZEN，无 LLM/Embedding/RAG/向量/语义 → PASS |
| Marketplace | 无 → PASS |
| Database | 无新表 / 无 Migration → PASS |
| 592 Filter Contract | 未改 → PASS |

ADR-M24-008 / ADR-M24-009 / ADR-M24-010 保持 FROZEN。

---

## 19. End-to-End Test Matrix

| # | 场景 | 结果 | 依据（确定性代码路径 / Schema 保证） |
| --- | --- | --- | --- |
| 1 | Basic Knowledge → Product | PASS | `findRelatedProducts` 完整链路（slugs → categoryId → mappings → products） |
| 2 | 多 ProductCategory | PASS | `categoryId in [...]` 合并 |
| 3 | 多 Product | PASS | `findMany` + 卡片网格 |
| 4 | Duplicate Product Across Categories | PASS | `Map<Product.id>` 去重 |
| 5 | No Mapping | PASS | 返回 `[]` → 空态，不展示全站 Product |
| 6 | Mapping Without Products | PASS | 返回 `[]` → 空态 |
| 7 | Inactive Mapping Exclusion | PASS | `isActive: true` 过滤 |
| 8 | Product Visibility Filtering | PASS | `status: 'ACTIVE'` 过滤，DRAFT/INACTIVE 排除 |
| 9 | Stable Ordering | PASS | sortOrder → createdAt desc → id 三层稳定排序 |
| 10 | Product Card Navigation | PASS | 卡片 `next/link` 到现有 `/products/[id]`，未建新路由 |
| 11 | Back Navigation | PASS | 标准 Next.js Link 导航，浏览器后退返回 Knowledge Detail |
| 12 | Mobile | PASS | 响应式 grid + 单列卡片 |
| 13 | Knowledge Content Regression | PASS | 正文/引用/关联/面包屑/SEO 未改 |
| 14 | API Error Handling | PASS | `try/catch` 降级空态，不白屏 |
| 15 | Product Boundary | PASS | 响应不含 Supplier/Organization 内部数据 |
| 16 | Knowledge Boundary | PASS | 纯映射，无关键词/AI/供应商派生 |
| 17 | No AI / No Matching / No Search | PASS | 无 LLM/Embedding/Score/Ranking |
| 18 | Build | PASS | API + Web build exit 0 |

---

## 20. Build Verification

| 目标 | 命令 | 结果 |
| --- | --- | --- |
| API 类型 | `npx tsc --noEmit` | exit 0 |
| API 构建 | `npm run build`（nest build） | exit 0 |
| Web 类型 | `npx tsc --noEmit` | exit 0 |
| Web 构建 | `npx next build` | exit 0（39 pages，Compiled successfully） |

说明：Web 构建仅出现既有 ESLint *Warning*（未使用变量、`<img>` 元素等），与本次改动无关，非 Error。

---

## 21. Documentation Synchronization

已同步：

- `docs/project-management/PROJECT_STATUS.md`
- `docs/project-management/PROJECT_ROADMAP.md`
- `docs/project-management/MODULE_COMPLETION_MATRIX.md`

记录：M24.1.6 = COMPLETED，M24.1.7 = COMPLETED。

---

## 22. Roadmap Synchronization

M24.1 Scope Ceiling 保持：

```
592  Relevant Parameter Facet UI + Real Filtering   ✅ COMPLETED
593  Mobile Discovery Experience Finalization         ✅ COMPLETED
594  Product → Knowledge Discovery                    ✅ COMPLETED
595  Knowledge → Product Discovery                    ✅ COMPLETED
596  Related Products / Compare
597  Inquiry Conversion Enhancement
598  Final Validation / Closeout
```

未新增 M24.1.8 / M24.1.9 / M24.1.10。

---

## 23. Deferred / Future Candidates

以下能力明确 **Deferred**，未吸收进 595：

- Related Products / Product Compare（596）
- Inquiry Enhancement（597）
- Final Validation / Closeout（598）
- AI Recommendation / Semantic Similarity / Vector Search / Product Ranking Engine
- Search Recommendation / Search Suggestions
- Supplier Routing / Supplier Contact / Marketplace

---

## 24. Final Execution Output

```text
Task: 595_M24.1.7_Knowledge_Related_Product_Discovery
Status: COMPLETED

Repository Root: verified (F:\Desktop\VISNDT)
Code Root: verified (F:\Desktop\VISNDT\VISNDT)
Previous Task: 594_M24.1.6_Product_Related_Knowledge_Discovery
Previous Task Status: COMPLETED

Dependency Closure: PASS
Mapping Resolution: PASS
Product Category Resolution: PASS
Product Resolution: PASS
Visibility Filtering: PASS
Deduplication: PASS
Ordering: PASS
Related Products UI: PASS
Product Detail Navigation: PASS
Mobile: PASS
Knowledge Regression: PASS
Product Regression: PASS
API Error Handling: PASS

Database: UNCHANGED
Migration: NONE
API: MINIMAL READ-ONLY EXTENSION (GET /knowledge/public/entries/:slug/related-products)
Web: MODIFIED
Admin: UNCHANGED
Supplier: UNCHANGED
Knowledge Runtime: MINIMAL READ-ONLY EXTENSION (read path in KnowledgeService, no new engine/service/module)
Search: UNCHANGED
Matching: UNCHANGED
AI: FROZEN

End-to-End: PASS (18/18)
Build: API tsc/nest build + Web tsc/next build all exit 0
Tests: deterministic code-path + schema + route audit (18 scenarios)
Documentation: SYNCHRONIZED
Review Report: docs/_review/595_M24.1.7_Knowledge_Related_Product_Discovery_Report.md

M24.1.7: COMPLETED
Next Task: 596 Related Products / Compare
```

---

## 25. Execution Principle Verification

依据执行原则顺序逐项完成：

`Repository → 594 Verification → Knowledge Detail Audit → Dependency Closure → Existing Product API Audit → Mapping Resolution → Product Resolution → Visibility Filtering → Deduplication → Stable Ordering → Related Products UI → Product Detail Navigation → Mobile → Knowledge Regression → Product Regression → End-to-End → Build → Documentation → Review Report → Roadmap → Close`

最终证明的是确定性的：

```
KnowledgeEntry → KnowledgeCategory → ProductCategoryKnowledgeMapping → ProductCategory → Product → Related Products → Product Detail
```

而非：

```
KnowledgeEntry → AI / Keyword Guess → Random Product Recommendation
```

同时保持 594（Product → Knowledge）与 595（Knowledge → Product）为独立任务、独立验收、同一权威 `ProductCategoryKnowledgeMapping`；595 未重新设计 594 已建立的映射，也未创建反向独立关系模型。