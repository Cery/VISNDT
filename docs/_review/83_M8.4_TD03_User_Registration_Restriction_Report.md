# 83_M8.4_TD03_User_Registration_Restriction_Report

**Date**: 2026-07-20  
**Phase**: M8.4-TD03 User Registration Restriction  
**Status**: ✅ PASSED  
**Type**: Security Technical Debt Resolution

---

## 1. Phase Overview

解决 M8 Final Architecture Audit 中发现的 TD-3 技术债：

> "用户注册开放限制 — 任意用户可创建账号，无组织归属控制"

**Before**: `POST /auth/register` 公开，任何人可注册，无组织绑定。

**After**: Invitation-Based Signup — Admin 创建邀请 → User 携带 token 注册 → 自动绑定 Organization + Role。

---

## 2. Modified Files

| File | Change | Type |
|------|--------|------|
| `database/prisma/schema.prisma` | 新增 `UserInvitation` model + 反向关系 | Modified |
| `database/prisma/migrations/20260720055154_add_user_invitation/migration.sql` | 新建 migration | Created |
| `apps/api/src/auth/dto/create-invitation.dto.ts` | 新建 CreateInvitationDto | Created |
| `apps/api/src/auth/dto/register.dto.ts` | 新增 `inviteToken` 字段 | Modified |
| `apps/api/src/auth/invitation.service.ts` | 新建 InvitationService | Created |
| `apps/api/src/auth/invitation.controller.ts` | 新建 InvitationController (Admin only) | Created |
| `apps/api/src/auth/auth.service.ts` | Register 增加邀请验证 + OrganizationMember 创建 | Modified |
| `apps/api/src/auth/auth.module.ts` | 注册 InvitationController + InvitationService | Modified |
| `apps/api/test/e2e/auth/auth-invitation.e2e-spec.ts` | 新建 E2E 测试 | Created |

---

## 3. Schema Impact

### 3.1 UserInvitation Model

| Field | Type | Nullable | FK | Default |
|-------|------|----------|-----|---------|
| `id` | UUID | — | — | uuid() |
| `email` | String | — | — | — |
| `token` | String (unique) | — | — | — |
| `organizationId` | UUID | — | `organization.id` | — |
| `role` | String | — | — | `MEMBER` |
| `status` | String | — | — | `PENDING` |
| `expiresAt` | DateTime | — | — | — |
| `createdBy` | UUID | — | `user.id` | — |
| `createdAt` | DateTime | — | — | now() |
| `usedAt` | DateTime? | Yes | — | null |

### 3.2 Reverse Relations

- `Organization.invitations UserInvitation[]`
- `User.createdInvitations UserInvitation[]`

---

## 4. Migration Impact

| Property | Value |
|----------|-------|
| **Name** | `add_user_invitation` |
| **Directory** | `20260720055154_add_user_invitation` |
| **Migration Count** | 11 (was 10 prior to M8.4-TD03) |
| **Status** | ✅ Applied |
| **DDL** | `CREATE TABLE user_invitation` + indexes + FKs |

---

## 5. API Impact

### 5.1 New Endpoint

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| POST | `/api/v1/auth/invitations` | JWT | ADMIN | Create user invitation |

### 5.2 Modified Endpoint

| Method | Path | Change |
|--------|------|--------|
| POST | `/api/v1/auth/register` | 新增 `inviteToken` 必填参数；注册后自动创建 OrganizationMember |

### 5.3 Unchanged Endpoints

- `POST /api/v1/auth/login` — No change
- `GET /api/v1/auth/me` — No change

---

## 6. Security Review

| Check | Status | Detail |
|-------|--------|--------|
| Invitation Token | ✅ | `crypto.randomUUID()` — 不可预测 |
| Token Expiry | ✅ | 7 天有效期 |
| Token Status | ✅ | PENDING → USED / EXPIRED |
| Token 一次性 | ✅ | 使用后标记 USED |
| Email 验证 | ✅ | 注册 email 必须与邀请 email 一致 |
| Invitation 创建权限 | ✅ | JWT + ADMIN 角色 |
| Organization 绑定 | ✅ | 注册时自动绑定 organizationId |
| OrganizationMember 创建 | ✅ | 注册时自动创建 membership |
| Password 不存明文 | ✅ | bcrypt hash |
| JWT 流程 | ✅ | 不变 |

---

## 7. Code Refactor

### Before (AuthService.register)

```typescript
async register(dto: RegisterDto) {
  const existing = await this.prisma.user.findUnique({ where: { email: dto.email } });
  // ... create user without organization
  const user = await this.prisma.user.create({
    data: { email: dto.email, passwordHash, name: dto.name },
  });
}
```

### After (AuthService.register)

```typescript
async register(dto: RegisterDto) {
  // 1. Validate invitation token
  if (!dto.inviteToken) throw new UnauthorizedException('Invitation token is required');
  const invitation = await this.invitationService.validateInvitation(dto.inviteToken);
  if (!invitation) throw new UnauthorizedException('Invalid or expired invitation token');
  if (invitation.email !== dto.email) throw new UnauthorizedException('Email does not match invitation');

  // 2. Create user with organization binding
  const user = await this.prisma.user.create({
    data: { email: dto.email, passwordHash, name: dto.name, organizationId: invitation.organizationId },
  });

  // 3. Create OrganizationMember
  await this.prisma.organizationMember.create({
    data: { organizationId: invitation.organizationId, userId: user.id, role: invitation.role },
  });

  // 4. Consume invitation
  await this.invitationService.consumeInvitation(dto.inviteToken);
}
```

---

## 8. Build Verification

```
npx nest build → Exit 0 ✅
```

---

## 9. Test Result

### 9.1 Auth Invitation E2E (New)

| # | Test Case | Expected | Status |
|---|-----------|----------|--------|
| 1 | Register without inviteToken | 401 | ✅ PASS |
| 2 | Register with valid token | 201 | ✅ PASS |
| 3 | AccessToken returned | truthy | ✅ PASS |
| 4 | User has organizationId | orgId | ✅ PASS |
| 5 | OrganizationMember created | exists | ✅ PASS |
| 6 | Register with used token | 401 | ✅ PASS |
| 7 | Register with expired token | 401 | ✅ PASS |
| 8 | Login after registration | 201 | ✅ PASS |
| 9 | Admin creates invitation | 201 | ✅ PASS |
| 10 | Unauthenticated create invitation | 401 | ✅ PASS |

**Auth Invitation**: 10/10 ✅

### 9.2 Regression Tests

| Test Suite | Tests | Status |
|------------|-------|--------|
| match-flow | 10/10 | ✅ PASS |
| category-filter | 3/3 | ✅ PASS |
| rematch | 4/4 | ✅ PASS |

**Regression**: 17/17 ✅

### 9.3 Summary

| Category | Count | Status |
|----------|-------|--------|
| Auth Invitation (new) | 10 | ✅ PASS |
| E2E Regression | 17 | ✅ PASS |
| **Total** | **27** | ✅ |

---

## 10. Freeze Compatibility

| Constraint | Status | Evidence |
|------------|--------|----------|
| Schema Change | ⚠️ Exception | TD resolution — new UserInvitation model |
| API Freeze | ⚠️ Exception | Register endpoint modified (new inviteToken param) |
| Auth Freeze | ⚠️ Exception | TD-3 explicitly targets auth module |
| JWT Payload | ✅ | No change |
| User Schema | ✅ | No change |
| Matching | ✅ | No change |
| Product | ✅ | No change |
| Demand | ✅ | No change |
| RFQ | ✅ | No change |
| Workflow | ✅ | No change |
| Build | ✅ | Exit 0 |
| All Tests | ✅ | 27/27 passing |

---

## 11. Final Status

| Check | Result |
|-------|--------|
| **Build** | ✅ Exit 0 |
| **Schema** | ✅ UserInvitation model added |
| **Migration** | ✅ `20260720055154_add_user_invitation` |
| **Signup Restriction** | ✅ Invitation-based registration |
| **RBAC Protection** | ✅ Admin-only invitation creation |
| **Organization Binding** | ✅ Auto-create OrganizationMember |
| **Auth Invitation Tests** | ✅ 10/10 PASS |
| **Regression Tests** | ✅ 17/17 PASS |
| **TD-3** | ✅ RESOLVED |

---

## 12. Next Step

**M8.4-TD04**: Workflow Event Permission — 限制 Workflow Event 创建权限