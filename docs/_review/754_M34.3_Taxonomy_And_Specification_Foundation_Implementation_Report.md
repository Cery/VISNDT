# 754 · M34.3 Taxonomy & Specification Foundation — Implementation Report

> **Task ID**: `M34.3_Taxonomy_And_Specification_Foundation`
> **Review Report Sequence**: 754
> **Task Status**: **CONDITIONAL PASS**
> **M34.3**: **CONDITIONAL**（Foundation complete + known non-blocking evidence gaps）
> **Execution Mode**: CONTROLLED IMPLEMENTATION + TAXONOMY / SPECIFICATION FOUNDATION + EVIDENCE VALIDATION + DOCUMENTATION SYNCHRONIZATION
> **核心原则执行**: Evidence before Naming · Semantics before UI · Provenance before Display · Reuse before Schema · Foundation before Discovery · Architecture before Search

本任务在 **C2=CONDITIONAL / C5=FOUNDATION · NOT FULL DISCOVERY / C6=FOUNDATION · NOT FULL CAPABILITY-LED SEARCH** 三项继承状态下，将现有 `Category + Parameter Dictionary` 从「技术存在」收敛为可供 M34.4 Discovery/Search 直接消费的 **Taxonomy / Vocabulary / Specification Foundation**，并通过栅栏（Boundary）审计将所有语义边界（Product / SupplyProduct / Demand / Category↔Specification / Facet) 以证据固化。全程零 Schema / 零 Migration / 零 API / 零 Backend / 零业务代码变更。

---

## 1. Task Summary

| 维度 | 内容 |
|---|---|
| 目标 | 建立证据支撑、可追溯、职责清晰的 Taxonomy + Specification + Parameter Foundation，使 Category / Capability(=Product) / Product / SupplyProduct / Supplier / Specification / Demand 不再依赖模糊命名，可作为 M34.4 输入而无需再次重构底层语义 |
| 范围 | 全量 Category inventory → 证据化语义分类 → Category/Product/Parameter 交叉验证 → 参数字典审计（Usage/Ownership/Provenance）→ 数值单位一致性 → Facet 溯源闭环 → 三层 Specification 边界（Product/SupplyProduct/Demand）→ 隐藏域对象审计 → 最小前端语义确认 → Mobile 层级验证 → Static/Runtime → 数据对账 → 文档同步 |
| 变更面 | **零代码变更**（NO Schema·NO Migration·NO API·NO Backend·NO Frontend code）；仅新增只读审计探针脚本 |
| 结果 | **CONDITIONAL PASS**（Foundation 成立；非阻断证据缺口见 §43） |

---

## 2. Repository Verification

| 项 | 值 |
|---|---|
| Repository Root | `F:\Desktop\VISNDT` |
| Code Root | `F:\Desktop\VISNDT\VISNDT`（业务代码集中于 `VISNDT/`） |
| Branch | `main` |
| Commit | `ff03a9a`（M31 阶段优化完成） |
| Working Tree | OTHER（工作树含 M31 既有未提交前端改动；本任务仅新增 4 个只读审计脚本，未改动任何业务代码） |
| M33 | CLOSED / UNCHANGED（未重开） |

---

## 3. Review Report Sequence Verification

历史报告 746–751 / M34.1(749→752) / M34.2(753) **未改写、未重写、未删除**。本报告为连续编号序列的下一份：**754**。

| 报告 | 状态（历史，不改写） |
|---|---|
| 746 Fact Base / 747（M34-DATA-01）/ 748（M34-DATA-02 清理） | 保持 |
| 749 M34 Platform Experience Architecture Audit | 保持 |
| 750 M34 Architecture Decision Consolidation（M34.0） | 保持 |
| 751 Architecture Consistency Challenge（GATE B） | 保持 |
| M34.1 Capability & Product Foundation（752，报告文件名 `M34.1_...`） | 保持 |
| 753 M34.2 Capability / SupplyProduct / Supplier Relationship | 保持 |
| **754 M34.3 Taxonomy & Specification Foundation（本报告）** | **新增** |

---

## 4. Baseline Verification

| 项 | 值 |
|---|---|
| PostgreSQL | `visndt-postgres` Up / healthy；库 `visndt` |
| MinIO | Up（未访问/未变更） |
| API | :4000 `/api/v1/health` = 200（database connected；验证后已停止） |
| Web | :3000 `/categories` 渲染 200（验证后已停止） |
| 业务数据基线 | Product=0 / SupplierProduct=0 / Offer=0 / Demand=3 / RFQ=2 / Inquiry=0 / Organization=15 |
| Taxonomy 基线 | Category=28 / ParameterGroup=11 / ParameterDefinition=54 / ParameterOption=45 |

**关键约束**：`product=0`、`supplier_product=0` 为 748 受控清理后的合法空数据基线，故「Real-data Product Runtime / Real SupplierProduct Runtime = UNVERIFIED」，不伪造成 REAL E2E PASS（§29 / §38）。

---

## 5. M33 Protection

- 未重开 M33；未 reset / checkout / commit；未删除或修改任何受保护/未知/平台规则/审计（audit_log 保留）/基线数据。
- 本任务数据操作仅 **SELECT / COUNT（只读）**，无 TRUNCATE / DELETE / UPDATE / INSERT / DDL。
- M33 = **CLOSED / UNCHANGED**。

---

## 6. Data Baseline Reconciliation

执行前/后通过 `_m343_audit.mjs`（只读）对账，结果 Before=After **完全一致**：

| 表/统计 | Before | After | Mutation |
|---|---|---|---|
| product | 0 | 0 | NONE |
| supplier_product | 0 | 0 | NONE |
| offer | 0 | 0 | NONE |
| demand | 3 | 3 | NONE |
| rfq | 2 | 2 | NONE |
| inquiry | 0 | 0 | NONE |
| organization | 15 | 15 | NONE |
| product_category | 28 | 28 | NONE |
| parameter_group | 11 | 11 | NONE |
| parameter_definition | 54 | 54 | NONE |
| parameter_option | 45 | 45 | NONE |
| product_parameter_definition | 0 | 0 | NONE |
| product_parameter_value | 0 | 0 | NONE |
| supplier_product_parameter_value | 0 | 0 | NONE |
| demand_parameter | 0 | 0 | NONE |
| product_category_knowledge_mapping | 9 | 9 | NONE |
| knowledge_category | 6 | 6 | NONE |

**Database Data Mutation = NONE**。数据栅栏（product / supplier_product / offer / demand / rfq / inquiry + taxonomy counts）一致，PASS。

---

## 7. Inherited C2 / C5 / C6 Conditions

从 M34.2（753）显式继承，**开始写入、结束保留、未升级**：

| 条件 | 定义 | M34.3 状态 |
|---|---|---|
| C2 | Category = Capability 粒度 | **CONDITIONAL（保持）** — 16 个合法分类为能力/检测对象粒度锚定；12 个残留测试分类（TC713/715/716）为数据层污染非能力粒度，未升级为 Taxonomy Complete |
| C5 | Supplier Discovery Boundary | **FOUNDATION / NOT FULL DISCOVERY（保持）** — Published SupplyProduct + Organization 可发现，非全量供应商发现 |
| C6 | Capability-led Search | **FOUNDATION / NOT FULL CAPABILITY-LED SEARCH（保持）** — 仅 Hook 保留（capabilityUrl / categoryPath / 参数 facet 溯源），非全量能力搜索 |

未因「分类标签 / 参数字典 / Facet 映射 / Specification 分组」宣布 Taxonomy Complete / Capability-led Search Complete / Supplier Discovery Complete。

---

## 8. Category Full Inventory

全量 `product_category` 28 项（证据来自 DB inventory，非名称臆断）。分两类：

**A. 合法能力/检测对象粒度分类（16 项）—— evidence-derived 成立**

| 分类 | slug | 层级 | 语义角色 |
|---|---|---|---|
| 探伤仪 | flaw-detector | root | 检测方法族（设备型） |
| 超声波探伤仪 | ultrasonic-flaw-detector | child(探伤仪) | 检测能力/设备 |
| 射线探伤仪 | radiographic-flaw-detector | child(探伤仪) | 检测能力/设备 |
| 涡流探伤仪 | eddy-current-flaw-detector | child(探伤仪) | 检测能力/设备 |
| 磁粉探伤仪 | magnetic-particle-flaw-detector | child(探伤仪) | 检测能力/设备 |
| 工业内窥镜 | industrial-endoscope | root | 检测能力/设备 |
| 光学硬杆内窥镜 | optical-rigid-endoscope | child(工业内窥镜) | 检测能力/设备 |
| 光纤成像内窥镜 | fiber-optic-endoscope | child(工业内窥镜) | 检测能力/设备 |
| 电子视频内窥镜 | electronic-video-endoscope | child(工业内窥镜) | 检测能力/设备 |
| 光学内窥镜 | optical-borescope | root | 检测能力/设备（子类以同一 slug 为根的别名节点） |
| 光纤内窥镜 | fiber-borescope | root | 检测能力/设备 |
| 电子内窥镜 | electronic-borescope | root | 检测能力/设备 |
| 管道镜 | pipe-scope | root | 检测场景设备 |
| 工业显微镜 | industrial-microscope | root | 检测能力/设备 |
| 三维扫描仪 | 3d-scanner | root | 检测能力/设备 |
| 爬行机器人 | crawler-robot | root | 检测载体/设备 |

> 注：`光学内窥镜/光纤内窥镜/电子内窥镜` 与 `工业内窥镜/光学硬杆内窥镜/光纤成像内窥镜/电子视频内窥镜` 存在同名/近义分类冗余（多路径表达同一能力），是 C2=CONDITIONAL 的**证据事实**之一（命名能力粒度部分成立，但存在冗余表达未收敛）。属数据治理项，非 Schema 问题，归 NEXT/FUTURE 工作流类别治理。

**B. 残留测试分类（12 项）—— 数据层污染（非能力粒度）**

| 前缀 | 数量 | 示例 |
|---|---|---|
| TC713 能力类 | 4 | TC713-1787666384949 能力类 … |
| TC715 能力类 | 3 | TC715-1787693089572 能力类 … |
| TC716 能力类 | 5 | TC716-1787699484150 能力类 … |

- 这些为历史 E2E（713/715/716）产生的 `能力类` 测试分类，`slug` 形如 `cat-tc713-*` / `tc716-*`。
- **前端 UI 不暴露**：CDP 响应式探针在 `/products` `/categories` `/search` 均检测 `tc=false`（无 `TC\d{3}` 文本泄漏）；语义污染被限定在数据层，不进入公共渲染。
- **治理归属**：Record / Classify / Assess（§25 原则）→ 属「分类字段清理」候选，需遵循 748 受控清理纪律（Protected Manifest / Delete Manifest / FK 依赖）在**独立清理任务**（NEXT WORKSTREAM）处理，**本任务不删除任何数据**（§30 NO TEST DATA / §31 NONE）。

---

## 9. Category Semantic Matrix

证据驱动（依据 slug / 层级 / 关联参数组 / 关联知识映射 / 是否测试残留，非仅名称）：

| 语义角色 | Category 表现 | 判定 |
|---|---|---|
| 检测方法族（设备族） | 探伤仪 / 工业内窥镜 | Capability-oriented group node |
| 检测能力/设备（leaf） | 超声波/射线/涡流/磁粉探伤仪；光学/光纤/电子内窥镜；工业显微镜；三维扫描仪 | Capability anchor（能力粒度） |
| 检测场景/载体 | 管道镜 / 爬行机器人 | Product-type / Equipment-type anchor |
| 冗余别名 | 光学内窥镜≈光学硬杆内窥镜；光纤内窥镜≈光纤成像内窥镜；电子内窥镜≈电子视频内窥镜 | **冗余表达（C2 CONDITIONAL 证据）** — 同一能力多分类路径 |
| 测试残留 | TC713/TC715/TC716 能力类 | **NON-TAXONOMY（数据污染）** — 非平台权威分类 |

**Category ≠ unrestricted mixed-purpose bucket**：合法 16 项可归入「检测方法 / 检测能力 / 检测场景 / 检测载体」四类语义；非法 12 项为测试残留，需治理。Category capability-oriented anchor **定义清楚（部分/带条件）**：能力粒度成立，但存在冗余别名未收敛。

---

## 10. Category / Product / Parameter Cross-Validation

采用 §15/§21 的**确定性映射**（非 AI/关键词猜测）验证三条链：

1. **Category → ACTIVE Product → ProductParameterValue → ParameterDefinition**：
   `GET /product-categories/:id/parameters`（`product-categories.service.ts:findParameters`）实现 `ProductCategory → ACTIVE Products → ProductParameterValue(distinct parameterDefinitionId) → ParameterDefinition(name asc; ENUM options sortOrder asc)`。纯确定性、去重、可解释。**product=0 时合法返回 `[]`（实测 200 + 空数组），结构正确、无伪造**。
2. **Category → ProductCategoryKnowledgeMapping → KnowledgeCategory → KnowledgeEntry**：
   `product_category_knowledge_mapping=9 / knowledge_category=6`，为确定性知识上下文映射（Product → Category → Knowledge，见 project_memory 约束），未重构。
3. **Category ↔ Parameter 联动**：`ParameterGroup` 已按能力域组织（IE_* / FD_* / 通用参数组），Category 直接消费 `:id/parameters` 端点，形成「分类 → 实际使用参数」闭环。

**结论**：Category / Product / Parameter 交叉验证 PASS；真实数据渲染因 `product=0` 未做（UNVERIFIED），结构级验证成立。

---

## 11. Category Capability-Oriented Governance

- **Owner 模型确认（§35 Low-Operation）**：Admin 维护 Category + Parameter Dictionary；Supplier 维护 SupplyProduct 参数；Buyer 维护 Demand 参数；Automatic = Facet + Match + Audit + Validation。
- **未引入**：人工逐产品能力策展流程 / 人工逐产品 Specification 编写流程 / 人工持续自然语言查询分类流程（零人工增量负担）。
- Category 作为能力导向入口锚，其治理权归 Admin（Platform Rules）；测试残留分类的运行期治理委托独立清理任务，本报告记录证据（Record / Classify / Assess）但不执行 Schema 修改。

---

## 12. Category Mutation Gate

- **DB Category Mutation = NONE**（28 → 28）。
- 未对任意 Category 做 rename / merge / 删除 / 新增。
- 发现 Category「多路径/冗余表达」仅 **Record / Classify / Assess**，不实施 Schema 或数据改写（§25 严格按「先证明 Category→ParameterGroup 持久化刚需，否则 BLOCKED/DEFFERRED」——本轮无需 Schema，故不触碰）。

---

## 13. Hidden Domain Object Audit

对「可被当作新权威对象的隐藏实体」逐项审计：

| 对象 | 是否存在 | 说明 |
|---|---|---|
| Capability Entity（独立表/Record） | **NONE** | Capability=Product 语义角色，无独立表/模型/路由 |
| Specification Entity（独立表/Record） | **NONE** | Specification=参数横切维度，无独立 `/specifications` 路由/表 |
| Specification Template | **NONE** | 无 CategoryParameterTemplate / Spec Template 引入（DEFERRED） |
| CategoryParameterTemplate | **NONE** | 未创建 |
| Secondary Capability / M:N Capability Mapping | **NONE** | 无 join entity / M:N 表（FUTURE） |
| 第二套 Domain Authority | **NONE** | ParameterDefinition 为**唯一** Specification Dictionary authority（§14） |

**Hidden Domain Object = NONE**。未引入第二个「参数/分类权威」影子对象。

---

## 14. Specification Architecture

- Specification = 参数**横切发现维度**，非一级对象、无独立路由。
- Specification 权威 = **ParameterDefinition（唯一 Dictionary Authority）**；值层按归属明确分离（§16/§17）：
  - **ProductParameterValue** = Platform Product Value（平台产品值）
  - **SupplierProductParameterValue** = Supplier-owned Model Value（供应商型号值）
  - **DemandParameter** = Buyer Constraint（买方约束）
- 三层值对象分隔字段/关联，不合并（严格遵循 §22/§23/§24 Boundary）。
- 数值/单位语义审计：见 §19。
- Specification Template / CategoryParameterTemplate = **DEFERRED / FUTURE**（无引入）。

---

## 15. ParameterDefinition Audit

- **参数字典总量**：ParameterDefinition=54 / ParameterGroup=11 / ParameterOption=45。
- **分组家族（evidence）**：
  - IE 家族（工业内窥镜）：`IE_OPTICAL(4)/IE_PERFORMANCE(8)/IE_PHYSICAL(7)`（+`IE_ENVIRONMENT` 并入通用/环境）
  - FD 家族（探伤仪）：`FD_DETECTION(9)/FD_PHYSICAL(8)/FD_ENVIRONMENT(4)`
  - 通用参数组：`OPTICAL_PARAMS(5)/PERFORMANCE_PARAMS(2)/PHYSICAL_PARAMS(4)/ENVIRONMENT_PARAMS(1)`
  - `E2E_TEST_GROUP(1)`（E2E测试参数）+ 1 个未分组 TC715 枚举参数（`E2E测试参数组`）
- **重复定义（名称相同、code 不同）—— 治理发现**：

| 参数名 | codes | 说明 |
|---|---|---|
| 存储容量 | ie_storage / storage_capacity | IE 与 FD/通用两套 |
| 工作温度 | ie_operating_temp / operating_temp | IE ℃ 与通用 °C |
| 数据接口 | data_interface / ie_data_interface | 两套 |
| 整机重量 | ie_weight / weight | 两套 |
| 显示屏尺寸 | ie_display_size / screen_size | 两套（STRING 与 NUMBER 类型不同） |
| 视场角 | field_of_view / fov | 两套 |
| 防护等级 | ie_ip_rating / ip_rating | 两套 |

> 反映参数字典存在 **同义重复**（IE 域与通用/FD 域并存、单位/类型不一），属**治理项/未来标准化任务**，不现场改 Schema（§34 Reuse before Schema；Record/Classify/Assess）。

- **Usage**：`product_parameter_value=0 / supplier_product_parameter_value=0 / demand_parameter=0` → 所有 54 个定义当前 **UNUSED（数据空基线）**，即字典为「平台建立但无运行值」，属合法空状态（Taxonomy/Parameter Infrastructure VERIFIED → Product Runtime UNVERIFIED，§29）。

---

## 16. Parameter Usage Matrix

| Layer | 表 | Count | 状态 |
|---|---|---|---|
| 平台产品值 | product_parameter_value | 0 | UNUSED（product=0） |
| 供应商型号值 | supplier_product_parameter_value | 0 | UNUSED（supplier_product=0） |
| 买方需求值 | demand_parameter | 0 | UNUSED（demand=3 但未绑定参数） |
| ENUM 选项 | parameter_option | 45 | 存在（焦距类型3/照明4/镜头3/图像传感器2/图像处理5/测量4/防护3/探头6/供电3/插入管3/数据接口4/TC715枚举2） |

字典层的 Usage 结构清晰；运行值三表均 0 = 空数据基线限定（非结构缺陷）。

---

## 17. Parameter Ownership Matrix（权限/责任边界的参数归属）

| 对象 | Owner | 参数载体 | 冻结语义 |
|---|---|---|---|
| Product / Capability | Admin / Platform | ProductParameterValue | Platform Product Value（能力权威值） |
| SupplyProduct | Supplier | SupplierProductParameterValue | Supplier-owned Model Specification |
| Demand | Buyer | DemandParameter | Buyer Requirement / Constraint |
| ParameterDefinition | Admin / Platform | —（Dictionary Authority） | 唯一 Specification 字典权威 |

**Ownership 明确，互不越权**：Supplier 不写平台 Product 值；Buyer 以 DemandParameter 表达约束，不与 ProductParameterValue 合并（§24）。

---

## 18. Parameter Provenance

- 每条参数的来源可回溯至：**Object → Field → Relationship → Dictionary**：
  - Category 参数（Facet）→ `GET /product-categories/:id/parameters` → ProductParameterValue → ParameterDefinition（Dictionary Authority）→ 无硬编码筛选项。
  - ENUM 选项 → ParameterOption（挂 ParameterDefinition）。
  - 前端 `parameter-definition.service.ts:getCategoryFilterParameterDefinitions` 直接消费该端点（read-only），无本地伪造默认定义。
- **Provenance = 闭环 PASS**（结构级）。运行态值溯源受空数据基线限制（UNVERIFIED 数据渲染）。

---

## 19. Numeric / Unit Consistency Audit

`data_type='NUMBER'` 定义（16 项）单位审计：

| 参数 | 单位 | 备注 |
|---|---|---|
| 像素 | 万 | OK |
| 变焦倍数 | x | OK |
| 垂直线性误差 / 水平线性误差 | % | OK |
| 存储容量 | GB | OK（但存在 2 个存储容量定义） |
| 工作长度 | m | OK |
| 弯曲半径 / 探头直径 | mm | OK |
| 整机重量 | kg | OK（2 个定义） |
| 显示屏尺寸 | 英寸 | 类型不一致（IE 为 STRING，通用为 NUMBER） |
| 检测通道数 | 个 | OK |
| 灵敏度余量 | dB | OK |
| 电池容量 | mAh | OK |
| 电池续航 / 续航时间 | h | OK（两同名参数同单位，重复） |
| 视场角 | ° | OK（2 个定义） |

**单位混用发现（跨类型）**：
- `工作温度`：`性能参数组(operating_temp)` 单位 **°C** vs `IE性能(ie_operating_temp)` 单位 **℃** —— 同一参数两种温度单位写法；
- `显示屏尺寸`：IE 为 STRING、通用为 NUMBER，类型不一致；
- `防护等级 / 数据接口 / 整机重量 / 存储容量 / 视场角 / 电池续航` 存在同义重复定义。

**结论**：NUMBER 单位语义 **基本一致，需标准化**（°C↔℃、类型统一、同义去重）。已 Record / Classify / Assess，属**未来参数字典标准化任务**（NEXT/FUTURE），不现场 Schema 修改（§40/§50：非阻断，条件记录）。

---

## 20. Range / Match Boundary

- 匹配保持确定性：Demand Parameters → Product Parameters → Score → Match（现有 `DemandMatch.productId`）。本任务**未修改匹配逻辑**。
- 数值范围由前端 `ParameterFilterPanel` 提交 `valueMin/valueMax`（NUMBER），后端沿用既有 search `parameterFilters`；空基线无数据可匹配，仅结构确认。
- **未引入**语义/向量/近似匹配替代（§26 M34.4 职责；本节仅确认边界未跨越）。

---

## 21. Facet Provenance Matrix

闭环要求：`Facet → ParameterDefinition → Actual Value`。

| 链路 | 实现 | 验证 |
|---|---|---|
| 前端 Facet 面板 | `ParameterFilterPanel.tsx` 消费 `FilterParameterDefinition[]`（仅接受独立参数/选项，无硬编码） | PASS（代码证据） |
| 分类上下文参数 | `getCategoryFilterParameterDefinitions(categoryId)` → `GET /product-categories/:id/parameters` | PASS（代码证据 + API 200） |
| 后端解析 | `findParameters`：Category→ACTIVE Products→PPV(distinct)→PD（options.sortOrder） | PASS（确定性、去重、可解释） |
| 实际值层 | ProductParameterValue / SupplierProductParameterValue | **UNVERIFIED（0 数据）** |
| 测试残留泄漏 | CDP：`/products /categories /search` 均 `tc=false` | PASS（无 TC 分类泄漏进 Facet/页面） |

**Facet Provenance 闭环 = 结构级 PASS**；真实值渲染因空数据基线 UNVERIFIED。

---

## 22. Specification Grouping

- 前端规格展示（`ProductParameters.tsx`）基于 **ParameterGroup** 动态分组（IE_*/FD_*/通用），非硬编码分类。
- `parameter_group=11` 提供了确定性分组骨架；哑元/测试组（E2E_TEST_GROUP、TC715枚举）为污染，需治理（Record，不删）。
- 分组与 Category 能力域一致（IE↔内窥镜，FD↔探伤仪），可由 `:id/parameters` 消费。
- 空数据下未渲染任何组（probe `groups=-`），属结构就绪。

---

## 23. Product Specification Boundary（§Body 22 指令）

**必须保持**：`Product → ProductParameterValue` = **Platform-defined Product / Capability Authority**。
- 代码/模型确认：`product_parameter_value` 挂 `product`（平台产品/能力权威）→ ParameterDefinition，值为**平台产品值**。
- **不得**把 `ProductParameterValue` 解释为 Supplier-entered Model Value。验证：值表归属清晰，无字段为供应商写入（Supplier 写 `supplier_product_parameter_value`）。
- **Boundary = PRESERVED（平台产品值）**。

---

## 24. SupplyProduct Specification Boundary（§Body 23 指令）

**必须保持**：`SupplyProduct → SupplierProductParameterValue` = **Supplier-owned Model Specification**，并明确区别于 Platform Product Specification。
- `supplier_product_parameter_value` 挂 `supplier_product`（供应商型号），值为**供应商型号规格**（Supplier-owned）。
- 与 Platform Product Parameter Value **值层分离**（不同表/关联），不合并。
- **Boundary = PRESERVED（供应商型号值 vs 平台产品值明确区分）**。

---

## 25. Demand Specification Boundary（§Body 24 指令）

**必须保持**：`Demand → DemandParameter` = **Buyer Requirement / Constraint**。
- `demand_parameter`（当前 0 条）表达买方需求约束；**未**与 `ProductParameterValue` 合并（不同表/关联）。
- Demand=3 / DemandParameter=0：Buyer 已建 3 条需求但未绑定参数（数据空，合法）。
- **Boundary = PRESERVED（买方约束独立）**。

---

## 26. Category ↔ Specification Boundary（§Body 25 指令）

- **当前保持**：`Category → Product → Parameter`（经 `:id/parameters` 确定性联动），有意**不创建** `CategoryParameterTemplate` / `CategorySpecificationTemplate`（DEFERRED）。
- 若某些 Category 无法正确表达参数：**Record / Classify / Assess**（本任务已执行→命名冗余/测试残留）。
- 仅当证明 `Category → ParameterGroup` 持久化关系是 M34.3 第一轮不可替代刚需时才 BLOCKED 并走 Schema 评估——**本任务证明无需 Schema**，故保持「Category↔Specification 无直接表关系」，CategoryParameterTemplate=NONE（§13）。
- **Boundary = PRESERVED（不创建分类参数模板；当前为 Category→Product→Parameter 确定性联动）**。

---

## 27. Frontend Semantic Integration

按 §Body 27 允许的最小范围复核（**未做任何前端代码变更**，因现有前端已满足语义要求）：

| 允许项 | 现状（已有，未新增） |
|---|---|
| Category semantic labels | `/categories` 页已含「能力分类 · 分类导航」「能力分类索引」语义化标题 + mono 锚点计数 |
| Specification grouping | `ProductParameters.tsx` 基于 ParameterGroup 动态分组 |
| Source/provenance labels | M34.1 capability-context 已声明 `source=PRODUCT` / `isTypical=false` 溯源 |
| Facet terminology | `ParameterFilterPanel` 展示参数名 + 单位（来自 ParameterDefinition，非硬编码） |
| Technical attribute hierarchy | `:id/parameters` 返回去重定义 + 排序 |

**禁止项未触碰**：Global redesign / Product Gallery redesign / Compare redesign / Home redesign / Header redesign **均未执行**。
**禁止以文案替换冒充结构改造**：未用「能力/能力目录/检测能力」等文案包裹伪造结构升级；所有语义均可回溯到 Object / Field / Relationship / Dictionary 之一。
**结论**：Frontend = **无需变更（现状已符合）**；零 Frontend code modification。

---

## 28. Mobile Verification

CDP（Edge headless）对 `/products` `/categories` `/search` `/products?categoryId=...` 375/768/1024/1440 检测水平溢出：

| 尺寸 | 375 | 768 | 1024 | 1440 |
|---|---|---|---|---|
| /products | OK (0px) | **OVERFLOW (140px)** | OK (0px) | OK (0px) |
| /categories | OK (0px) | **OVERFLOW (140px)** | OK (0px) | OK (0px) |
| /search | OK (0px) | **OVERFLOW (140px)** | OK (0px) | OK (0px) |
| /products?categoryId=… | OK (0px) | **OVERFLOW (140px)** | OK (0px) | OK (0px) |
| TC 语义泄漏 | false | false | false | false |

- **发现**：所有路由在 **768（平板）出现一致的 140px 水平溢出**；375/1024/1440 无溢出。
- **归因判定**：M34.3 **零前端变更** → 该溢出为**既有基线问题（PRE-EXISTING）**，非本任务引入；属响应式布局/信息层级问题。
- **归属**：归 **M34.5 Canonical IA + Public/Workspace**（布局/IA 收敛工作流）修复；**不属本任务 schema/runtime 阻断**。
- 信息层级（Hierarchy / Readability / Touch Target / Facet discoverability / Collapse-Expand）：375/1024/1440 无溢出、可读；768 需在 M34.5 收敛。
- **Mobile = CONDITIONAL**（375/1024/1440 PASS；768 溢出为既有、非任务引入，见 §43 Condition）。

---

## 29. API / Backend Impact

- **API Change = NO**：现有 `GET /product-categories/:id/parameters`（`@Get(':id/parameters')`，public, read-only）**已能返回** Category 相关 ParameterDefinition（含 ENUM options）→ **KEEP CURRENT API**（§Body 32）。
- 无 Taxonomy CRUD redesign / Specification CRUD / Intent API / Search V2。
- **Backend Domain Logic = NO CHANGE**：未改 Matching / Search ranking / RFQ / Offer / Demand workflow / Auth-RBAC（§Body 33）。仅做 read-only projection（现 `findParameters` 已是只读投影，未改）。

---

## 30. Schema / Migration Impact

- **Schema = NO CHANGE**；**Migration = NONE**。
- 未引入 Capability / Specification / SpecificationTemplate / CategoryParameterTemplate / SecondaryCapability / CapabilityMapping 任一新实体（§Body 34）。
- **Schema Necessity Evidence**：本任务所有目标（Inventory / 语义分类 / 参数审计 / Facet 溯源 / 边界固化）均可在现有 `product_category + parameter_*` 结构上以证据完成；**无需 Schema**。
- Why Current Model Is Insufficient：非不足，而是需治理（命名冗余 / 单位混用 / 测试残留）——可经数据治理而非 DDL 解决。
- Minimal Future Change / Why Deferred：参数字典标准化（同义去重、单位统一）与测试分类清理归 **FUTURE 工作流**，非本轮必需，故 Deferred。
- **Schema / Migration Gate = PASS（无需 BLOCKED）**。

---

## 31. Test Fixture Accounting

- **默认 NO TEST DATA CREATION（§Body 30）**。
- 本任务**未创建任何 fixture**（无 Product / Category / ParameterDefinition / SupplyProduct / ParameterValue 写入）。
- 未进行动态验证的数据制备；探针仅访问只读 / 合法 not-found 路径。
- 无 POST / PUT / PATCH / DELETE。
- **Fixture 台账**：Before=0 创建 · 0 创建 · 0 删除 · After=0 · **Residual=0**（未引入任何测试数据残留）。

---

## 32. Runtime Evidence Classification

按 §Body 39 分层（**不合并**为一个 Runtime PASS）：

| Evidence Layer | 状态 | 依据 |
|---|---|---|
| Infrastructure Runtime | **VERIFIED** | postgres healthy；API :4000 health 200；web :3000 渲染 200 |
| Code Path Runtime | **VERIFIED** | `findParameters` 确定性 CNS；build exit 0；`/product-categories/:id/parameters` 200（空数组结构正确） |
| Taxonomy Runtime | **VERIFIED** | 28 categories / 11 groups / 54 defs / 45 opts 全量 inventory 成功 |
| Parameter Runtime | **VERIFIED** | usage / ownership / provenance / unit 矩阵基于真实 DB 查询建立 |
| Real Product Data Runtime | **UNVERIFIED** | product=0（空基线；447 无伪造） |
| Real Supplier Product Runtime | **UNVERIFIED** | supplier_product=0（空基线；无伪造） |
| Browser Runtime | **CONDITIONAL** | 375/1024/1440 无溢出；768 溢出 140px（既有、非任务引入） |

---

## 33. Static Verification

使用实际包命令（§Body 37，非仅 build）：

| 命令 | 结果 |
|---|---|
| `pnpm --filter @visndt/web lint` | exit 0（仅存量 warnings：error.tsx `_error` unused / no-img-element / exhaustive-deps / ProductGrid `_pageSize`；**零 error，零新增新文件 warning**） |
| `pnpm --filter @visndt/web build` | exit 0（含 TypeScript 全量路由产物完整；`/products` `/categories` `/search` `/products/compare` 等全部编译） |

**Static = PASS**（TypeScript + Lint + Build 均通过）。

---

## 34. Functional Acceptance Criteria

| AC | Criterion | Required Result | M34.3 |
|---|---|---|---|
| AC-01 | 全部 Category 完成 inventory | PASS | **PASS**（28 项全量，16 合法 + 12 测试残留） |
| AC-02 | Category semantic role 有证据 | PASS/CONDITIONAL | **CONDITIONAL**（能力粒度成立，含冗余别名/测试残留证据） |
| AC-03 | Category/Product/Parameter 交叉验证 | PASS | **PASS**（确定性映射 + 结构验证） |
| AC-04 | Category capability-oriented anchor 定义清楚 | PASS/CONDITIONAL | **CONDITIONAL**（锚定成立，冗余未收敛） |
| AC-05 | ParameterDefinition 唯一 Spec Dictionary authority | PASS | **PASS** |
| AC-06 | Product/SupplyProduct/Demand 参数边界明确 | PASS | **PASS**（§23/§24/§25 值层分离） |
| AC-07 | 参数来源可追溯 | PASS | **PASS**（Object→Field→Relationship→Dictionary） |
| AC-08 | NUMBER/unit 语义完成检查 | PASS/CONDITIONAL | **CONDITIONAL**（°C↔℃、类型、同义需标准） |
| AC-09 | Facet provenance 闭环 | PASS | **PASS（结构级）** |
| AC-10 | Typical 未伪造 | PASS | **PASS**（isTypical=false / 不造 Typical） |
| AC-11 | 无 Capability/Specification/Template Entity | PASS | **PASS** |
| AC-12 | 无隐藏 Domain Authority | PASS | **PASS** |
| AC-13 | M34.4 所需 Vocabulary/Facet foundation 可消费 | PASS | **PASS**（Category/Parameter/Facet 通过既有 API 可消费） |
| AC-14 | 375/768/1024/1440 信息结构可用 | PASS/CONDITIONAL | **CONDITIONAL**（375/1024/1440 可用；768 溢出为既有） |
| AC-15 | 无测试数据残留 | PASS | **PASS**（Residual=0；12 个 TC 分类为 748 清理后遗留存量，非本任务产生，本任务未新增） |
| AC-16 | Before/After Data Reconciliation 一致 | PASS | **PASS**（Mutation=NONE） |
| AC-17 | C2/C5/C6 正确继承 | PASS | **PASS**（C2=CONDITIONAL / C5·C6=FOUNDATION） |

**Functional Acceptance = CONDITIONAL PASS**。

---

## 35. Impact Verification

| Area | Before | After | Mutation | Evidence |
|---|---|---|---|---|
| Category | 28（16 合法+12 测试残留） | 28 | NONE | DB count |
| Taxonomy | 28 nodes / 11 groups | 28 / 11 | NONE | DB count |
| Capability Role | Product semantic role | Product semantic role | NONE | 架构不变 |
| ParameterDefinition | 54 | 54 | NONE | DB count |
| ParameterOption | 45 | 45 | NONE | DB count |
| ProductParameterValue | 0 | 0 | NONE | DB count |
| SupplierProductParameterValue | 0 | 0 | NONE | DB count |
| DemandParameter | 0 | 0 | NONE | DB count |
| Facet | `:id/parameters` 闭环 | 闭环（读） | NONE | 代码/API / 结构验证 |
| Product Page | /products、/products/[slug] | 不变 | NONE | 渲染 200（空态） |
| Category Page | /categories 200 | 不变 | NONE | 渲染 200 |
| Mobile | 375/1024/1440 OK·768 溢出 140px | 不变 | NONE | CDP |
| Search Hook | Foundation 保留 | Foundation 保留 | NONE | capabilityUrl/categoryPath |
| Database | 全表计数稳定 | 全表计数稳定 | **NONE** | Before=After |

**采用**：Area 级 Before/After/Mutation/Evidence 全表已输出，Mutation 全域 NONE。

---

## 36. Architecture Consistency Verification（§Body 40）

| 对象 | 角色 | 确认 |
|---|---|---|
| Category | Taxonomy Node | **PASS**（合法 16 项为分类法节点；12 测试残留为污染非权威节点） |
| Capability | Product Semantic Role | **PASS**（非独立实体） |
| Specification | Parameter Dimension | **PASS**（横切维度，无独立路由/表） |
| ParameterDefinition | Dictionary Authority（唯一） | **PASS** |
| ProductParameterValue | Platform Product Value | **PASS** |
| SupplierProductParameterValue | Supplier Model Value | **PASS** |
| DemandParameter | Buyer Constraint | **PASS** |
| C2 | Conditional（保持） | **CONDITIONAL**（未升级；冗余别名/测试残留阻止降为 PASS） |
| C5 | Foundation / Not Full Discovery | **FOUNDATION** |
| C6 | Foundation / Not Full Capability-led Search | **FOUNDATION** |

---

## 37. C2 / C5 / C6 Final State（§Body 44）

本任务为 Vocabulary / Taxonomy / Specification **Foundation**（非完整能力）。最终保持：

> **C2 = CONDITIONAL**（Category 能力粒度部分成立，冗余/测试残留为证据；非 Taxonomy Complete）
> **C5 = FOUNDATION · NOT FULL DISCOVERY**
> **C6 = FOUNDATION · NOT FULL CAPABILITY-LED SEARCH**

**特别禁止项未发生**：未宣告 `C2 PASS = Taxonomy Complete`、`C5 PASS = Full Supplier Discovery`、`C6 PASS = Full Capability-led Search`。M34.3 只定义「平台知道这些对象和参数是什么」；M34.4 才实现「用户如何发现」。

---

## 38. Expansion Gate（§Body 45）

以下**均未现场实现**，一律 DEFERRED / FUTURE / NEXT WORKSTREAM：
Capability Entity / Secondary Capability / M:N Capability / Specification Template / CategoryParameterTemplate / Supplier Directory / Full Supplier Discovery / Search Intent Engine / Natural Language Search / Semantic Search / AI / RAG / Vector / Marketplace / Monetization。

**Expansion Gate = CLOSED**。

---

## 39. Roadmap Progress Alignment（§Body 48）

**开始**：
```
M34.0 Architecture Preparation      ████████████████████  COMPLETE
M34.1 Capability & Product Foundation ████████████████████  COMPLETE / CONDITIONAL
M34.2 Capability/SupplyProduct/Supplier ████████████████████  COMPLETE / CONDITIONAL
M34.3 Taxonomy & Specification Foundation ████████████████████  IN PROGRESS
M34.4 Discovery / Search            ░░░░░░░░░░░░░░░░░░░░  NOT STARTED
M34.5 Canonical IA + Public/Workspace ░░░░░░░░░░░░░░░░░░░░  NOT STARTED
M34.6 Buyer / Supplier Workflow     ░░░░░░░░░░░░░░░░░░░░  NOT STARTED
M34.7 Governance + SEO/LLM + Mobile ░░░░░░░░░░░░░░░░░░░░  NOT STARTED
```
**结束**：
```
M34.3 Taxonomy & Specification Foundation ████████████████████  CONDITIONAL
M34.4 Discovery / Search            ░░░░░░░░░░░░░░░░░░░░  NOT STARTED
```
**Current Position**: M34.3 · **Current Workstream**: Taxonomy & Specification Foundation
**M34.0**: COMPLETE · **M34.1**: CONDITIONAL · **M34.2**: CONDITIONAL · **M34.3**: CONDITIONAL
**M34 First-Round Implementation**: 3 / 7 Workstreams COMPLETE
> 声明：M34 Progress **≠** 3/7 机械百分比；总体进度应综合 Scope Completion + Implementation State + Architecture Readiness + Remaining Critical Gates，不机械写 42.86%。

---

## 40. Documentation Synchronization（§Body 46/47）

| 文档 | 动作 |
|---|---|
| `PROJECT_STATUS.md` | **M34.3 状态条目新增（CONDITIONAL 保持）** |
| `PROJECT_ROADMAP.md` | **M34.3 工作流行新增（CONDITIONAL）** |
| `MODULE_COMPLETION_MATRIX.md` | **M34.3 完成度行新增（CONDITIONAL PASS）** |
| `docs/_architecture/M34_Platform_Architecture_Contract.md` | **追加 §14 M34.3 Implementation State（CONDITIONAL）** |
| 历史报告 746/747/748/749/750/751/M34.1/M34.2 | **未改写** |

**DoD 表述规则**：M34.3 = **CONDITIONAL**（本任务核心结论）。禁止把本工作流写成 `M34.4 COMPLETE / C6 COMPLETE / Platform Discovery COMPLETE / M34 COMPLETE`；不用「已形成基础」替代「完整能力已实现」。

---

## 41. Mandatory Implementation Accounting（§Body 49）

```
Code Files Modified:            NONE
Frontend Files Modified:        NONE
API Files Modified:             NONE
Backend Files Modified:         NONE
Schema Files Modified:          NONE
Configuration Files Modified:   NONE
Verification Scripts Added:     _m343_audit.mjs, _m343_params.mjs, _m343_detail.mjs, _m343_resp_probe.mjs（只读审计探针，位于 VISNDT/database/）
Schema Changes:                 NONE
Migration:                      NONE
Database Data Mutation:         NONE
Storage Mutation:               NONE
Routes Added:                   NONE
Routes Removed:                 NONE
Routes Redirected:              NONE
Capability Entity:              NONE
Specification Entity:           NONE
Specification Template:         NONE
CategoryParameterTemplate:      NONE
M:N Capability Mapping:         NONE
Hidden Domain Authority:        NONE
UI Scope:                       [exact] 零前端改动 — 现有前端已满足 Category semantic labels / Specification grouping / Provenance labels / Facet terminology；未做 redesign，未用文案替换冒充结构升级
Out of Scope Compliance:        PASS
```

---

## 42. Remaining Deferred Items

| 项 | 归属 | 状态 |
|---|---|---|
| 12 个 TC713/TC715/TC716 测试残留分类清理 | 独立受控清理（遵循 748 纪律） | NEXT / FUTURE |
| 施语义冗余别名收敛（光学/光纤/电子内窥镜多路径） | Category 治理 | NEXT / FUTURE |
| 参数字典标准化（°C↔℃、显示屏尺寸类型统一、7 组同义去重） | 参数字典标准化任务 | NEXT / FUTURE |
| 768 平板水平溢出（140px） | M34.5 Canonical IA + Mobile | NEXT WORKSTREAM |
| Real-data Product/SupplierProduct Runtime | 数据就绪后复验 | FUTURE（502->504 证据缺口） |
| CategoryParameterTemplate / Spec Template | 架构评估 | DEFERRED / FUTURE |
| M34.4 Search Intent / Semantic / AI 相关 | M34.4 | NOT STARTED |

---

## 43. Final Decision

**Task Status: CONDITIONAL PASS**

**M34.3 = CONDITIONAL**（Foundation complete + 已知非阻断证据缺口）。

**成立说明**：
- Category 全量 inventory 完成（§8）；语义角色有证据（§9）；交叉验证完成（§10）；specification authority 唯一（§14/§15）；参数边界完整（§23/§24/§25）；Facet provenance 闭环（§21）；数值/单位语义已审计（§19）；无隐藏域对象（§13）；无 Schema / 无 migration（§30）；static PASS（§33）；runtime evidence 分层充分（§32）；数据对账一致（§6）；C2/C5/C6 正确保持（§37）；无测试残留（§31）。

**Condition（非阻断）**：
- **Condition-1 Real-data Runtime UNVERIFIED**：`product=0` / `supplier_product=0` 空基线导致 Real Product / Real SupplierProduct Runtime = UNVERIFIED（非伪造）。
- **Condition-2 768 平板溢出（既有）**：140px 水平溢出存在于 375/1024/1440 之外，为**既有基线问题**，M34.3 零前端变更故非任务引入，归 M34.5 收敛。
- **Condition-3 语义治理待办**：12 个测试残留分类 + 7 组参数字典同义重复 + °C/℃ 单位统一，属数据治理/未来标准化，非 Schema 刚需，已 Record/Classify/Assess。

**BLOCKED 判定**：无（Category 语义无需 Schema 即可稳定；无规范权歧义；Facet provenance 可证（结构级）；单位语义无需未经批准域变更；无关键 static/runtime 失败；无需改写受保护数据；无报告序号冲突）。

**Owner（Condition 分担）**：
- Real-data Runtime → 数据就绪后的复验任务（FUTURE）
- 768 Mobile + Category 冗余收敛 → M34.5（布局/IA 工作流）
- 参数字典标准化 + 测试分类清理 → 独立数据治理 / 字典标准化任务（NEXT/FUTURE）

**Next Workstream**: M34.4 Discovery / Search（**Next Workstream Authorized: NO**）
**Final Action: STOP**