# 334 M14.2.4.5 WorkspaceRole Contract Minimal Fix Report

## 0. 文档勘误说明

1. 本报告是历史执行过程中形成的“合并记录”，同时覆盖了 `M14.2.4.5A` 与 `M14.2.4.5B` 两个子任务。
2. 按当前已明确的单任务原则，“一个任务窗口只能执行一个编号任务”，“一个任务一个编号一个报告”，因此本报告**不应再作为 `5A/5B` 独立放行依据**。
3. 后续涉及 `5A` 的审核结论，以 [`336_M14.2.4.5A_Result_Audit_Report.md`](file:///F:/Desktop/VISNDT/docs/_review/336_M14.2.4.5A_Result_Audit_Report.md) 为准；涉及冻结符合性复核，以 [`335_M14.2.4.5C_RBAC_Workspace_Contract_Freeze_Review.md`](file:///F:/Desktop/VISNDT/docs/_review/335_M14.2.4.5C_RBAC_Workspace_Contract_Freeze_Review.md) 为准。
4. 本次仅补充历史定位说明，不重写原始执行记录内容。

## 1. Review Summary

- 任务编号：`M14.2.4.5`
- 任务范围：
  - `M14.2.4.5A Backend Minimal RBAC Contract Fix`
  - `M14.2.4.5B Frontend Workspace Contract Alignment`
- Git 仓库根目录：`F:\Desktop\VISNDT`
- 业务代码目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 前置冻结规范：[`333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md`](file:///F:/Desktop/VISNDT/docs/_review/333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md)
- 执行方式：在冻结边界内做最小代码修复，不改数据库、不引入新角色体系

本次目标是把运行时代码与 `4.4C` 冻结契约对齐，核心冻结口径如下：

1. `Organization.type = 组织业务身份`
2. `OrganizationMember.role = 组织权限`
3. `workspaceRole = SUPPLIER | BUYER | null`
4. `ADMIN = Permission only`
5. `UNKNOWN / NULL -> null`，不得再静默回退为 `BUYER`

## 2. Freeze Boundary Check

本次执行前已核对：

- Git 根目录：`F:\Desktop\VISNDT`
- 代码根目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 冻结范围：仅允许修改 AuthService/DTO/Interface、前端 auth.service/RoleGuard/WorkspaceSidebar 等冻结规范明确许可文件

本次未修改：

1. `database/prisma/schema.prisma`
2. `database/migrations/**`
3. 权限系统结构
4. Supplier Store / 公开价格 / 交易支付相关模块

## 3. Implemented Changes

### 3.1 Backend

1. [`auth.service.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/auth.service.ts)
   - 将 `workspaceRole` 类型收敛为 `SUPPLIER | BUYER | null`
   - 移除 `OrganizationMember.role === ADMIN -> workspaceRole = ADMIN` 的覆盖逻辑
   - 按冻结矩阵实现映射：
     - `SUPPLIER / MANUFACTURER / DISTRIBUTOR -> SUPPLIER`
     - `BUYER -> BUYER`
     - 其他值或空值 -> `null`
   - `login/register/refresh/me` 统一复用 `buildAuthUser` 返回同一份 Auth 用户上下文

2. [`auth-response.dto.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts)
   - 将 `workspaceRole` Swagger 枚举收敛为 `SUPPLIER | BUYER | null`
   - 删除与当前运行时不一致的 `accessToken` body 字段声明

3. [`auth-request.interface.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts)
   - 将请求上下文中的 `workspaceRole` 类型与冻结契约保持一致

### 3.2 Frontend

1. [`auth.service.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/web/src/services/auth.service.ts)
   - 将前端 `WorkspaceRole` 类型收敛为 `SUPPLIER | BUYER | null`

2. [`RoleGuard.tsx`](file:///F:/Desktop/VISNDT/VISNDT/apps/web/src/auth/RoleGuard.tsx)
   - 去掉 `workspaceRole ?? 'BUYER'` 的静默兜底
   - 当 `workspaceRole = null` 时直接拒绝访问

3. [`WorkspaceSidebar.tsx`](file:///F:/Desktop/VISNDT/VISNDT/apps/web/src/components/workspace/WorkspaceSidebar.tsx)
   - 去掉基于 `ADMIN` 的独立工作区导航分支
   - 去掉 `workspaceRole ?? 'BUYER'` 的默认导航兜底
   - `workspaceRole = null` 时不再渲染 Buyer 导航，改为显示“未映射到可用工作区”

## 4. Contract Result

本次修复后，Auth 运行时契约收敛为：

- `organization.type`：保留原始组织业务身份
- `organizationMember.role`：保留原始组织权限
- `workspaceRole`：仅输出 `SUPPLIER | BUYER | null`

因此：

1. Supplier 管理员仍可保留 `organizationMember.role = ADMIN`
2. 其业务工作区人格由 `organization.type` 决定，不再错误变成 `workspaceRole = ADMIN`
3. `MANUFACTURER` 不再被折叠为 `BUYER`
4. `UNKNOWN / NULL` 不再被前端默默视为 `BUYER`

## 5. Build Verification

在业务代码目录 `F:\Desktop\VISNDT\VISNDT` 下执行：

1. `pnpm --filter @visndt/api build`
   - 结果：通过

2. `pnpm --filter @visndt/web build`
   - 结果：通过
   - 保留 warning（本次未处理，且与本任务无直接关系）：
     - `src/app/layout.tsx` 的 `@next/next/no-page-custom-font`
     - `src/components/products/ManufacturerInfo.tsx` 的 `@typescript-eslint/no-unused-vars`

## 6. Residual Notes

1. Supplier 路由文件本次未单独改动，因为它们已经统一通过 `RoleGuard roles={['SUPPLIER']}` 消费工作区人格；在 `RoleGuard` 修正后即可自动继承冻结行为。
2. 当前 `AuthController` 的 Swagger 装饰器仍使用未包装的 DTO 类型描述响应；本次按冻结范围未修改控制器，仅先完成运行时与 DTO/前端类型的一致性收口。

## 7. Execution Result

1. 修改文件
   - `F:\Desktop\VISNDT\VISNDT\apps\api\src\auth\auth.service.ts`
   - `F:\Desktop\VISNDT\VISNDT\apps\api\src\auth\dto\auth-response.dto.ts`
   - `F:\Desktop\VISNDT\VISNDT\apps\api\src\auth\interfaces\auth-request.interface.ts`
   - `F:\Desktop\VISNDT\VISNDT\apps\web\src\services\auth.service.ts`
   - `F:\Desktop\VISNDT\VISNDT\apps\web\src\auth\RoleGuard.tsx`
   - `F:\Desktop\VISNDT\VISNDT\apps\web\src\components\workspace\WorkspaceSidebar.tsx`
   - `F:\Desktop\VISNDT\docs\_review\334_M14.2.4.5_WorkspaceRole_Contract_Minimal_Fix_Report.md`

2. 修改原因
   - 将后端 Auth 派生逻辑、DTO、请求上下文以及前端工作区消费逻辑收敛到 `4.4C` 冻结契约，消除 `ADMIN` 混入 `workspaceRole` 与 `null -> BUYER` 静默兜底问题

3. 架构影响
   - 无新增模块、无新增角色体系、无权限架构扩展；仅修正既有 Auth 契约与工作区导航消费逻辑

4. 数据库影响
   - 无；未修改 Prisma Schema，未新增字段，未执行 migration

5. API 影响
   - Auth 用户契约中的 `workspaceRole` 收敛为 `SUPPLIER | BUYER | null`
   - `MANUFACTURER / DISTRIBUTOR` 统一映射为 `SUPPLIER`
   - `UNKNOWN / NULL` 返回 `null`

6. Build 结果
   - `@visndt/api build`：通过
   - `@visndt/web build`：通过
   - 存在 2 条既有 warning，未阻断构建

7. Review 报告路径
   - [`334_M14.2.4.5_WorkspaceRole_Contract_Minimal_Fix_Report.md`](file:///F:/Desktop/VISNDT/docs/_review/334_M14.2.4.5_WorkspaceRole_Contract_Minimal_Fix_Report.md)
