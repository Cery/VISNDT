# 359_M14.2.5_RFQ_Detail_Response_Integrity_Fix_Report

## 1. 执行摘要

- 项目路径：
  - Git 根目录：`F:\Desktop\VISNDT`
  - 业务目录：`F:\Desktop\VISNDT\VISNDT`
- 分支：`main`
- 执行前 git status：`git status --short` 无输出，工作区干净
- 修复目标：将 `/workspace/rfqs/[id]` 页面“响应详情”从错误的 `GET /rfq-responses/mine` + 前端 `rfqId` 过滤，切换为正确的 `GET /rfqs/:id/responses`
- 最终结果：已完成前端数据链路修复；未新增 API，未修改后端、数据库或权限体系；`pnpm --filter @visndt/web build` 通过，`/workspace/rfqs/[id]` 仍进入构建产物

## 2. 修改文件

- `VISNDT/apps/web/src/lib/api/rfqs.ts`
  - 新增最小 API client 方法 `getRfqResponsesById(rfqId, page, pageSize)`，封装已有 `GET /rfqs/:id/responses`
- `VISNDT/apps/web/src/services/rfq.service.ts`
  - 将 `getRfqResponses()` 调整为按指定 `rfqId` 调用 RFQ 维度响应接口，不再别名到 `getMyRfqResponses()`
- `VISNDT/apps/web/src/app/workspace/rfqs/[id]/page.tsx`
  - 将详情页响应加载改为直接请求当前 RFQ 的响应列表，并删除前端按 `rfqId` 二次过滤

## 3. 数据链路修复说明

修复前：

`RFQ Detail`
`↓`
`rfq.service`
`↓`
`GET /rfq-responses/mine`
`↓`
`前端 filter rfqId`

修复后：

`RFQ Detail`
`↓`
`rfq.service`
`↓`
`GET /rfqs/:id/responses`

说明：

- 数据语义由 `organization scope` 修正为 `RFQ scope`
- 页面现在直接获取“当前 RFQ 的全部响应”，不再依赖“当前组织的响应集合”后置过滤
- 保持原有 loading、error、empty state 与列表展示逻辑，不改变 UI

## 4. API影响

- 无新增 API
- 仅切换已有 API 消费路径
- 后端仍复用已存在的 `GET /rfqs/:id/responses`

## 5. Database影响

- 无

## 6. Build结果

- 执行命令：`pnpm --filter @visndt/web build`
- 结果：构建通过
- 验证点：
  - 无 TypeScript 错误
  - `/workspace/rfqs/[id]` 仍出现在 Next build 路由输出中
- 现存 warning：
  - `src/app/layout.tsx` 的 `@next/next/no-page-custom-font`
  - `src/components/products/ManufacturerInfo.tsx` 的 `@typescript-eslint/no-unused-vars`

## 7. 冻结符合性检查

- 未修改：`apps/api/**`
- 未修改：`database/**`
- 未修改：`prisma/**`
- 未修改：`schema.prisma`
- 未修改权限体系相关冻结模块：
  - `AuthGuard`
  - `RoleGuard`
  - `AuthProvider`
  - `AuthUser`
  - `WorkspaceSidebar`

## 8. 未完成事项

- `dashboard namespace` 未处理
- `workspace search` 未处理
- `warning` 未处理
