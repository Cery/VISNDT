# 361_M14.2.5_Workspace_Dashboard_Namespace_Migration_Report

## 1. 执行摘要

- Git 根目录：`F:\Desktop\VISNDT`
- 业务代码目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 执行前 git status：
  - `M VISNDT/apps/web/src/app/workspace/rfqs/[id]/page.tsx`
  - `M VISNDT/apps/web/src/lib/api/rfqs.ts`
  - `M VISNDT/apps/web/src/services/rfq.service.ts`
  - `M docs/_review/M_Series_Development_Control_Plan.md`
  - `?? docs/_review/359_M14.2.5_RFQ_Detail_Response_Integrity_Fix_Report.md`
  - `?? docs/_review/360_M14.2.5_Workspace_Dashboard_Namespace_Decision_Audit_Report.md`
  - `?? docs/_review/361_M_Series_Control_Plan_Temporary_Task_Insert_Report.md`
- 任务目标：将 Buyer Dashboard 从 `/dashboard` 正式迁移到 `/workspace/dashboard`，并保留旧 `/dashboard` 作为兼容跳转入口
- 最终结果：已完成正式路由迁移、旧路由兼容跳转、Sidebar/PublicHeader/Login/Register/constants 同步迁移；未修改 API、数据库或冻结认证契约；`pnpm --filter @visndt/web build` 通过

## 2. Route Migration

- 旧：`/dashboard`
- 新：`/workspace/dashboard`
- 正式页面位置：`VISNDT/apps/web/src/app/workspace/dashboard/page.tsx`
- 旧路由兼容方式：`VISNDT/apps/web/src/app/dashboard/page.tsx` 使用 `redirect('/workspace/dashboard')`
- 是否保留旧入口：是，保留为兼容入口，不再承载 Dashboard 业务实现

## 3. 修改文件

- `VISNDT/apps/web/src/app/workspace/dashboard/page.tsx`
  - 修改目的：创建正式 Buyer Workspace Dashboard 页面
  - 关键修改点：迁移原 `/dashboard` 的 UI、数据加载与快捷入口；新增 `AuthGuard + RoleGuard roles={['BUYER']}`
- `VISNDT/apps/web/src/app/dashboard/page.tsx`
  - 修改目的：保留旧路由兼容能力
  - 关键修改点：删除原业务实现，改为最小 `redirect('/workspace/dashboard')`
- `VISNDT/apps/web/src/components/workspace/WorkspaceSidebar.tsx`
  - 修改目的：同步 Buyer Sidebar Dashboard 入口
  - 关键修改点：BUYER 的 `仪表盘` href 从 `/dashboard` 改为 `/workspace/dashboard`
- `VISNDT/apps/web/src/components/layout/PublicHeader.tsx`
  - 修改目的：同步 Header 中用户菜单入口
  - 关键修改点：Desktop 与 Mobile 的 `工作台` 链接统一改为 `/workspace/dashboard`
- `VISNDT/apps/web/src/app/login/page.tsx`
  - 修改目的：同步登录成功后的正式跳转目标
  - 关键修改点：`router.replace('/dashboard')` 改为 `router.replace('/workspace/dashboard')`
- `VISNDT/apps/web/src/app/register/page.tsx`
  - 修改目的：同步注册成功后的正式跳转目标
  - 关键修改点：`router.replace('/dashboard')` 改为 `router.replace('/workspace/dashboard')`
- `VISNDT/apps/web/src/lib/constants.ts`
  - 修改目的：同步正式 Dashboard 常量
  - 关键修改点：`DASHBOARD` 从 `/dashboard` 改为 `/workspace/dashboard`

## 4. Sidebar Mapping

- BUYER：
  - `/workspace/dashboard`
  - `/workspace/demands`
  - `/workspace/rfqs`
  - `/workspace/matches`
  - `/workspace/notifications`
  - `/workspace/settings`
- SUPPLIER：
  - 已确认未被本任务改变

## 5. Access Control

- `/workspace/dashboard`
  - `AuthGuard = Yes`
  - `RoleGuard = BUYER`
- 结果说明：
  - `BUYER` 允许访问
  - `SUPPLIER` 拒绝访问
  - `workspaceRole = null` 不放行
- `/workspace/page.tsx`
  - 保持原有 `AuthGuard + RoleGuard roles={['BUYER', 'SUPPLIER']}` 不变

## 6. Redirect Compatibility

- `/dashboard`
  - `-> /workspace/dashboard`
- 兼容说明：
  - 旧链接与书签仍可继续使用
  - 旧页面不再重复加载 Dashboard UI 或数据逻辑

## 7. Dependency Update

- Sidebar：已完成迁移
- PublicHeader：已完成迁移
- Login：已完成迁移
- Register：已完成迁移
- constants：已完成迁移

补充检查：

- 已执行全局搜索，业务代码中未再发现主动导航到旧 `/dashboard` 的引用
- 当前仅保留兼容跳转页本身使用 `redirect('/workspace/dashboard')`

## 8. API Impact

- 无 API 变化
- 未新增 API
- 未删除 API
- 未修改 service / DTO / 后端实现

## 9. Database Impact

- 无数据库变化

## 10. Build Result

- 执行命令：`pnpm --filter @visndt/web build`
- 结果：`PASS`
- 构建输出确认：
  - `/dashboard` 仍存在于构建输出，作为兼容路由保留
  - `/workspace/dashboard` 已进入构建输出
  - `/workspace/demands` 保持存在
  - `/workspace/rfqs` 保持存在
  - `/workspace/matches` 保持存在
  - `/workspace/notifications` 保持存在
  - `/workspace/settings` 保持存在

## 11. Freeze Compliance

- `apps/api` 未修改
- `database` 未修改
- `prisma` 未修改
- `schema.prisma` 未修改
- `AuthProvider` 未修改
- `AuthGuard` 未修改
- `RoleGuard` 未修改
- `AuthUser` 未修改
- `Supplier Workspace` 未修改

## 12. Existing Warnings

- `src/app/layout.tsx` 的 `@next/next/no-page-custom-font`
- `src/components/products/ManufacturerInfo.tsx` 的 `@typescript-eslint/no-unused-vars`

说明：上述 warning 为既有问题，本次未处理。

## 13. Remaining Issues

- `workspace search` 仍未处理
- 现有 build warnings 仍未处理
- `359` 相关 RFQ 修复文件仍在工作区中，属于本次任务外的既有改动

## 14. Final Status

`COMPLETE`
