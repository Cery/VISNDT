# 701_M29.1_Search_Boundary_Cleanup Report

> Version: VISNDT Trae Execution Instruction **V3.2.3** / Status: **FROZEN / DEFAULT EXECUTION STANDARD**
>
> Project: VISNDT Industrial Inspection Capability Discovery Platform
>
> Task: 701_M29.1_Search_Boundary_Cleanup
>
> Task Type: Frontend Boundary Cleanup + Search Scope Correction + Architecture Compliance Fix
>
> Stage: M29 Search Experience Optimization
>
> Execution Mode: Trae Implementation

---

## 1. Repository Verification

- **Repository Root**：`F:\Desktop\VISNDT`（Git root，branch = `main`）。
- **Code Root**：`F:\Desktop\VISNDT\VISNDT`（apps/web、apps/api、packages、prisma、docs 结构完整）。
- **Git State**：执行前 working tree 干净；最新提交 `2143efc M28产品化收尾(661-668)与M29搜索审计(700)`。本任务未做 reset/checkout/clean/stash/commit（提交交由用户决定）。
- **Baseline**：700_M29.0 审计已完成——Search Architecture=VERIFIED，Supplier Search=FOUND / SCOPE VIOLATION（F1，P1）。

---

## 2. Changed Files

### 删除（1）

| 文件 | 变更 |
| --- | --- |
| `apps/web/src/components/search/SupplierResultCard.tsx` | 删除供应商搜索结果卡（Supplier Directory Listing 展示，href=`/suppliers/{id}`） |

### 修改（7）

| 文件 | 变更 |
| --- | --- |
| `apps/web/src/components/search/GlobalSearchBar.tsx` | 移除 `SEARCH_DOMAINS` 中 `{ value: 'supplier', label: '供应商' }` |
| `apps/web/src/components/search/SearchTypeTabs.tsx` | 移除「供应商」Tab |
| `apps/web/src/app/search/SearchPageContent.tsx` | 移除 supplier 类型/结果 Section/counts/hasAnyResults/hasMore/analytics 汇总、`SupplierResultCard` import、`VALID_TYPES` 中 `supplier` |
| `apps/web/src/components/home/CapabilityProviderSection.tsx` | 首页 CTA `href` 由 `/search?type=supplier` 改为 `/search?type=supplier-product`；文案「发现能力提供商」→「发现检测能力」 |
| `apps/web/src/services/search.service.ts` | 移除 `SearchDomain` 的 `'supplier'`、`SupplierSearchResult`、`mapSupplier`、`UnifiedSearchResults.suppliers`、`SupplierDiscoveryItem` import |
| `apps/web/src/lib/api/search.ts` | 移除 `SupplierDiscoveryItem`、`UnifiedDiscoveryResponse.suppliers` |
| `apps/web/src/lib/search-utils.tsx` | 更新注释（去除 SupplierResultCard 引用） |

---

## 3. Removed Search Exposure

- GlobalSearchBar 搜索域下拉中的 **供应商** 选项。
- SearchTypeTabs 结果页中的 **供应商** Tab。
- 搜索结果中的 **供应商 Section** + `SupplierResultCard`。
- 首页 **「发现能力提供商」CTA**（原指向 `type=supplier` 供应商目录搜索）。
- 前端 supplier 搜索类型链：`SearchDomain 'supplier'` / `SupplierSearchResult` / `SupplierDiscoveryItem` / `mapSupplier` / `UnifiedDiscoveryResponse.suppliers`。

搜索恢复为 **Industrial Inspection Capability Discovery**，搜索实体收敛为：Product / SupplierProduct Capability / Knowledge / Content / Solution。

---

## 4. Preserved Domain Model

- **SupplierProduct（供应商型号 = 能力派生实例）**：`supplier-product` 搜索域、`SupplierProductResultCard`、`SupplierModelFacetPanel`、`SupplierModelsSection`、`SupplierCompareBar` / `SupplierCompareTable` 全部保留。
- **Supplier = Capability Provider 上下文**：`/suppliers/[id]` 公共能力页、Supplier Workspace（dashboard / runtime / offers / rfqs / opportunities / responses / display / profile）、`SupplierCapability` 组件全部保留。
- **RFQ / Matching / Inquiry / Offer** 体系零改动。

---

## 5. API Impact

**UNCHANGED**。后端 `search.controller` / `search.service`（含 `searchSuppliers`）/ `UnifiedSearchDto` / 统一响应契约未修改。本任务仅收窄前端对搜索响应类型的读取（前端类型，非后端契约）。

---

## 6. Database Impact

**UNCHANGED**。无 Prisma Schema、无 Migration 变更。

---

## 7. Mobile Verification

- 375px / 768px / 1440px 三断点均无 Supplier Search Entry。
- 因移除为组件级（搜索域下拉 / Tab / 首页 CTA 共用同一实例），跨断点一致生效。
- 「供应商型号」（supplier-product）能力发现入口保留，Header 信息架构优化（拥挤缓解）不属本任务范围，交 702。

---

## 8. Build Verification

| 命令 | 结果 | Exit Code |
| --- | --- | --- |
| `npx tsc --noEmit`（apps/web） | PASS | 0 |
| `pnpm --filter web build`（43 页静态生成） | PASS | 0 |

> 注：首次 `next build` 因遗留 dev server（3000）争用 `.next` 目录报 `ENOENT`（`Cannot find module for page: /categories`、`.next/.../route.js.nft.json`）。停掉遗留 dev server、清理 `.next` 后重建 **exit 0**，与代码无关。

---

## 9. Documentation Sync

- `PROJECT_STATUS.md`：新增 701 节。
- `PROJECT_ROADMAP.md`：新增 701 节。
- `MODULE_COMPLETION_MATRIX.md`：新增 M29.1 行。

---

## 10. Scope Compliance

- 搜索边界已恢复为 **Industrial Inspection Capability Discovery**。
- 移除 Supplier Directory Search / Supplier Listing Search / Marketplace Style Supplier Discovery 入口。
- 未新增 SupplierSearch API / Table / Index / Ranking；未进入 Search V2 / AI / Semantic / Vector / RAG / Supplier Marketplace / Search Rewrite（保持 Future Candidate）。

---

## 11. Final Decision

**PASS**

- 搜索边界已恢复；供应商搜索入口已移除；供应商作为能力提供方上下文完整保留。
- 无 P0 / P1 残留；核心搜索链路与 SupplierProduct / RFQ 体系无损；构建与类型检查 exit 0。

---

## 12. Final Execution Output

```text
Task:
701_M29.1_Search_Boundary_Cleanup

Status:
PASS

Search Boundary:
RESTORED

Supplier Search:
REMOVED FROM SEARCH ENTRY

Supplier Domain:
PRESERVED

Database:
UNCHANGED

Migration:
NONE

API:
UNCHANGED

Search Core:
UNCHANGED

Frontend:
UPDATED

Mobile:
VERIFIED

Documentation:
UPDATED

Review Report:
docs/_review/701_M29.1_Search_Boundary_Cleanup_Report.md

Next:
702_M29.2_Search_Entry_And_Hero_Optimization
```