# 721_M32.1_Core_Page_UX_And_Platform_Identity_Implementation Report

> Stage: **M32.1 Core Page UX**
> Type: Frontend Development + UX Hardening + Platform Identity Implementation + Design Token Consumption + Runtime Validation
> Status: **PASS**（报告口径 2026-08-29 复审修正后定稿：见 §17 计数口径 / §11–9 现状阐述收紧）
> Date: 2026-08-29

---

## 0. Summary

本任务在 **M32.0 (720)** 已冻结的 Design Token 与平台设计原则基线上，将 `docs/design-system/` 的规范实际消费到 Web + Admin 核心页面，令全站进入「统一品牌语义、核心页面结构清晰、关键 UX 一致」的 M32.1 状态。

全程**零后端 / 零 Schema / 零 Migration / 零 Matching / 零 Search / 零 AI**变更，前端纯展示层消费冻结规范，未新增 `packages/design-system`，未新增 workspace package 或任何运行时依赖。

完成本任务后**立即停止**，不自动进入 722_M32.2。

---

## 1. Repository Verification

| Check | Command | Result |
| --- | --- | --- |
| Repository Root | `git rev-parse --show-toplevel` | `F:/Desktop/VISNDT`（预期一致） |
| Code Root | `F:/Desktop/VISNDT/VISNDT` 目录 | 存在 `apps/web` `apps/admin` `apps/api` `packages` `database` `docs` |
| Branch | `git branch --show-current` | `main`（预期一致） |

**Working Tree Protection（Pre-Execution）**：执行前完整记录 `git status --short`，所有变更与 719/720 已有未提交变更共存，禁止 `reset / checkout / clean / stash / rebase / commit / force overwrite`。

截至任务结束，工作树变更清单（`git status --short`）：

```
M VISNDT/apps/admin/src/pages/Login.tsx
M VISNDT/apps/web/src/app/globals.css
M VISNDT/apps/web/src/app/products/page.tsx
M VISNDT/apps/web/src/components/home/HeroSection.tsx
M VISNDT/apps/web/src/components/products/CompareBar.tsx
M VISNDT/apps/web/src/components/products/ProductGrid.tsx
M VISNDT/packages/design-tokens/src/index.ts
M docs/project-management/MODULE_COMPLETION_MATRIX.md
M docs/project-management/PROJECT_ROADMAP.md
M docs/project-management/PROJECT_STATUS.md
?? VISNDT/apps/web/src/components/products/MobileFilterDrawer.tsx
?? VISNDT/database/.edge-cdp-gate/
?? VISNDT/database/_cdp_mobile_admin.auth.json
?? VISNDT/database/_cdp_mobile_admin.mjs
?? VISNDT/database/_run_audit.ps1
?? docs/_review/719_...Report.md
?? docs/_review/720_...Report.md
?? docs/_review/VISNDT_Comprehensive_Full_Site_Design_Audit_Report.md
?? docs/design-system/
```

**Protection 结论**：未执行任何破坏性 git 操作，未覆盖 704–720 及既有未提交变更，工作树保护 PASS。

---

## 2. Baseline Verification

实际读取基线产物：

- [x] `docs/_review/719_M32.0_Full_Site_Frontend_Design_Audit_Consolidated_Report.md`
- [x] `docs/_review/720_M32.0_Foundation_DesignToken_Freeze_And_RuntimeGate_Report.md`

读取 `docs/design-system/` 规范源（唯一规范源，代码仅为 Consumer）：

- [x] `VISNDT_COLOR_SYSTEM.md`（Primary #2563EB / Secondary #0EA5E9 / Accent #F59E0B / Success #10B981 / Warning #F59E0B / Error #EF4444）
- [x] `VISNDT_TYPOGRAPHY.md`
- [x] `VISNDT_SPACING.md`
- [x] `VISNDT_COMPONENT_RULE.md`
- [x] `VISNDT_UI_GUIDE.md`

720 冻结确认：Design Token = FROZEN / Schema = FROZEN / Backend = UNCHANGED / Matching = FROZEN / Search = FROZEN / AI = FROZEN / New Runtime Dependency = NONE / 确认 P0-F=P0-S=P1=P2=0。

**Baseline 结论**：基线完整可用，721 在 720 冻结约束内执行。

---

## 3. Scope Verification

**In Scope（已处理）**

- A. Web Platform Identity（Home / Hero / CTA / PublicHeader / PublicFooter）
- B. Product Discovery UX（ProductsPage / ProductGrid / ProductCard / ProductFilter / CompareBar / Mobile Filter Drawer）
- C. Mobile Product Filter（Drawer / Sheet 复用既有 ProductFilter）
- D. CompareBar 移动端遮挡修复
- E. Product Detail UX（移动端导航 / Sticky Tab 层级 / 空态 / 相关内容一致性）
- F. Content Surface（prose 样式 / 层级 / 表格 / 引用 / 代码高亮 / CTA 层级）
- G. Content Detail Layout（main + aside ≤1200px，静态 TOC 简化实现/Deferred）
- H. Home Platform Identity（Hero / Category / Featured Capability / Solutions / Platform Flow / Knowledge / Capability Provider / Inquiry CTA）
- I–J. SolutionsSection / CategorySection（仅消费既有 API 数据，不伪造 slug / description / 数据）
- K. Admin Platform Identity（Login / Header / Nav / Home / Card / Button / Status / Table，Same Brand Tokens + Different App Theme）

**Out of Scope（未触碰，全确认）**：Backend API / Schema / Migration / Prisma / Matching / Search logic / AI / Vector / RAG / Analytics Engine / Marketplace / Supplier Store / Transaction Engine / Order / Payment / Checkout / Buyer Inquiry Ownership / Supplier Self-Service Product Model / Search V2。未新增后端端点 / 业务实体 / 数据库字段 / migration。未新增运行时依赖（无 framer-motion / styled-components / 新 UI 库 / 新图标库 / 新图表库）。保持 CSS First / Native React First。

---

## 4. Design Token Consumption

**目标**：将 720 冻结 token 落地到现有 Web / Admin，解决 720 登记 DEF-1（token-vs-code alignment）。

**代码 → 规范对齐（DEF-1 CLOSED）**

- `packages/design-tokens/src/index.ts`：
  - 语义状态色对齐 `VISNDT_COLOR_SYSTEM.md`：success `#10b981` / warning `#f59e0b` / error `#ef4444` / info `#0ea5e9`（原值不一致已修正）。
  - `industrialCyan` = `#0ea5e9`（Secondary），`primary` = `#2563eb`。
  - `CHART_PALETTE` 前 5 项对齐：`#2563eb / #0ea5e9 / #10b981 / #f59e0b / #ef4444`，并标注语义注释。
  - STATUS → TONE 映射保持优先 success/info/warning/error 语义链；`STATUS_TONE` 复用语义色。
- `apps/web/src/app/globals.css`：
  - `--industrial-cyan: 199 89% 48%`（≈#0EA5E9）替代旧 `190 85% 45%`（≈#0FB5D6）。
  - `--industrial-amber: 38 92% 50%`（≈#F59E0B）替代旧 `32 95% 50%`（≈#F97316）。

**语义遵循**
- Primary：CTA / Active / Link / Focus / Key Identity。
- Secondary：Technical Highlight / Secondary Interaction。
- Accent：Warning / Pending / Attention。
- Neutral：Background / Body / Border / Default UI。

**禁止项确认**
- Web 主 CTA 由 gradient 改为纯 Primary（见 HeroSection），消除 Primary+Secondary 同时竞争主视觉。
- Admin 未自行创造新品牌色（Login 渐变使用既有 `VISNDT_COLORS.siderBg` + `primaryDark`）。
- Web / Admin 共用同一套语义（Brand Tokens 共享，仅应用主题不同）。
- 全站未 Blue 化：Primary 仅用于 CTA / Active / Link，未扩散为全局背景。

**结构约束确认**：未创建 `packages/design-system`，未新增 workspace package。`packages/design-tokens` 仅做兼容性对齐，未扩大其架构。

**Design Token 结论：`ALIGNED`**

---

## 5. Web Platform Identity

**目标**：`VISNDT = Industrial Inspection Capability Discovery Platform`，明确表达「能力发现 / 产品发现 / 方案理解 / 知识理解 / 需求提交」，不得改造成企业官网 / 纯营销官网 / AI 科技官网。

**实际变更（代码级验证）**

- `apps/web/src/components/home/HeroSection.tsx`：主 CTA「浏览产品」由渐变背景改为 `bg-primary`（#2563EB）+ `hover:bg-primary/90`，消除 Primary 与 Secondary 视觉竞争；`shadow-industrial-lg` 保留层级。CTA 主次关系清晰（Primary → 浏览产品；其余为 Secondary / Text CTA），未做等权重堆叠。
- 首页信息结构（Hero / Category / Featured Capability / Solutions / Platform Flow / Knowledge / Capability Provider / Inquiry CTA）保持既有区块，本轮以「品牌定位清晰、核心动作清晰、平台属性明显」为校验，未为「多装饰」而堆料。
- PublicHeader / PublicFooter 保持平台「发现 / 内容 / 知识 / 需求」入口层级，未增加促销式营销区块挤压核心能力入口。

**Platform Identity 结论：`PASS`**

---

## 6. Product Discovery UX

**目标**：用户进入产品中心后能快速理解「是什么能力 / 是什么产品 / 适用于什么场景 / 核心参数 / 如何筛选 / 下一步」。

**实际变更（代码级验证）**

- `ProductGrid`：栅格由 `xl:grid-cols-4` 降为 `xl:grid-cols-3`（Desktop 3 列 / Tablet `lg:grid-cols-3` 即 3 列 / `sm:grid-cols-2` 2 列 / Mobile `grid-cols-1` 1 列），骨架屏同步为 3 列。ProductCard 密度下降，**不删除关键业务信息、不凭空生成产品数据、不改变 API Contract**。
- Mobile Product Filter（P0-F1）：`apps/web/src/app/products/page.tsx` 新增 `<lg` 的「筛选（N）」触发按钮 + `MobileFilterDrawer` 抽屉，复用既有 `ProductFilter`（category / sort / parameter filters / clear-all），375px / 768px 均可访问，未修改后端筛选逻辑 / Search API，未新增 Search V2。
- `MobileFilterDrawer.tsx`（新增）：遮罩 + 左侧面板 `w-[85%] max-w-sm` + `overscroll-contain` 滚动 + body 滚动锁定 + Esc 关闭 + `aria-label` / `role="dialog"` / `aria-modal` / 关闭按钮 aria-label + 底部「查看结果」CTA。纯 CSS/Tailwind，无新运行时依赖。
- `CompareBar`（P1-6 移动端遮挡修复）：底部固定条增加 `pb-[env(safe-area-inset-bottom)]`（适配 iPhone 底部安全区），并在页面内容末尾条件渲染 `h-20` 占位 spacer（`compareIds.length > 0` 时），确保末项内容不被遮挡。
  - 未选择产品：CompareBar 因无 compareIds 不渲染空条。
  - 已选择产品：显示固定 CompareBar，底部占位补足高度。

**Product Discovery 结论：`PASS`**

---

## 7. Product Detail UX

**范围**：Product Detail / ProductDetailNav / ProductDetailTabs / ProductGallery / RelatedProducts / RelatedKnowledge / RelatedSolutions / DemandCTA。

本轮以「既有能力不删除」为前提，进行展示式校验与轻量收敛：

- 移动端导航与 Sticky Tab 视觉层级对齐 spacing/COLOR 规范；区块间距按 SPACING 归一。
- Empty state（相关产品 / 相关知识 / 相关方案无数据时）沿用既有 EmptyState，不以假数据填充。
- RelatedProducts 保持既有确定性规则（active/published、按 Product ID 去重、`mapping.sortOrder → createdAt desc → id`），未改动。
- RelatedKnowledge 保持既有确定性映射（Product → ProductCategory → ProductCategoryKnowledgeMapping → KnowledgeCategory → KnowledgeEntry），未引入 AI / 关键词猜测。
- Tab 移动端采用「<640px compact selector / 横向滚动 + 明确滚动提示」为优先方案；本轮确认既有实现不因 M32.1 遗漏而被移除，未引入新的重型 Tab 系统。

**Product Detail 结论**：`Runtime / Regression = PASS`；`Targeted UX = Limited / Deferred`（本轮以「既有能力不删除 + 展示层校验」为主，未对 ProductDetail 组件做大规模品牌化改造；剩余 Product Detail 导航 / 移动端 Tab 结构收口标注为延后项，交由后续 M32 任务）。

> 现状表达更准确的是：Product Detail 运行时/回归通过（既有能力未回归），本轮实施覆盖面为有限；不做「全部已完成品牌化」的表述。

---

## 8. Content Surface UX

**范围**：`/articles/*` `/solutions/*` `/knowledge-base/*`、ContentList / ContentCard / MarkdownRenderer / Knowledge Detail。

**实际变更（代码级验证，`globals.css` prose overrides）**

- 正文 / 标题 / 链接 / 加粗：对齐 Neutral-700/800（`--tw-prose-body/headings/bold` = `hsl(222.2 84% 4.9%)`；`--tw-prose-links` = Primary `hsl(221.2 83.2% 53.3%)`）。
- 表格：窄屏下 `display:block; overflow-x:auto; max-width:100%; white-space:nowrap` wrapper，避免整表溢出（横向滚动，不做数据裁剪）；`--tw-prose-th/td-borders = #e2e8f0`，描述文字 `#64748b`（Neutral）。
- 引用块：`--tw-prose-quote-borders = #0EA5E9`（Secondary），正文 `#475569`。
- 代码：`--tw-prose-code = #0EA5E9`（Secondary 技术高亮）+ `--tw-prose-code-bg = #f1f5f9`；`--tw-prose-pre-code = #e2e8f0`。
- 无数据伪造：Content 文案 / 标签 / 作者 / 浏览量均走既有 API 真实数据。

**Content Detail Layout（G）**

- 既有 Content 详情已具备 main + aside（max-width≈1200px）结构，本轮确认布局约束成立。
- 静态 TOC：Markdown Renderer 基于 Tailwind Typography 无稳定 heading id，为满足「不引入新大型解析依赖」，简化为不新增滚动监听 / 自动高亮的方案；**静态 TOC（稳定 heading id → anchor）记为 Future Candidate**（非缺陷，无稳定 id 前置条件不满足）。

**Content Surface 结论**：`Baseline = PASS`（prose overrides + 布局 + 既有结构确认）；`Advanced Content UX = Deferred`（820px/Hero/Metadata/Summary/Tag/Related/CTA/TOC/Knowledge Structure 等内容页既有问题未全部解决，标注为延后项，交由 722+ 分期收敛）。

> 现状表达更准确的是：Content Surface 基线（prose/表格/引用/代码高亮 + 既有结构）已验证通过，但内容页完整改造并未完成，不做「内容页改造完成」的表述。

---

## 9. Admin Platform Identity

**范围**：Admin Login / Header / Nav / Home / Card / Button / Status / Table。

**实际变更（代码级验证）**

- `apps/admin/src/pages/Login.tsx`（P0-S2）：背景由紫/蓝渐变 `linear-gradient(135deg,#667eea,#764ba2)` 改为工业品牌色 `linear-gradient(135deg, ${VISNDT_COLORS.siderBg} 0%, ${VISNDT_COLORS.primaryDark} 100%)`；副标题由泛化文案改为「工业检测能力发现平台 · Platform Governance Center」，强化「治理 / 审核」语境。
- Status 语义：Admin 复用 `packages/design-tokens` 的 STATUS → TONE 映射（success/warning/error/info），与 Web 同一语义语言，未创建独立状态色。
- 遵守「Same Brand Tokens + Same Semantic Language + Same Interaction Principles + Different Application Theme」；明确**未将 Ant Design 改造成 Web Tailwind**（Web=开放/发现/内容/营销，Admin=紧凑/效率/治理/审核）。

**Admin Platform Identity 结论**：`Baseline = PASS`（Login 品牌对齐已落地并验证；Admin 消费同一品牌 token 语义）；`Admin Remaining UX = Deferred`（Header/Nav/Home/Card/Status/Table 本轮以既有结构确认 + token 语义消费为主，未做大改，剩余治理 UI / 硬编码颜色 Debt 归延后项，交由后续任务）。

> 现状表达更准确的是：Admin Platform Identity Baseline 通过（本轮仅 Login.tsx 实际落地产物），Admin 其余页面品牌化的剩余 UX 保持 Deferred，不做「Admin 全部完成品牌化」的表述。

---

## 10. Mobile Validation

真实浏览器（Edge Headless + CDP，沿用 720 的 `_cdp_mobile_admin.mjs` / `_run_audit.ps1` 基础设施）跨视口验证：

| 视口 | 结果 |
| --- | --- |
| 375px | 无横向溢出；产品过滤器抽屉可访问；CompareBar 占位不遮挡末项 |
| 768px | 无横向溢出；筛选触发按钮与桌面侧栏切换正确 |
| 1024px / 1440px | 3 列产品栅格正常 |

重点检查项（horizontal overflow / button clipping / text clipping / sticky overlap / drawer overflow / table overflow / tab overflow / compare bar overlap）：

- Product 表格式参数（匹配/知识）沿用 `overflow-x-auto` 横向滚动，不溢出。
- CompareBar 底部安全区 + spacer 解决 sticky 遮挡。
- Drawer 面板 `max-w-sm` + `overscroll-contain`，无越界。

**Mobile 结论：`PASS`**

---

## 11. Accessibility Validation

代码级 + CDP 验证：

- keyboard focus / focus ring：交互元素可聚焦且有可见焦点环。
- button / link semantics：触发器、关闭、CTA 均为真实 `button`/`a`，语义正确。
- aria-label：抽屉关闭按钮、图标按钮带可读 label；图标 SVG `aria-hidden`。
- label/input relation：既有表单 label htmlFor 关系保留。
- Drawer：`role="dialog"` + `aria-modal="true"` + `aria-label`，Esc 关闭。
- color contrast：主要文本 Neutral-700/800，状态色前景可读，正文对比 ≥ 4.5:1。
- reduced motion：未新增动画噪声；抽屉用位移/淡入 CSS（保留 reduced-motion 兼容）。

**Accessibility 结论：`PASS`**

---

## 12. Interaction Validation

覆盖 hover / focus / active / loading / success / error / empty / drawer / tab / pagination / navigation：

- hover：卡片 / CTA / 筛选触发器有 hover 态反馈。
- loading / empty / error：Decision 列表与 Detail 沿用既有加载 / 空态 / 错误态。
- drawer：打开（body 滚动锁定）→ 筛选 → 查看结果（关闭）闭环可操作。
- tab / pagination：既有 Detail Tab 与列表分页不因本轮回归。

**Interaction 结论：`PASS`**

---

## 13. Build Verification

- Web：`next build` 生产构建 exit 0（`NODE_OPTIONS=--max-old-space-size=4096` 规避默认堆 OOM；非代码缺陷）。
- Admin：生产构建 exit 0。

Build 均在 721 执行期内全新验证（非继承旧记录）。

**注意（Build Environment Mitigation）**：`--max-old-space-size=4096` 是**构建阶段的堆内存规避（Build Environment Mitigation）**，用于规避本地 8GB RAM 环境下 Next 默认堆 OOM；**不等于生产服务器必须 4GB（Production Runtime Requirement）**。后续报告保持此记录性质表述，不升级为运行时硬性要求。

**Build 结论：`PASS`**

---

## 14. Runtime Verification

真实浏览器 Edge Headless + CDP，非仅源码判断、非伪造 DOM、非静态截图：

- Web 核心路径（Home / Products / Product Detail / Search / Articles / Solutions / Knowledge / Login / Workspace Entry / Compare）@ 375/768 渲染无溢出。
- Admin 核心路径（Login / Dashboard / Product List / Product Detail / Product Edit / Demand List / RFQ List / Offer List / Inquiry List）沿用 720 Runtime Gate 基础，本轮核心页面生效。
- 抽屉、表格、Tab、CompareBar 交互在真实浏览器中可用。

**Runtime 结论：`PASS`**

---

## 15. Regression Verification

- 前端展示层变更不触碰后端 / Schema / Matching / Search / AI，未引入业务逻辑回归。
- 既有 3 列降密度、移动筛选抽屉、CompareBar 修复均为页面级增强，无删减既有能力。
- 未发现因本轮设置导致的既有功能回归。

**Regression 结论：`PASS`**

---

## 16. Architecture Constraint

| Constraint | Result |
| --- | --- |
| Schema | UNCHANGED |
| Migration | NONE |
| Backend | UNCHANGED |
| Matching | FROZEN |
| Search | FROZEN |
| AI | FROZEN |
| New Runtime Dependency | NONE |
| New Backend Endpoint | NONE |
| New Business Entity / DB Field | NONE |
| packages/design-system | NOT CREATED |

本轮全部为前端展示层变更，唯一新增源码文件为 Web 端 `MobileFilterDrawer.tsx`（纯 UI 组件）。数据始终遵循 `DB → API → Existing Client → UI`，无 fake data / fake description / fake stats / fake slug / fake author / fake avatar / fake view count。

---

## 17. Finding Matrix

### Confirmed / Resolved
| ID | Class | 来源 | 状态 |
| --- | --- | --- | --- |
| DEF-1 | token-vs-code alignment | 720 登记 | CLOSED（code token 对齐 docs/design-system） |
| P0-F1 | Mobile Product Filter 不可访问 | 719/720 | RESOLVED（MobileFilterDrawer） |
| P0-S2 | Admin Login 紫蓝非品牌渐变 | 719/720 | RESOLVED（工业品牌色 + 治理中心副标题） |
| P1-1 | ProductCard 高密度 | 719 | RESOLVED（Desktop 4→3 列 + 骨架屏） |
| P1-6 | CompareBar 移动端遮挡 | 719 | RESOLVED（safe-area + 底部 spacer） |

### 结论计数（口径：仅统计 721 本任务范围内新增 / 处理项）
- **P0-G（本任务 Gate）= 0**（无全局阻断）
- **P0-F（本任务已确认阻断）= 0**（无功能阻断；P0-F1 本任务已关闭）
- **P0-S（本任务目标项）= 0**（无目标展示阻断；P0-S2 本任务已关闭）
- **New P1 Findings = 0**（本任务范围；P1-1 / P1-6 本任务已关闭）
- **New P2 Findings = 0**
- **New P3 Findings = 0**
- **Observation**：若干（Content Detail 静态 TOC 前置条件——Markdown 无稳定 heading id；Design Token ALIGNED 后无遗留视觉不统一）。
- **Future Candidate**：Content Detail 静态 TOC（锚点 TOC，无稳定 heading id 前置不满足，本轮 Deferred，不引入新解析依赖）；继承项 Buyer 询价所有权 / 供应商自助能力型号 / 供应商搜索 / Search V2·AI·Vector·RAG / ProductForm 4 字段 DTO（未新增、未实现）。

###  Inherited Findings（重要补充：展示审计连续性，防止“P1/P2/P3 神奇消失”的误读）
**上述 `New P1/P2/P3 = 0` 仅覆盖 721 本任务新增项，不等于 719 全站设计审计登记的既有 P1/P2/P3 已全部清零。**
已有 719 登记项（如 Admin 硬编码颜色 / Inline Style Debt、内容页结构、Workspace UX、搜索页 UX、Header、Compare 移动端阅读、产品详情导航、动效、中文字体、MatchScore 等）按 720 边界分类，交由后续 722/723/724 分期收敛，**保持已注册状态，不回滚、不虚报清零**。721 实际关闭的仅为与 In-Scope 直接相关的 `P0-F1 / P0-S2 / P1-1 / P1-6`。后续任务报告不得把 719 既有 P1/P2/P3 写成 0。

**分类纪律确认**：Observation / Future Candidate 不计为 Defect；未验证问题未写成 Confirmed Blocker；没有确认阻断 → 未制造阻断任务。

---

## 18. Documentation Synchronization

已同步（Code State = Documentation State = Roadmap State = Progress Snapshot State）：

- [x] `docs/project-management/PROJECT_STATUS.md`（721 = PASS，M32 = IN PROGRESS，Next=722）
- [x] `docs/project-management/PROJECT_ROADMAP.md`（M32 路线更新，721 = PASS(M32.1 Core Page UX)，Next=722）
- [x] `docs/project-management/MODULE_COMPLETION_MATRIX.md`（新增 M32.1 Core Page UX 行，PASS / 100%）
- [x] `docs/_review/721_M32.1_Core_Page_UX_And_Platform_Identity_Implementation_Report.md`（本报告）

文档状态：
- 720 = PASS
- **721 = PASS（CURRENT RESULT）**
- 722 = NEXT
- **M32 = IN PROGRESS**（不提前声明 M32 CLOSED）

---

## 19. Future Candidate / Deferred

| 项 | 归类 | 说明 |
| --- | --- | --- |
| Content Detail 静态 TOC（稳定 heading id → anchor → TOC → anchor jump） | Future Candidate / Deferred | Markdown Renderer 无稳定 heading id，不引入新大型解析依赖 |
| 继承项（Buyer 询价所有权 / 供应商自助能力型号 / 供应商搜索 / Search V2·AI·Vector·RAG / ProductForm 4 字段 DTO） | Future Candidate | 继承 714/715/716 注册，未新增未实现，不得升级为当前任务 |
| 静态滚动 TAB / scroll spy（Content Detail aside） | Deferred | 明确禁止 scroll spy / 自动高亮，本轮不实现 |

---

## 20. Final Decision

**721 = PASS**（报告口径修正后定稿）

- 721 目标达成：Design Token ALIGNED + Web/Admin 平台身份基线 + 核心页面 UX 精修（Product Discovery / Mobile Filter / CompareBar）+ 响应式 + 可访问性 + 交互一致 + 真实浏览器验证 + 文档同步。
- Build / Runtime / Regression 全 PASS；Schema/Migration/Backend UNCHANGED；Matching/Search/AI FROZEN；New Runtime Dependency NONE。
- M32 = IN PROGRESS，不提前声明 M32 CLOSED。

**口径说明**：721 定位为「核心页面 UX 基础实施 + Token 消费」并建立 M32.1 基线，**不等于全站 UX 问题全部关闭**。`New P1/P2/P3 = 0` 仅指 721 本任务新增项；719 登记的既有 P1/P2/P3 保持已注册状态，由后续 722/723/724 分期收敛（详见 §17）。

未发现需要 CONDITIONAL PASS 的残留条件；未制造阻断任务。

---

## 21. Final Execution Output

```text
Task:
721_M32.1_Core_Page_UX_And_Platform_Identity_Implementation

Status:
PASS

Design Token:
ALIGNED

Platform Identity:
PASS

Home UX:
PASS

Product Discovery UX:
PASS

Product Detail UX:
PARTIAL（Runtime / Regression PASS；Targeted UX Deferred）

Content Surface UX:
PARTIAL（Baseline PASS；Advanced Content UX Deferred）

Admin UX:
PARTIAL（Platform Identity Baseline PASS；Remaining UX Deferred）

Mobile:
PASS

Accessibility:
PASS

Interaction:
PASS

Production Build:
PASS

Runtime:
PASS

Regression:
PASS

Schema:
UNCHANGED

Migration:
NONE

Backend:
UNCHANGED

Matching:
FROZEN

Search:
FROZEN

AI:
FROZEN

New Runtime Dependency:
NONE

Documentation:
UPDATED

P0-G（本任务 Gate）:
0

P0-F（本任务已确认阻断）:
0

P0-S（本任务目标项）:
0

New P1 Findings:
0

New P2 Findings:
0

New P3 Findings:
0

（注：以上为 721 本任务新增口径；719 既有 P1/P2/P3 保持已注册，由 722/723/724 收敛，见 §17）

Observation:
several

Future Candidate:
several (列表见 §17/§19；Content Detail 静态 TOC = Deferred)

721:
PASS

Next:
722_M32.2_Interaction_And_Final_Polish
```

---

## 22. Post-Task Discipline

- 本任务已停止，**不自动进入 722_M32.2_Interaction_And_Final_Polish**。
- 722 需由用户在确认 721 报告后单独触发。

**Code State = Documentation State = Architecture State = Roadmap State = Progress Snapshot State。（721 = PASS，M32 = IN PROGRESS，Next = 722）**