# 324_M14.2.1 Web Build Dependency Stabilization Report

## 1. 根目录确认

- 仓库根目录：`F:\Desktop\VISNDT`
- 代码根目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 执行时间：`2026-08-09 12:35:21 +08:00`

执行前确认文件：

- `F:\Desktop\VISNDT\VISNDT\apps\web\package.json`
- `F:\Desktop\VISNDT\VISNDT\apps\web\eslint.config.mjs`
- `F:\Desktop\VISNDT\VISNDT\pnpm-lock.yaml`
- `F:\Desktop\VISNDT\docs\_review\323_M14.2.0_Web_Build_Environment_Stabilization_Audit_Report.md`

## 2. 修改文件列表

本次任务实际修改文件如下：

1. `F:\Desktop\VISNDT\VISNDT\apps\web\package.json`
2. `F:\Desktop\VISNDT\VISNDT\pnpm-lock.yaml`
3. `F:\Desktop\VISNDT\docs\_review\324_M14.2.1_Web_Build_Dependency_Stabilization_Report.md`

说明：

- 未修改任何 `apps/web/src/**` 业务代码
- 未修改 `apps/api/**`
- 未修改 `database/prisma/**`

## 3. 修改原因

根据 `323_M14.2.0_Web_Build_Environment_Stabilization_Audit_Report.md` 的审计结论：

- `apps/web/eslint.config.mjs` 直接 `import { FlatCompat } from "@eslint/eslintrc"`
- 但 `apps/web/package.json` 未显式声明 `@eslint/eslintrc`
- 导致 `next build` 进入 ESLint 配置解析阶段时，出现：

```text
Cannot find package '@eslint/eslintrc' imported from apps/web/eslint.config.mjs
```

因此本次只做最小工程依赖修复：

- 在 `apps/web/package.json` 的 `devDependencies` 中补齐 `@eslint/eslintrc`
- 保持版本固定为 `3.3.6`
- 不升级其他 eslint / next / react / typescript 相关版本

## 4. package.json 变化

本次仅新增以下依赖声明：

```json
"@eslint/eslintrc": "3.3.6"
```

变更位置：

- `apps/web/package.json` -> `devDependencies`

未变更内容：

- `eslint`
- `eslint-config-next`
- `next`
- `react`
- `react-dom`
- `typescript`

## 5. pnpm install 结果

执行目录：

```text
F:\Desktop\VISNDT\VISNDT
```

执行过程分两步：

### 5.1 首次执行

执行命令：

```text
pnpm.cmd install
```

结果：

- **FAIL**

失败原因：

```text
ERR_PNPM_OUTDATED_LOCKFILE
Cannot install with "frozen-lockfile" because pnpm-lock.yaml is not up to date with apps/web/package.json
```

说明：

- 在新增 `apps/web/package.json` 依赖声明后，当前环境默认启用了 `frozen-lockfile`
- 因此第一次安装被锁文件一致性检查拦截

### 5.2 第二次执行

执行命令：

```text
pnpm.cmd install --no-frozen-lockfile
```

结果：

- **SUCCESS**

关键结果：

- workspace 链接重新生成
- `pnpm-lock.yaml` 已写入 `apps/web` 对 `@eslint/eslintrc@3.3.6` 的直接声明
- 安装输出包含 Node 的 `DEP0169` deprecation warning，但未阻断安装完成

补充说明：

- `pnpm install` 后，`pnpm-lock.yaml` 除新增 `apps/web` 的 `@eslint/eslintrc` 记录外，还出现了若干 peer / snapshot 归一化变化
- 本次未主动调整这些包版本，属于 install 过程中 lockfile 的自动重写结果

## 6. build 前后错误变化

### 6.1 修复前

来源：`323_M14.2.0_Web_Build_Environment_Stabilization_Audit_Report.md`

修复前 `pnpm.cmd --filter @visndt/web build` 的核心失败信息为：

```text
Cannot find package '@eslint/eslintrc' imported from apps/web/eslint.config.mjs
```

结论：

- build 在 ESLint 配置加载阶段失败
- `@eslint/eslintrc` 解析问题为阻断项

### 6.2 修复后

执行命令：

```text
pnpm.cmd --filter @visndt/web build
```

结果：

- **FAIL**
- exit code：`1`

修复后不再出现以下错误：

```text
Cannot find package '@eslint/eslintrc'
```

新的失败点为现存 ESLint 规则问题：

```text
./src/lib/api/rfqs.ts
26:18  Error: An interface declaring no members is equivalent to its supertype.  @typescript-eslint/no-empty-object-type

./src/services/inquiry.service.ts
26:20  Error: Unexpected any. Specify a different type.  @typescript-eslint/no-explicit-any
```

同时存在 lint warning：

```text
./src/app/layout.tsx
26:9  Warning: Custom fonts not added in `pages/_document.js` will only load for a single page.

./src/components/products/ManufacturerInfo.tsx
10:3  Warning: 'productName' is defined but never used.
```

结论：

- 本次任务目标中的 `@eslint/eslintrc` 依赖解析问题已修复
- 但 `@visndt/web build` 仍未通过，原因已切换为仓库中现存的 ESLint 规则错误

## 7. 是否影响业务代码

结论：

- **不影响业务代码**

说明：

- 本次未修改任何页面逻辑、组件逻辑、接口调用逻辑或业务流程
- 未修改 `apps/web/src/**`
- 本次仅处理 `apps/web` 的工程依赖声明与 lockfile 同步

## 8. Freeze检查

本次任务确认未修改：

- `apps/web/src/**`
- `apps/api/**`
- `database/prisma/**`
- API
- 权限体系

补充说明：

- 工作区中存在其他既有未提交变更
- 本次未改动这些既有业务代码，仅在允许范围内修改：
  - `apps/web/package.json`
  - `pnpm-lock.yaml`
  - 本报告文件

## 9. API影响

- 无 API 变更
- 无接口路径变更
- 无请求/响应结构变更
- 无鉴权或权限链路变更

## 10. 数据库影响

- 无 Prisma Schema 修改
- 无 migration
- 无数据库结构变更
- 无测试数据写入

## 11. Build结果

执行命令：

```text
pnpm.cmd --filter @visndt/web build
```

执行结果：

- **FAIL**
- exit code：`1`

结果判定：

- `@eslint/eslintrc` 缺失问题：**已消除**
- 当前 build 状态：**仍失败**
- 当前阻断项：现存 ESLint 规则错误，而非依赖解析错误

## 12. 下一步建议

本次任务完成后建议停止，不进入其他开发。

如需继续推进 Web build 全绿，建议单独开新任务处理以下已有 lint 问题：

1. `apps/web/src/lib/api/rfqs.ts` 的 `@typescript-eslint/no-empty-object-type`
2. `apps/web/src/services/inquiry.service.ts` 的 `@typescript-eslint/no-explicit-any`

说明：

- 后续任务应明确为 lint / code quality 修复
- 不应与本次依赖稳定化任务混做
