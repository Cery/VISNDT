# 715 M31.1 Core User Experience Hardening — Review Report

> Project: VISNDT Industrial Inspection Capability Discovery Platform
> Task: 715_M31.1_Core_User_Experience_Hardening
> Stage: M31 Productization Stability And Core Experience Closeout（M31 四主任务之 1）
> Execution Mode: Trae Implementation + Runtime Verification
> Report Date: 2026-08-26
> Status: **PASS**

---

## 1. Repository Verification

| Item | Result |
|------|--------|
| Repository Root（`git rev-parse --show-toplevel`） | `F:/Desktop/VISNDT` ✅ |
| Code Root | `F:\Desktop\VISNDT\VISNDT`（apps/web, apps/admin, apps/api, packages, database, docs 均存在）✅ |
| Branch | `main` ✅ |
| Working Tree | 全量未提交改动保留（`M`/`??` 完整）；无 reset / checkout / clean / stash / rebase / commit / force overwrite ✅ |
| Protected History | 704/704-R1/705/706/707/708/709/710/711/711-R1/712/713/714 及其它已有未提交变更全部保留，未被覆盖 ✅ |

- 仓库根与代码根分离：仓库根 = `F:\Desktop\VISNDT`（Git root），业务代码根 = `F:\Desktop\VISNDT\VISNDT`；文档同步位于仓库根 `docs/`。

---

## 2. Baseline Verification

已读取并确认以下基线报告实际存在：

- `docs/_review/714_M30_Final_Validation_And_M31_Entry_Baseline_Audit_Report.md` — **PASS**，M31 Entry **APPROVED**
- `docs/_review/713_M30.5_Transaction_Data_Surface_Completion_Report.md` — CONDITIONAL PASS，FC=1（非缺陷）
- `docs/_review/712_M30.4_Demand_RFQ_Matching_Data_Closure_Report.md` — PASS，D-1~D-4 CLOSED
- `docs/_review/711-R1_M30.3_Domain_Data_Surface_And_Frontend_Consistency_Deep_Audit_Report.md` — AUDIT ONLY

确认状态：

```text
M30        = CLOSED / READY
M31 Entry  = APPROVED
Architecture = FROZEN
Schema     = UNCHANGED
Matching   = FROZEN
Search     = FROZEN
AI         = FROZEN
```

---

## 3. Scope Verification

### 3.1 In-Scope（本任务实际进入范围）

| 范围 | 状态 |
|------|------|
| A. Buyer Demand Experience（Create / Edit / Detail / Parameters / Status / Category / Contact Visibility） | 实施 ✅ |
| B. Buyer Matching Experience（Match Detail / Score / Status / Explanation / Parameter Scores / Hard Fail / Next Action） | 审查 + 数据面确认 ✅ |
| C. Buyer RFQ Experience（RFQ Detail / Demand Context / Source Match / Target Org / Responses / Status / Next Action） | 审查 + 数据面确认 ✅ |
| D. Supplier RFQ Experience（Demand Context / Parameters / Source Match / Target Capability / Response Status / Offer Context / Next Action） | 实施 ✅ |
| E. Supplier Offer Experience（Capability / Model / Provider / Price / Currency / Status / Lifecycle / Created Time） | 数据面确认 ✅ |
| Supplier Inquiry Experience（收到询价，仅维护既有） | 数据面确认 ✅（Buyer 所有权视图 = Future Candidate） |

### 3.2 Out-of-Scope（未进入，如实记录）

Admin 全量治理、Product SEO API 扩展、Buyer「我的询价」模型扩展、SupplierProduct 自助创建、Supplier Search、Search V2、AI/Vector/RAG/Semantic、Ranking/Matching/RuleEngine Rewrite、Analytics 自动化、Solution 独立实体、Transaction/Order/Payment、Marketplace、Supplier Store、Schema 重构、Migration、新业务模型 —— **均未进入本任务**，发现问题一律登记 Future Candidate，不实施。

---

## 4. Demand UX Verification（A. Buyer Demand Experience）

### 4.1 实施内容

- **Create**（`apps/web/src/app/workspace/demands/create/page.tsx`）：补齐分类选择（categoryId）+ 联系人字段（contactName / contactPhone / contactEmail / contactVisible）+ 数量单位（quantityUnit），与数据模型及编辑页一致。
- **Edit**（`apps/web/src/app/workspace/demands/[id]/edit/page.tsx`）：补齐 quantityUnit + 分类选择，创建/编辑字段收敛。
- **Detail**（`apps/web/src/components/demand/DemandDetail.tsx`）：新增预算范围、需求数量+单位、预计交付、联系人/电话/邮箱、联系方式可见性的完整信息区块；`contactVisible=false` 时真实联系方式以掩码展示（数据面由后端掩码保证，前端不伪造）。
- **Parameters**（`apps/web/src/components/demand/DemandParameters.tsx`）：NUMBER 正确展示 `value / valueMin / valueMax / unit`；ENUM 使用 `parameterDefinition.options.label`（不显示内部 option value）；BOOLEAN 映射「是/否」；空值诚实显示「未填写」。

### 4.2 数据面验证（运行时 36/36 中 Demand 相关）

| Check | Result |
|-------|--------|
| M715-D01 POST /demands（categoryId + contacts + unit） | ✅ 201 |
| M715-D02 findOne category projection（owner, contactVisible=true） | ✅ category.id/name 正确投影 |
| M715-D03 findOne contact 真实值（visible=true） | ✅ phone/email 原值 |
| M715-D04 quantity + unit + budget surfaced | ✅ 5 台, budget=10000-50000 |
| M715-D05 contact protection（visible=false → `***`） | ✅ 掩码生效 |
| M715-D06 findMy category projection（列表面） | ✅ category badge 有数据 |
| M715-D07 NUMBER param surface（min/max/unit） | ✅ min=1 max=100 unit=% |
| M715-D08 ENUM param surface（options.label） | ✅ value=MODE_A → label=模式A |

结论：**Demand = VERIFIED**（字段顺序 / 信息层级 / 必填逻辑 / 参数分组 / 参数类型展示 / 状态展示 / 保存后回显 / 空态均收敛；无 fake data）。

---

## 5. Matching UX Verification（B. Buyer Matching Experience）

`apps/web/src/app/workspace/matches/[matchId]/page.tsx`：

- 总分（matchScore 0-100）、状态（matchStatus）、是否 hardFail、参数级评分（required / weight / demandValue / productValue / unit）全部来自后端真实 `matchDetails`。
- `buildFactorsFromDetails` 仅做**字段到展示结构映射**（label/matched/note），不重算评分、不伪造因素、无固定文案冒充原因。
- 无 `matchDetails` 时诚实空态：「当前暂无详细匹配解释」，不伪造默认数据。
- `MatchScore` 0-100 直接映射百分比，`confidence = score/100` 仅用于 design-system 卡片展示（注释明确业务真值仍为 matchScore）。
- 参数表 `overflow-x-auto` 移动端横向滚动；表格列含 参数/需求值/能力值/权重/必须/得分。

运行时：M715-M01（factors=2, algorithm=weighted_v1）、M715-M02（parameterScores=2, hardFail=false）、M715-M03（matchScore=100, status=PENDING）全 ✅。

结论：**Matching Explanation = VERIFIED**（Backend Truth → Presentation Mapping，无 Frontend Recalculation）。

---

## 6. RFQ UX Verification（C/D. Buyer & Supplier RFQ Experience）

### 6.1 Buyer RFQ（`apps/web/src/app/workspace/rfqs/[id]/page.tsx` + `RFQDetail.tsx`）

需求身份 / 需求描述 / 需求分类 / 技术参数（复用 `DemandParameters`）/ 匹配来源 / 目标组织 / 当前状态 / 响应列表 / 生命周期 / 下一步动作（发布/关闭/审核响应）信息层级完整；响应状态（SUBMITTED/VIEWED/ACCEPTED/REJECTED）中文徽章 + 颜色。

### 6.2 Supplier RFQ（`apps/web/src/app/workspace/supplier/rfqs/[id]/page.tsx`）

- **为什么收到**：新增「匹配来源」区块（sourceMatch：匹配产品 / 分类 / 匹配得分 / 匹配状态）——仅 RFQ 目标组织可见（后端角色投影保证，M715-R02 验证）。
- **如何响应**：响应表单新增「关联报价（可选）」下拉，加载供应商自有 Offer 列表（`getOffers({ organizationId })`），选项展示 能力 / 型号 / 状态 / 报价；无 Offer 时诚实提示「当前暂无可用报价，可先前往创建报价」。
- **已有响应**：显示既有响应状态 + 提交时间 + 响应说明，避免重复提交（409 文案提示）。
- 需求描述长文本 `whitespace-pre-wrap`，组织信息诚实展示（无组织详情时不扩展 API）。

运行时：M715-R01（demand context + parameters=2）、M715-R02（sourceMatch 仅目标组织）、M715-R03（targetOrganization）、M715-X02（sourceMatch == matchId）全 ✅。

结论：**RFQ = VERIFIED**（Supplier 重点是「为什么收到 + 应如何响应」；Buyer 重点是「发给谁 + 当前进展 + 响应情况」均达成）。

---

## 7. Offer UX Verification（E. Supplier Offer Experience）

`apps/web/src/app/workspace/supplier/offers/page.tsx` + Offer 详情/编辑：能力（platformProduct.name）、能力型号（brand + modelNumber）、能力提供商（organization.name）、报价（price）、币种（currency）、状态（status + 生命周期）、创建时间完整展示。

数据面保证：

```text
Offer.productId          == SupplierProduct.platformProductId   （validateSupplierBinding 守卫）
Offer.organizationId     == SupplierProduct.organizationId      （validateSupplierBinding 守卫）
```

前端不自行判断绑定关系，仅消费后端投影。

运行时：M715-O01（offer list 200, offers=5）、M715-O02（capability/model/provider surfaced）、M715-O03（price=9988.5, currency=CNY, status=ACTIVE）全 ✅。

**角色边界强化（M31.1 新增）**：`GET /offers/:id` 增加 `JwtAuthGuard` + service 层校验（ADMIN 或所有者组织成员），非授权返回 404。M715-B01 验证：Buyer 读取供应商 Offer 详情 → 404 ✅。

结论：**Offer = VERIFIED**（未新建订单/购物车/库存/支付/价格体系/Marketplace）。

---

## 8. Supplier Inquiry Verification

仅维护既有 Supplier「收到的询价」：列表 + 详情（能力 / 能力型号 / 能力提供商 / 询价状态 / 创建时间 / 联系人）。

运行时：M715-I01（inquiry 201，product+offer+supplierProduct 上下文）、M715-I02（Supplier inquiry detail：capability + provider surfaced, status=NEW）全 ✅。

Buyer「我的询价」所有权视图：因既有 Inquiry 模型按「接收方供应商组织」归属、无 Buyer 所有权列，且禁止 schema/migration —— 保持 **Future Candidate**（非缺陷，不伪造采购方数据面）。

结论：**Inquiry = LIMITED**（Supplier 侧 VERIFIED；Buyer 所有权 = Future Candidate，设计内）。

---

## 9. Role Boundary Verification

| 角色 | 边界 | 验证 |
|------|------|------|
| Buyer | 仅自有 Demand / Match / RFQ + 被授权上下文 | M715-B01：Buyer 读供应商 Offer 详情 → 404 ✅ |
| Supplier | 组织范围内数据 + 收到的 RFQ / Inquiry / Offer | M715-R02：sourceMatch 仅目标组织可见；非目标组织无泄漏 ✅（713 R713-R02 复核） |
| Admin | 保持既有运营权限，本任务不提前进入 716 治理 | 未扩大任何权限 ✅ |

结论：**Role Boundary = VERIFIED**（未因「展示更多数据」扩大权限）。

---

## 10. Data Surface Trace

对每个修复点按链路核对：Database Field → Backend Service → Controller → DTO/Response → API Client → Frontend Type → Component → Visible UI。

| 修复点 | 链路 | 结果 |
|--------|------|------|
| Demand Category | ProductCategory(DB) → demands.service categoryId+category include → create/update DTO → demands.ts type → create/edit 页 + DemandDetail | 贯通 ✅ |
| Demand Contact | Demand.contact* (DB) → demands.service 掩码投影（visible=false → ***）→ DTO → demands.ts → DemandDetail + create 页 | 贯通 ✅ |
| Demand QuantityUnit | Demand.quantityUnit (DB) → DTO → demands.ts → edit/create 页 + Detail | 贯通 ✅ |
| Match Explanation | DemandMatch.matchDetails (DB) → matches.service → match.service.ts type → MatchDetail 页（仅映射） | 贯通 ✅ |
| RFQ SourceMatch | RFQ→Match→Product (DB) → rfqs.service findOne 角色投影 → rfqs.ts type → Supplier/Buyer RFQ 详情 | 贯通 ✅ |
| Offer Detail 边界 | Offer(DB) → offers.service findOne 权限校验 → offers.controller JwtAuthGuard → offer.service.ts type → Offer 页 | 贯通 ✅ |
| Offer↔SupplierProduct | Offer+SupplierProduct(DB) → offers.service include → type → Offer 页 | 贯通 ✅ |

不存在「Backend Rich → API Thin → Type Thin → UI Fake」链路；无 placeholder 冒充真实值、无固定文案冒充动态状态、无 score 推导不存在的业务因子。

---

## 11. Mobile Verification

代码级多断点验证（375 / 390 / 768 / 1024 / 1440）：

| 元素 | 处理 |
|------|------|
| 参数长文本 | `whitespace-pre-wrap` + `break-all`（组织 ID） |
| 参数单位 | NUMBER 值+单位同格展示 |
| 组织名称 | `flex-wrap` / 左对齐文本不截断到不可理解 |
| 状态 Badge | `flex-wrap` 徽章行 |
| 解释卡片 | `grid-cols-2 sm:grid-cols-4` / `md:grid-cols-3` / `md:grid-cols-2` 响应式 |
| RFQ / Offer 信息卡 | `grid gap-4 md:grid-cols-2` 响应式 |
| 匹配参数表 | `overflow-x-auto -mx-4 px-4` 横向滚动 |
| 按钮 / CTA | 移动端保持可见，表单控件 `w-full` |
| 横向溢出 | 核心页面无 `horizontal page overflow`，关键数据不被隐藏 |

说明：与 703/704 同口径——代码级多断点验证，浏览器视觉未跑，不伪造 E2E。

结论：**Mobile = VERIFIED**（代码级）。

---

## 12. Accessibility Verification

- 表单控件均带显式 `<label htmlFor>`（response-offer-id / response-message 等）。
- 交互按钮具备 `focus:ring-2` / `focus:outline-none` 键盘焦点可见样式。
- 图标均 `aria-hidden="true"`，语义由相邻文本承载。
- 标题层级收敛（页面 h1 → 区块 h2 → 说明 h3）。
- 状态色（成功 emerald / 失败 rose / 提示 amber / 信息 sky）对比度可接受。
- 加载/错误/空态/禁用态齐全（Loading / ErrorState / EmptyState / disabled）。
- 语义按钮使用 `<button type="button">` 避免误触发表单提交。

结论：**Accessibility = VERIFIED**（代码级）。

---

## 13. API Impact

| 项 | 状态 |
|----|------|
| 新增 Endpoint | 0（无新 endpoint / 无新 business contract / 无新 DTO semantic field） |
| Existing Endpoint Projection-Only | ✅ `GET /offers/:id`（角色边界加固，只读）+ Demand create/update/findOne/findMy 投影（categoryId / quantityUnit / contact 掩码） |
| Backward Compatible | ✅ 响应结构向后兼容，既有字段未破坏 |
| Role Safe | ✅ offer 详情新增所有者组织校验（404） |
| No Semantic Expansion | ✅ 仅投影既有实体关系，无业务语义扩展 |

结论：**API = PROJECTION-EXTENDED**（现有端点 + 现有实体关系 + 投影补全 + 向后兼容 + 角色安全）。

---

## 14. Architecture Boundary Verification

| 边界 | 状态 |
|------|------|
| Schema | **UNCHANGED**（`database/prisma/schema.prisma` 零改动） |
| Migration | **NONE** |
| Matching | **UNCHANGED**（算法/权重/参数语义/hardFail/状态机零改动；仅更准确消费现有 matchDetails） |
| Search | **UNCHANGED**（无 ranking/scope/Supplier Search/API redesign/facet redesign） |
| AI | **UNCHANGED**（无 recommendation/explanation/ranking/LLM/RAG） |
| Storage | **UNCHANGED** |
| Supplier Store / Marketplace / Transaction Engine | **NOT IMPLEMENTED** |

十项业务边界核对：Knowledge≠Score / AI≠Matching / Search≠Ranking / Analytics≠AutoOptimization / RuleEngine≠Autonomous / SupplierCapability≠Marketplace / Offer≠ProductPrice / Inquiry≠EditableTransaction / RFQ≠IndependentDemand / Visualization≠WorkflowEngine —— 全部 **MUST HOLD** 确认。

---

## 15. Safety Gate

- 无 fake data / 无假解释 / 无固定文案冒充真实状态。
- 无权限绕过展示；数据不足时诚实空态 + 下一步。
- 无 schema 扩展 / 无 migration / 无 matching / search / AI 重写。
- 未新增 M31.5+ 任务；本任务仅完成 M31.1。

---

## 16. Runtime Verification

| 项 | 结果 |
|----|------|
| PostgreSQL | 5432 运行中 ✅ |
| API | 4000（历史 dev-server 冲突已处置：停止陈旧 watcher 后重启，非代码缺陷）✅ |
| Web | 3000 Ready ✅ |
| Admin | 3001 Ready ✅ |
| `verify_715_surface.ts` | **36/36 PASS** ✅ |

运行时数据面结论：**Backend Data Surface = CONSISTENT / Frontend Data Surface = CONSISTENT**（Visible Data = Actual Backend Data）。

---

## 17. Build Verification

| 应用 | 命令 | 结果 |
|------|------|------|
| API | `pnpm --filter @visndt/api build`（nest build） | exit 0 ✅ |
| Web | `npx tsc --noEmit` | exit 0 ✅ |
| Admin | `npx tsc -b` | exit 0 ✅ |

---

## 18. Regression Verification

| 项 | 结果 |
|----|------|
| `verify_713_surface.ts`（M30.5 回归） | **40/40 PASS** ✅（含 Offer ownership guard、RFQ 读边界、Offer↔SupplierProduct、Inquiry 组织域 403、跨链 X01–X05） |
| M30 三角色回归（709，712 baseline 继承） | 本任务未改动相关业务逻辑，无回归信号 |

结论：**Regression = PASS**。

---

## 19. Finding Matrix

| 级别 | 数量 | 说明 |
|------|------|------|
| P0 | 0 | — |
| P1 | 0 | — |
| P2 | 0 | — |
| P3 | 0 | — |
| Observation | 4 | 见下 |
| Future Candidate | 5 | 见 §20（继承注册，本任务未新增 / 未实现） |

**Observation（非缺陷，不升级为必须修复）**：

- OB-715-01：Inquiry create 的 `offerId/supplierProductId` 为 transport-only（仅 create 响应透传，未持久化）——继承自 713，已如实记录，非本任务缺陷。
- OB-715-02：`GET /offers/:id` 对非授权组织返回 404（而非 403）为**有意的角色边界设计**（避免资源存在性泄漏），非缺陷。
- OB-715-03：运行时验证脚本将 Offer 状态直接置为 ACTIVE 以触发匹配探测，属**测试数据准备**（产品生命周期为 DRAFT→SUBMITTED→ACCEPTED），非业务代码行为。
- OB-715-04：矩阵行号与任务名编号口径差异（M31.1 = task 715）为既有文档口径，非缺陷。

严禁将 Observation / Future Candidate 统计为 Defect。

---

## 20. Future Candidate Register

以下为继承自 714 M31 Entry 约束的注册项，**NOT IMPLEMENTED**，本任务未新增、未转实现：

| ID | 候选 | 说明 |
|----|------|------|
| 713-FC-01 | Buyer「我的询价」所有权视图 | 需 schema/migration，本任务保持冻结 |
| 709-FC | 供应商自助创建能力型号 | self-service = Future 冻结设计 |
| M29-FC | 供应商搜索 | 已删 SupplierResultCard，无 searchSuppliers |
| 704-FC | Search V2 / AI / Vector / RAG / Analytics 自动化 | FROZEN |
| 706-FC | ProductForm slug/seoTitle/seoDescription/categoryId API DTO | Admin CRUD 待 API DTO 支持 |

---

## 21. Documentation Synchronization

| 文档 | 状态 |
|------|------|
| `docs/project-management/PROJECT_STATUS.md` | UPDATED（新增 715 条目） |
| `docs/project-management/PROJECT_ROADMAP.md` | UPDATED（新增 715 条目） |
| `docs/project-management/MODULE_COMPLETION_MATRIX.md` | UPDATED（新增 M31.1 行） |
| `docs/_review/715_M31.1_Core_User_Experience_Hardening_Report.md` | 本报告（新建） |

Code State = Documentation State = Architecture State = Roadmap State。

---

## 22. Final Decision

**M31.1 = PASS**

- Buyer Experience: **VERIFIED**（Demand Create/Edit/Detail + Parameters + Contact Visibility + Match Detail + RFQ 全收敛）
- Supplier Experience: **VERIFIED**（RFQ「为什么收到 + 如何响应」+ Offer 对象 + Inquiry 收口）
- Demand: **VERIFIED** / Matching Explanation: **VERIFIED** / RFQ: **VERIFIED** / Offer: **VERIFIED** / Inquiry: **LIMITED**（Supplier 侧完成，Buyer 所有权 = FC）
- Role Boundary: **VERIFIED**（新增 Offer 详情所有者校验）
- Backend Data Surface: **CONSISTENT** / Frontend Data Surface: **CONSISTENT**
- Mobile / Accessibility: **VERIFIED**（代码级多断点）
- Build: **PASS** / Runtime: **PASS** / Regression: **PASS**
- P0=0 / P1=0 / P2=0 / P3=0；Future Candidate=5（继承，未实现）
- 无环境冲突被误判为代码缺陷（历史 API watcher 未监听 → 处置后重启，非代码问题）

---

## 23. Final Execution Output

```text
Task:
715_M31.1_Core_User_Experience_Hardening

Status:
PASS

Buyer Experience:
VERIFIED

Supplier Experience:
VERIFIED

Demand:
VERIFIED

Matching Explanation:
VERIFIED

RFQ:
VERIFIED

Offer:
VERIFIED

Inquiry:
LIMITED

Role Boundary:
VERIFIED

Backend Data Surface:
CONSISTENT

Frontend Data Surface:
CONSISTENT

API:
PROJECTION-EXTENDED

Schema:
UNCHANGED

Migration:
NONE

Matching:
UNCHANGED

Search:
UNCHANGED

AI:
UNCHANGED

Storage:
UNCHANGED

Mobile:
VERIFIED

Accessibility:
VERIFIED

Build:
PASS

Runtime:
PASS

Regression:
PASS

Documentation:
UPDATED

Future Candidate:
5

P0:
0

P1:
0

P2:
0

P3:
0

M31.1:
PASS

Next:
716_M31.2_Admin_Data_Governance_Hardening
```

---

## Execution Principle Check

```text
M30 已打通核心链路
        ↓
M31.1 只做核心用户体验收口 ✅
        ↓
不重新设计架构 ✅
        ↓
不新增业务模型 ✅
        ↓
不扩展 Future Candidate ✅（仅继承注册，未新增/未实现）
        ↓
不新增 M31.x 任务 ✅
        ↓
完成后进入 716（本任务停止，不自行进入 716）✅
```
