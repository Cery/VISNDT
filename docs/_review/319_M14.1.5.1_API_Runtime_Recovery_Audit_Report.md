# 319_M14.1.5.1 API Runtime Recovery Audit Report

Audit Time: 2026-08-09 09:03:40
Current Branch: `main`

## 1. 根目录确认

- 仓库根目录：`F:\Desktop\VISNDT`
- 代码根目录：`F:\Desktop\VISNDT\VISNDT`
- 已确认存在：
  - `apps/web`
  - `apps/api`
  - `database`
  - `package.json`
  - `pnpm-workspace.yaml`

## 2. 当前 API 启动状态

- 前置报告 `318_M14.1.5_Supplier_Workspace_Manual_Regression_Report.md` 已确认阻塞：`localhost:4000` API unavailable，页面侧存在 `ERR_CONNECTION_REFUSED`
- 本次执行 `pnpm.cmd --filter @visndt/api dev` 后，API 未成功启动
- `localhost:4000` 未监听
- 阻塞阶段：Nest dev watch 编译阶段
- Build 结果：未单独执行 `build`，本次任务仅按指令验证 `dev` 启动；`dev` 已在 TypeScript 编译阶段失败

## 3. 启动命令

- 检查文件：`F:\Desktop\VISNDT\VISNDT\apps\api\package.json`
- 已确认脚本存在且未修改：
  - `dev`: `nest start --watch`
  - `start`: `node dist/main`
  - `build`: `nest build`
- 实际执行命令：

```bash
pnpm.cmd --filter @visndt/api dev
```

## 4. 错误日志（如有）

启动日志如下：

```text
[09:02:03] Starting compilation in watch mode...

src/organizations/organizations.service.ts:58:9 - error TS2322: Type '{ select: { members: true; }; }' is not assignable to type 'never'.

58         _count: { select: { members: true } },
           ~~~~~~

[09:02:37] Found 1 error. Watching for file changes.
```

- 直接涉及文件：
  - `F:\Desktop\VISNDT\VISNDT\apps\api\src\organizations\organizations.service.ts`
  - `F:\Desktop\VISNDT\VISNDT\database\prisma\schema.prisma`
- 代码定位：
  - `organizations.service.ts` 第 58 行使用 `_count: { select: { members: true } }`
  - `schema.prisma` 中 `Organization` 模型存在 `members OrganizationMember[]` 关系字段
- 初步判断：
  - 当前阻塞更接近 Prisma Client 类型与查询写法不匹配，或生成类型与当前 schema/代码不同步
  - 该问题发生在本地 API 编译期，尚未进入端口监听和 health 响应阶段

## 5. 环境变量检查

检查范围：

- `F:\Desktop\VISNDT\VISNDT\apps\api\.env`
- `F:\Desktop\VISNDT\VISNDT\apps\api\.env.local`
- `F:\Desktop\VISNDT\VISNDT\.env`

检查结果：

- `apps/api/.env`
  - `PORT`: 存在
  - `DATABASE_URL`: 存在
  - `JWT_SECRET`: 存在
  - `API_PREFIX`: 缺失
- `apps/api/.env.local`
  - 文件不存在
- 根目录 `.env`
  - `PORT`: 存在
  - `DATABASE_URL`: 存在
  - `JWT_SECRET`: 存在
  - `API_PREFIX`: 缺失

结论：

- 本次未发现 `PORT` / `DATABASE_URL` / `JWT_SECRET` 缺失
- `API_PREFIX` 在已存在环境文件中未找到
- 但当前主阻塞并非环境变量报错，而是编译错误先行中断启动

## 6. 是否修改文件

- 否
- 本次仅进行了目录确认、报告读取、脚本检查、环境变量检查、API 启动尝试、端口检查、health 检查和日志记录

## 7. Freeze检查

- 未修改业务逻辑
- 未修改 API 接口
- 未修改数据库结构
- 未修改 Prisma Schema
- 未修改 Auth 代码
- 未修改 Supplier Workspace 代码
- 未执行自动修复

## 8. health检查结果

执行地址：

```bash
http://localhost:4000/api/v1/health
```

检查结果：

- 请求失败：`无法连接到远程服务器`
- 端口检测：`localhost:4000` -> `TcpTestSucceeded = False`
- 原因：API 在 TypeScript 编译阶段失败，未进入正常监听状态

## 9. 下一步建议

1. 先由审核方确认是否允许进入“仅修复本地编译阻塞”的下一步处理
2. 优先核查 `apps/api/src/organizations/organizations.service.ts` 中 `_count` 查询写法是否与当前 Prisma Client 类型兼容
3. 对照 `database/prisma/schema.prisma` 与当前生成产物，确认 Prisma Client 是否存在类型不同步或生成物未更新问题
4. 若后续允许修复，再决定是否需要执行 Prisma Client 重新生成或调整该查询写法
5. 环境变量侧可补充确认 `API_PREFIX` 是否采用代码默认值；当前它不是首要阻塞项

