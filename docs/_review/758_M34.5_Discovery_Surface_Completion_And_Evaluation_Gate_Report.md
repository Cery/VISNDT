# 758 — M34.5 Discovery Surface Completion And Evaluation Gate — Report

| Field | Value |
|-------|-------|
| Task ID | `758_M34.5_Discovery_Surface_Completion_And_Evaluation_Gate` |
| Task Status | **CONDITIONAL PASS** |
| Repository Root | `F:\Desktop\VISNDT` |
| Code Root | `F:\Desktop\VISNDT\VISNDT` |
| Branch | `main` |
| Commit | `ff03a9a` |
| Working Tree | OTHER（758 仅新增/修改 8 个前端文件，其余为 Pre-existing 累积） |
| Related | M33=CLOSED · M34.0–M34.4R（CONDITIONAL/COMPLETE）· M34.5=CONDITIONAL PASS · 758=CURRENT · M34.6=READY FOR INDEPENDENT AUTHORIZATION/NOT AUTHORIZED · M34.7·M34-FINAL=NOT AUTHORIZED / NOT STARTED |

---

## 1. Task Summary

758 是 **M34.5 之后的 Discovery Completion Gate**：在 757（Canonical Discovery IA）基础上，**补齐真正的能力导向、供应商导向发现面与跨实体发现连续性**，构成 B1–B7 七项 Discovery Completion Gate；仅当 Gate 达成，M34.6（Evaluation + Connection）才进入 **READY FOR INDEPENDENT AUTHORIZATION**，但**不得由此自动授权执行**，任务完成后必须 **STOP**。

758 的定位 = **Discovery Surface Completion + Evaluation Entry Gate**，非 Evaluation、非 Connection、非视觉改造。

---

## 2. Repository Verification

- 仓库根：`F:\Desktop\VISNDT`；代码根 `F:\Desktop\VISNDT\VISNDT`
- Branch：`main`；HEAD commit：`ff03a9a`
- 历史报告 **746–757 Preserved**，未修改、未覆盖
- 工作区存在海量 Pre-existing Modified/Untracked（M31/M33/M34.1-4/756/757 累积，含 admin/web、capability-/supplier-context、css、`apps/api/src/search/search.service.ts`），均已与 758 区分（见 §3）
- 唯一后端改动文件 `apps/api/src/search/search.service.ts` = **Pre-existing（755 任务一等提供 `suppliers` 组）**，758 **未触碰后端**

---

## 3. Change Attribution（Pre-existing vs 758）

### Pre-existing（不归因 758，保持原状）
- `apps/api/src/search/search.service.ts`：755（M34.4）已实现 `searchSuppliers()`，聚合 **PUBLISHED SupplierProduct → Organization(type=SUPPLIER)** 产出 `suppliers` 组（search.service.ts L529-602）；controller `return { ...result, supplierProductFacets }`（L85）全量返回含 `suppliers`。此为 **758 供应商发现面的数据源（已存在，非本次新建）**。
- 其余 admin/web/css/design-tokens/capability-context/supplier-context 等改动 = 历史累积。

### 758（本次唯一变更，全为前端展示层）
| # | 文件 | 变更 |
|---|------|------|
| 1 | `apps/web/src/lib/api/search.ts` | 新增 `SupplierDiscoveryItem` 接口 + 在 `UnifiedDiscoveryResponse` 增加 `suppliers: EntitySearchGroup<SupplierDiscoveryItem>`（字段名与后端 JSON `suppliers` 对齐） |
| 2 | `apps/web/src/services/search.service.ts` | `SearchDomain` 增加 `'supplier'`；新增 `SupplierSearchResult` 接口；新增 `mapSupplier()`；`UnifiedSearchResults` 增加 `suppliers`；`unifiedSearch()` 消费后端 `suppliers` 组 |
| 3 | `apps/web/src/components/search/SupplierResultCard.tsx`（**新增**） | 供应商结果卡 → `Link /suppliers/${organizationId}`，展示已发布能力型号数 + 能力/系列 chips |
| 4 | `apps/web/src/app/search/SearchPageContent.tsx` | VALID_TYPES 增加 `supplier`；渲染"供应商"结果区（shouldRenderSection('supplier',…)）；counts/hasAnyResults/hasMore/header-total/trackEvent/SearchResultSummary 纳入 suppliers；append 合并 suppliers |
| 5 | `apps/web/src/components/search/SearchTypeTabs.tsx` | 新增"供应商"Tab（ALL/检测能力/方案/能力型号/**供应商**/知识） |
| 6 | `apps/web/src/components/search/SupplierProductResultCard.tsx` | 能力提供商名称由纯文本改为 `Link /suppliers/${organization.id}`（Product/SupplierModel→Supplier 跳转） |
| 7 | `apps/web/src/components/products/ProductDetailContent.tsx` | 能力提供商名称由纯文本改为 `Link /suppliers/${supplier.organizationId}`（Product→Supplier 跳转） |
| 8 | `apps/web/src/components/search/GlobalSearchBar.tsx` | 类型下拉增加"供应商" |

Scope：**Frontend-Only · Display-Layer Only · 复用后端既有 `suppliers` 组数据源 · 无新路由 · 无 Schema · 无 Migration · 无 API 修改**。

---

## 4. Current Discovery Surface Inventory

| Surface | 入口 | 状态 | 说明 |
|---------|------|------|------|
| **Search** | `/search` | **Canonical**（B1） | ALL/产品/方案/能力型号/**供应商**/知识，多实体统一发现 |
| **Categories** | `/categories` + `/products?categoryId=` | **Canonical**（B2） | 能力分类，Category→Product 上下文保留 |
| **Capabilities** | `/products`（能力注册表）+ `/products/[slug]` | **Canonical**（B3） | Product=Capability Authority（Semantic Role） |
| **Products** | `/products` 列表 + `/products/[slug]` | **Canonical** | 平台权威对象 + 参数筛选 |
| **Suppliers** | `/search?type=supplier` + `/suppliers/[id]` | **Canonical（本次补齐）**（B4） | 供应商列表（PUBLISHED SupplierProduct 聚合）+ 供应商详情 |
| （遗留）`/supplier-models` | — | Legacy / Future Candidate | 静态 207B 重定向页，未虚构收敛 |

> 备注：`/suppliers/[id]` 详情页内部"供应能力"区仍基于 Offer（Pre-existing legacy，非本次范围）。**Supplier List Surface 已由 PUBLISHED SupplierProduct 数据源支撑（无 Offer 依赖）**，供应商详情页 Offer 展示 = LEGACY，标记为 M34.6/Governance Future Candidate，不改写。

---

## 5. Actual Implementation

逐项（File / Before / After / Reason / Scope / Architecture Impact）：

| File | Before | After | Reason | Scope | Architecture Impact |
|------|--------|-------|--------|-------|---------------------|
| `lib/api/search.ts` | DTO 无 suppliers 字段 → 后端 `suppliers` 组被丢弃 | DTO 增加 `SupplierDiscoveryItem` + `suppliers` 组 | 打通供应商列表数据链路 | Frontend type | NONE |
| `services/search.service.ts` | 无 supplier domain / mapping | 增加 `supplier` domain + `SupplierSearchResult` + `mapSupplier` + `unifiedSearch` 映射 | 前端消费后端供应商组 | Frontend service | NONE |
| `SupplierResultCard.tsx`（新） | 不存在 | 新增供应商结果卡（→`/suppliers/[id]`） | 建立 Public Supplier List 展示件 | Frontend component | NONE（Organization 语义角色，非新 Entity） |
| `SearchPageContent.tsx` | 无 supplier type/区 | 增加 supplier type + 供应商结果区 + 计数 | 落地供应商发现面 UI | Frontend page | NONE |
| `SearchTypeTabs.tsx` | 无供应商 Tab | 增加"供应商"Tab | 供应商发现入口 | Frontend component | NONE |
| `SupplierProductResultCard.tsx` | 供应商名为文本 | 供应商名 →`/suppliers/[orgId]`链接 | Product→Supplier 连续性 | Frontend component | NONE |
| `ProductDetailContent.tsx` | 供应商名为文本 | 供应商名 →`/suppliers/[orgId]`链接 | Product→Supplier 连续性 | Frontend component | NONE |
| `GlobalSearchBar.tsx` | 类型下拉无供应商 | 增加"供应商" | 全局搜索供应商入口 | Frontend component | NONE |

---

## 6. Capability Surface

- **Capability-oriented Surface = Product Semantic Role**（非新 Entity）：`capability-context.ts` 明确 Product=Capability Authority，identity=`product.id`。能力导向发现 = `/categories`（能力分类）→ `/products?categoryId=`（能力注册表）→ `/products/[slug]`（能力详情 = 参数 + 能力档案 + 供应商关系），全部复用既有 Product 对象，**无 Capability 表 / Model / FK**。

---

## 7. Supplier Surface

- **Supplier = Organization(type=SUPPLIER)** 语义角色（`supplier-context.ts`），非新 Entity / Store Owner / Marketplace Seller。
- 公开供应商面数据源 = **PUBLISHED SupplierProduct → Organization**（`searchSuppliers()` L534 `status: PUBLISHED, organization.type='SUPPLIER'`），**无 Offer / RFQ / Transaction 依赖**。
- 758 落地：`/search?type=supplier` 供应商列表（`SupplierResultCard`）+ `/suppliers/[id]` 详情，构成 **Discovery → Supplier → Published SupplyProducts → Product** 路径。

---

## 8. Cross-Entity Discovery Matrix

| 路径 | 结构 | 验证 |
|------|------|------|
| Search → Product | `/search` ProductResultCard → `/products/[slug]` | VERIFIED·STRUCTURAL |
| Search → Supplier / Context | `/search` ProductResultCard / SupplierResultCard → `/suppliers/[id]`；SupplierModelFacetPanel 上下文 | VERIFIED·STRUCTURAL |
| Category → Product | `/categories|/products` → `/products?categoryId=` | VERIFIED·STRUCTURAL |
| Capability → Product | `/products?categoryId=` → `/products/[slug]`（能力详情） | VERIFIED·STRUCTURAL |
| Product → Supplier | `ProductDetailContent` 能力提供商 / `SupplierProductResultCard` → `/suppliers/[orgId]`（**758 修复链接**） | VERIFIED·STRUCTURAL |
| Supplier → Product | `/suppliers/[id]` 详情 → 供应能力区（**当前依赖 Offer=LEGACY**）；列表侧 → 类型页 | STRUCTURAL / LEGACY，见 §4 |

---

## 9. Runtime Evidence（分层）

- **Route Reachability**：`/search?type=supplier` 接入 VALID_TYPES + 渲染区（编译通过）；后端 `/search` 返回 `suppliers` 组（controller `{...result}`）；路由存在性 = VERIFIED
- **Navigation Wiring**：GlobalSearchBar 类型下拉 + SearchTypeTabs + PublicHeader（757 已加 `/search?type=supplier-product`）→ supplier 入口 = VERIFIED·STRUCTURAL
- **Interaction Evidence**：SupplierResultCard/SupplierProductResultCard/ProductDetailContent 链接 → `/suppliers/[id]` = 结构验证通过（需 build + 运行时）
- **Real Data Population**：**UNVERIFIED**（product=0 / supplier_product=0 / offer=0，合法空数据，无法做真实数据运行时遍历）

---

## 10. Empty Data

- `product=0`、`supplier_product=0`、`offer=0`（沿用 756/757 数据基线对账）。
- 空数据合法且不影响结构/接线验证；**Real Product Population / Real Supplier Population / Real Capability Population = UNVERIFIED**。
- `Empty Data ≠ Search Failure`、`Structural Verification ≠ Real-data Verification`。

---

## 11. Mobile

- **Responsive Structure**：新增 SupplierResultCard 采用响应式 grid（sm:2 / lg:3），`/search?type=supplier` 复用现有搜索页响应式布局；主导航计数不变 = STRUCTURAL。
- **Browser Runtime**：本次前端改动 + build 通过，未做逐视口视觉重审计（受 No-Visual-Loop Gate 约束，未扩大为视觉项目）。
- **Historical Overflow**：768 ≈140px 历史溢出 = **CARRY FORWARD → Future Candidate**（非本次引入，未重构）。

---

## 12. Static

| 检项 | 结果 |
|------|------|
| `pnpm --filter @visndt/api build` | **exit 0** ✅ |
| `pnpm --filter @visndt/web lint` | **exit 0**（仅存量 warnings，无新增 Error）✅ |
| `pnpm --filter @visndt/web build` | **exit 0**（`/search`、`/suppliers/[id]` 路由正常生成）✅ |
| `pnpm --filter @visndt/api lint` | **PRE-EXISTING TOOLING GAP**（apps/api 无 `eslint.config.*`，eslint@9 flat-config 必需；lint 脚本含 `--fix` 会改写，非功能性 lint gate）。**按要求不得写成 PASS** |

---

## 13. Regression

- **Auth / RBAC / Demand / Matching / RFQ / RFQResponse / Inquiry**：758 未触碰任何相关文件，行为无变化。
- **Product / SupplierProduct / Search / Category / Workspace**：仅新增前端展示/接线，未改业务逻辑；后端 `apps/api/src/search/search.service.ts` 为 Pre-existing（755），758 未改。
- 结论：**NO BEHAVIOR CHANGE（无未授权变化）**。

---

## 14. Discovery Completion Gate（B1–B7）

| Gate | 状态 | 证据 |
|------|------|------|
| **B1 Search** | **PASS** | 统一 `/search` 多类型发现成立 |
| **B2 Category** | **PASS** | `/categories` + `/products?categoryId=` 成立 |
| **B3 Capability** | **PASS** | `/products` 能力注册表 + Product=Capability Authority |
| **B4 Supplier** | **PASS** | `/search?type=supplier` 供应商列表（PUBLISHED SupplierProduct 聚合，无 Offer 依赖）+ `/suppliers/[id]` 详情（**本次补齐**） |
| **B5 Cross-Entity** | **PASS（STRUCTURAL）** | Product→Supplier 链接修复；Search/Category/Capability/Product/Supplier 路径接线完整 |
| **B6 Continuity** | **PASS（STRUCTURAL）** | 搜索供应商 Tab + 全局搜索下拉 + 导航入口 + 计数联动；真实数据遍历=UNVERIFIED |
| **B7 Authority** | **PASS** | ONE 权威集（Product=Canonical，Supplier=Organization(type=SUPPLIER)，Specification=ParameterDefinition）保持，无第二套 authority |

> Rich 维度说明：B1–B4（Entity Surface）、B5（Cross-Entity）、B6（Continuity）、B7（Authority）**结构证据充足且无架构违规**；Real-data runtime 因合法空数据无法验证 → 依据 §14 Final Decision Logic，不得升级为 PASS，采用 **CONDITIONAL PASS**。

---

## 15. M34.6 Authorization Recommendation

- 依据 §10：Gate B1–B7 **全部达成（结构性）** → **M34.6 = READY FOR INDEPENDENT AUTHORIZATION**。
- **但**：真实数据为空，Real-data runtime 未验证；故 M34.6 状态 = **READY FOR INDEPENDENT AUTHORIZATION（NOT AUTHORIZED）**，**不得由本任务直接授权 M34.6**，须独立任务指令在（真实/测试）数据就绪情况下正式授权。

---

## 16. Roadmap State

| 节点 | 状态 |
|------|------|
| M34.0 | CONDITIONAL |
| M34.1 | COMPLETE / CONDITIONAL |
| M34.2 | COMPLETE / CONDITIONAL |
| M34.3 | COMPLETE / CONDITIONAL |
| M34.4 | CONDITIONAL PASS |
| M34.4R | CONDITIONAL PASS |
| M34.5 | CONDITIONAL PASS |
| **758** | **CONDITIONAL PASS（DISCOVERY COMPLETION GATE）** |
| M34.6 | NOT AUTHORIZED / READY FOR INDEPENDENT AUTHORIZATION |
| M34.7 | NOT AUTHORIZED |
| M34-FINAL | NOT STARTED |

**M34 平台化路线**：ENTITY → DISCOVERY → **EVALUATION（M34.6：Compare→Shortlist→Inquiry→RFQ→Buyer↔Supplier）** → CONNECTION → GOVERNANCE/DISCOVERABILITY（M34.7：Taxonomy/Parameter/Supplier-Product Governance + SEO/LLM + Mobile；承接 TC713/TC715/TC716、Category redundancy、Parameter duplicate groups、°C/℃、STRING/NUMBER、768 overflow、SITE_URL）。

---

## 17. Final Decision

> **PASS 需 B1–B7 全成立 + Runtime Navigation Evidence 完整（含真实数据）+ 无 defect + 无架构违规**；Real-data population=0 → Runtime Navigation 非完整 → **不满足 PASS**。

**CONDITIONAL PASS**：

- ✅ 核心 Discovery Surface 已建立（Search/Category/Capability/Product/Supplier）
- ✅ 结构证据充分（B1–B7）
- ✅ 无架构违规（Boundary/Authority/Schema/Migration/无 marketplace）
- ⚠️ 条件：Real Data 因合法空数据（product=0/supplier_product=0/offer=0）无法验证 → Real-data runtime=UNVERIFIED

**Final Execution Output**：

```
Task ID:            758_M34.5_Discovery_Surface_Completion_And_Evaluation_Gate
Task Status:        CONDITIONAL PASS
Repository Root:    F:\Desktop\VISNDT
Code Root:          F:\Desktop\VISNDT\VISNDT
Branch:             main
Commit:             ff03a9a
Working Tree:       OTHER
M33:                CLOSED
M34.0:              CONDITIONAL
M34.1:              COMPLETE / CONDITIONAL
M34.2:              COMPLETE / CONDITIONAL
M34.3:              COMPLETE / CONDITIONAL
M34.4:              CONDITIONAL PASS
M34.4R:             CONDITIONAL PASS
M34.5:              CONDITIONAL PASS
758:                CONDITIONAL PASS
M34.6:              NOT AUTHORIZED / READY FOR INDEPENDENT AUTHORIZATION
M34.7:              NOT AUTHORIZED
M34-FINAL:          NOT STARTED

Search Surface:             Canonical
Category Surface:           Canonical
Capability-oriented Surface: Canonical
Product Surface:            Canonical
Supplier Surface:           Canonical（本次补齐）
Cross-Entity Discovery:     STRUCTURAL VERIFIED
Discovery Continuity:       STRUCTURAL VERIFIED
Public / Workspace Boundary: VERIFIED

B1 Search:                  PASS
B2 Category:                PASS
B3 Capability:              PASS
B4 Supplier:                PASS
B5 Cross-Entity:            PASS（STRUCTURAL）
B6 Continuity:              PASS（STRUCTURAL）
B7 Authority:               PASS

M34.6 Recommendation:       READY FOR INDEPENDENT AUTHORIZATION
Real Product Population:    UNVERIFIED（product=0）
Real Supplier Population:   UNVERIFIED（supplier_product=0）
Real Capability Population: UNVERIFIED（无真实 Capability 数据）
Schema:                     NO CHANGE
Migration:                  NONE
Database Mutation:          NONE
Test Data Creation:         NONE
AI / Vector / RAG / Semantic Search: NONE
Matching:                   NO CHANGE
RFQ:                        NO CHANGE
Transaction:                NONE
Mobile:                     STRUCTURAL
768 Overflow:               CARRY FORWARD
Documentation:              UPDATED
Review Report:              docs/_review/758_M34.5_Discovery_Surface_Completion_And_Evaluation_Gate_Report.md
```

---

## 18. M34.6 Future Scope Freeze（冻结，不实施）

**M34.6 Evaluation + Connection Workflow** 核心 = Discovery → Compare → Shortlist → Inquiry → RFQ → Buyer↔Supplier；**复用现有 Demand / RFQ / RFQResponse / Inquiry**，禁止重新设计交易系统。具体实现仍待 **Independent M34.6 Authorization**，本任务不得实施 Compare/Shortlist/Inquiry/RFQ/Buyer-Supplier workflow 任何部分。

## 19. M34.7 Future Scope Freeze（冻结，不实施）

**M34.7** = Platform Governance + SEO/LLM + Mobile：Taxonomy/Parameter/Supplier-Product Governance、SEO、Structured Data、LLM Discoverability、Mobile、Accessibility、Runtime；承接 TC713/TC715/TC716、Category redundancy、Parameter duplicate groups、°C/℃、STRING/NUMBER、768 overflow、SITE_URL。**不得提前实施**。

---

## 20. Mandatory STOP

758 已完成，**必须 STOP**，**不得自动执行** M34.6 Compare / Shortlist / Inquiry / RFQ / Buyer-Supplier workflow。即使 M34.6 = READY FOR INDEPENDENT AUTHORIZATION，也**只可提交独立授权**，后续所有工作须新的独立任务指令。