# 321_M14.1.5.3 Supplier Workspace Manual Regression Retry Report

Audit Time: 2026-08-09 10:10:28
Current Branch: `main`

## 1. 环境确认

- 仓库根目录：`F:\Desktop\VISNDT`
- 代码根目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 前端访问检查：`http://localhost:3000/login` 返回 `200`
- 本次任务原始性质：`Verification Only`

本次执行过程中发生两类经用户批准的最小例外，落地为以下 3 项最小数据操作，用于补齐回归前置数据：

1. 创建 1 个供应商组织：`SZ Wise Supplier`
2. 创建 1 个供应商账号：`vsndt@sz-wise.cn`
3. 复用既有 Demand `a178b89f-f282-482c-847d-64387ffd0749`，生成并发布 1 条 RFQ：`3f00bc92-a8e5-4905-8004-0e409f83dc8c`

说明：

- 例外发生原因：环境中原本不存在可登录 supplier 用户，且不存在可供 supplier 回归的开放 RFQ
- 本次未修改业务代码、未修改 API、未修改 Prisma、未修改权限
- 本次新增文件仅为本报告

## 2. API health结果

验证地址：

```text
http://localhost:4000/api/v1/health
```

结果：

```json
{"status":"ok","database":"connected"}
```

结论：

- API 已恢复
- 数据库连接状态正常
- 前置条件满足

## 3. 登录结果

验证账号：

```text
vsndt@sz-wise.cn / admin123456
```

验证结果：

- 登录成功
- 登录后进入 `/dashboard`
- `GET /auth/me` 返回 `200`
- `workspaceRole` = `SUPPLIER`
- `organizationId` = `5538dbde-8a84-41e9-a74a-49c9fbc218b2`

## 4. 页面验证矩阵

| 验证项 | 页面/路径 | 结果 | 观察 |
| --- | --- | --- | --- |
| 登录 | `/login` | 通过 | supplier 账号可成功登录并进入已登录态 |
| Supplier Workspace 首页 | `/workspace/supplier` | 通过 | 页面正常加载，Supplier Workspace 入口页可见 |
| RFQ 列表 | `/workspace/supplier/rfqs` | 通过 | 成功加载 1 条开放 RFQ；页面存在“查看详情”入口 |
| RFQ 详情 | `/workspace/supplier/rfqs/3f00bc92-a8e5-4905-8004-0e409f83dc8c` | 通过 | 页面正常加载；显示 Demand 信息；响应表单可见 |
| 提交 Response 成功态 | RFQ 详情页 | 通过 | 提交后显示“响应提交成功”；并提示同一组织对同一 RFQ 仅允许一次响应 |
| 提交 Response 错误态 | 同一 POST 接口二次提交 | 通过 | 使用同一 supplier 会话补齐 CSRF 后重放请求，返回 `409`，符合“重复提交”业务限制 |
| Response 列表 | `/workspace/supplier/responses` | 通过 | 成功展示 1 条响应记录，状态为 `SUBMITTED` |

补充说明：

- 首次成功提交后，详情页前端会将表单替换为成功提示，因此重复提交错误态无法再通过同一页面按钮直接触发
- 为验证该错误态，使用同一 supplier 会话按前端同样的 CSRF 约束重放 `POST /rfqs/:id/responses`，返回 `409`

## 5. API验证矩阵

浏览器 Network 已确认以下目标请求均被触发：

| 接口 | 结果 | 说明 |
| --- | --- | --- |
| `GET /auth/me` | `200` | 已登录 supplier 身份确认成功 |
| `GET /rfqs/available` | `200` | 返回 1 条 `OPEN` RFQ |
| `GET /rfqs/:id` | `200` | RFQ 详情成功加载 |
| `POST /rfqs/:id/responses` | 成功链路已命中；重复提交返回 `409` | 浏览器成功提交后生成 response；同会话二次提交返回 `409` |
| `GET /rfq-responses/mine` | `200` | 返回当前 supplier 组织的 1 条响应记录 |

浏览器 Network 日志文件：

```text
c:\Users\ws_tj\AppData\Local\Temp\trae\browser-logs\network-2026-08-09T02-06-21-470Z.log
```

补充验证数据：

- 响应记录 ID：`dc819903-0bf3-423b-bc0c-8bd7aab9941b`
- 响应状态：`SUBMITTED`
- 响应消息：`Manual regression retry response from supplier workspace.`

## 6. 317修复项验证

对 `317_M14.1.4.1_Supplier_Workspace_Navigation_Status_Stabilization_Report.md` 的两项修复，本次人工回归结果如下：

1. RFQ 列表页详情入口
   - 已验证 `/workspace/supplier/rfqs` 页面出现“查看详情”入口
   - RFQ 详情页可成功打开并完成后续响应提交流程

2. Response 状态展示语义
   - 已验证 `/workspace/supplier/responses` 页面展示响应记录
   - 当前页面展示 `SUBMITTED` 响应状态，符合 Response 语义而非 RFQ 语义
   - 本次回归未发现 317 所修复的“状态徽章语义错用”回归现象

## 7. Build结果

执行目录：

```text
F:\Desktop\VISNDT\VISNDT
```

执行结果：

1. `pnpm.cmd --filter @visndt/api build`
   - 结果：成功
   - 退出码：`0`

2. `pnpm.cmd --filter @visndt/web build`
   - 结果：失败
   - 退出码：`1`
   - 关键日志：

```text
ESLint: Cannot find package '@eslint/eslintrc' imported from apps/web/eslint.config.mjs
Build error occurred
[Error: spawn UNKNOWN]
```

结论：

- API build 通过
- Web build 在当前本地环境未通过，因此本次验证不是全绿构建状态

## 8. 修改与影响汇总

修改文件：

- `F:\Desktop\VISNDT\docs\_review\321_M14.1.5.3_Supplier_Workspace_Manual_Regression_Retry_Report.md`

修改原因：

- 记录 `M14.1.5.3 Supplier Workspace Manual Regression Retry` 在 API 恢复后的人工回归结果
- 归档经用户批准的最小测试数据例外，确保回归前置条件与结论可追溯
- 按项目规则补齐本次任务的验证、构建与冻结说明

架构影响：

- 无业务架构变更
- 无前端页面层、Service 层、API lib 分层调整
- 无后端模块边界、权限体系、Workspace 鉴权链路调整

数据库影响：

- 未修改 Prisma Schema
- 未执行 migration
- 未变更表结构、索引、约束
- 经用户批准，写入最小测试数据用于回归验证：新增 1 个 supplier 组织、1 个 supplier 账号、1 条已发布 RFQ；并在人工回归成功提交后生成 1 条 response 业务记录

API影响：

- 未修改 API 实现
- 未新增、删除或调整接口
- 未变更接口入参、出参或状态码契约
- 本次仅对既有接口执行连通性与业务行为验证，包括 `GET /auth/me`、`GET /rfqs/available`、`GET /rfqs/:id`、`POST /rfqs/:id/responses`、`GET /rfq-responses/mine`

Review报告路径和编号：

- 报告路径：`F:\Desktop\VISNDT\docs\_review\321_M14.1.5.3_Supplier_Workspace_Manual_Regression_Retry_Report.md`
- 报告编号：`321`
- 编号位置：位于 `docs/_review/` 序列中，用于承接本次 `M14.1.5.3` 回归重试任务归档

## 9. Freeze检查

本次任务期间确认如下：

- 未修改业务代码
- 未修改 API 实现
- 未修改 Prisma Schema
- 未执行 migration
- 未修改权限体系
- 未新增未经批准功能

例外说明：

- 经用户批准，执行了最小数据库写入，仅用于补齐 supplier 回归前置数据
- 本次文件层面的新增仅为：
  - `F:\Desktop\VISNDT\docs\_review\321_M14.1.5.3_Supplier_Workspace_Manual_Regression_Retry_Report.md`

工作区补充观察：

- 仓库中存在既有未提交改动，属于任务开始前已存在状态
- 本次未对这些既有代码改动做额外修改

## 10. 最终结论

本次 `M14.1.5.3 Supplier Workspace Manual Regression Retry` 在 API 恢复后已完成核心人工回归：

- supplier 登录通过
- Supplier Workspace 首页通过
- RFQ 列表加载通过
- RFQ 详情加载通过
- Response 成功提交通过
- 重复提交错误态通过（`409`）
- Response 列表展示通过
- 317 修复项本次回归未见回退

但本次结论为“有条件通过”：

1. 回归链路通过的前提是执行了经批准的最小测试数据例外
2. `@visndt/web build` 在当前本地环境失败，构建面未达到全绿

因此，本次任务可结论为：

**Supplier Workspace 人工回归主链路通过，API 恢复后的业务验证成立；但需同时记录“最小测试数据例外”与“Web build 未通过”两项事实。**
