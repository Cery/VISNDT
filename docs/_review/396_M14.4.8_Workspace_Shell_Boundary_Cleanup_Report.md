# M14.4.8 Workspace Shell Boundary Cleanup Report

## 1. Execution Context

- Repository Root: `F:\Desktop\VISNDT`
- Code Root: `F:\Desktop\VISNDT\VISNDT`
- Branch: `main`

## 2. Modified Files

- `VISNDT/apps/web/src/components/workspace/WorkspaceSidebar.tsx`
- `docs/_review/396_M14.4.8_Workspace_Shell_Boundary_Cleanup_Report.md`

## 3. WorkspaceSidebar Boundary Change

修改前：

- `WorkspaceSidebar.tsx`
  -> `getUnreadCount()`
  -> `@/lib/api/notifications`
  -> Notification API

修改后：

- `WorkspaceSidebar.tsx`
  - 仅负责：
    - `workspaceRole` 导航选择
    - active route 判断
    - 静态导航入口渲染
  - 不再负责：
    - unread count 状态管理
    - 通知数据加载
    - Notification API 调用

补充说明：

- 通知入口 `/workspace/notifications` 仍保留在 Sidebar 导航中
- 本次未删除通知能力代码
- 本次未修改通知页面
- 本次未修改通知 API 或任何 API contract

## 4. Component Responsibility Audit

### WorkspaceHeader

职责确认：

- 当前用户信息展示
- workspace 标题展示
- logout 入口

审计结论：

- 未发现业务 service 调用
- 未发现 domain 数据聚合

### WorkspaceSidebar

职责确认：

- 基于 `workspaceRole` 选择导航
- route active 状态判断
- workspace 导航入口展示

审计结论：

- 本次已移除 `getUnreadCount()` 调用
- 当前不再直接调用业务 service 或 API client
- 未发现 domain 数据聚合

### StatCard

职责确认：

- 纯展示组件
- 仅接收 `label / value / description / icon` 进行渲染

审计结论：

- 未发现业务请求
- 未发现 service 调用
- 未发现聚合逻辑

组件职责审计总结：

- `apps/web/src/components/workspace/` 中本次检查到的明显边界问题仅为 `WorkspaceSidebar` 的通知读取职责
- 收口后，`WorkspaceHeader`、`WorkspaceSidebar`、`StatCard` 均符合 shell / presentation 边界

## 5. Route Responsibility Audit

- `/dashboard`
  - 职责：统一入口 + role redirect
  - 当前实现：根据 `user.workspaceRole` 分流到 `/dashboard/buyer` 或 `/dashboard/supplier`，未知角色回退到 `/workspace`
- `/workspace`
  - 职责：导航入口
  - 当前实现：仅展示 Buyer / Supplier 入口导航，不承担 Dashboard summary 或业务聚合
- `/workspace/dashboard`
  - 职责：兼容 redirect
  - 当前实现：`redirect('/dashboard')`
- `/workspace/supplier`
  - 职责：Supplier workspace shell
  - 当前实现：保留 Supplier shell 骨架与角色守卫，不新增业务数据聚合

审计结论：

- 本次未改变任何现有路由行为
- 本次未重新设计路由
- 当前路由职责与 M14.4.7 冻结报告要求一致

## 6. Freeze Check

- Backend: `No`
- Database: `No`
- API: `No`
- Auth: `No`
- RBAC: `No`
- Domain Logic: `No`

补充说明：

- 未修改 `apps/api/`
- 未修改 `database/`
- 未修改 `schema.prisma`
- 未修改任何 migration
- 未修改 `AuthProvider`
- 未修改 `AuthGuard`
- 未修改 `RoleGuard`
- 未新增任何 Domain API 调用

## 7. Validation

执行目录：

- `F:\Desktop\VISNDT\VISNDT\apps\web`

执行结果：

- `pnpm exec tsc --noEmit`
  - 结果：`PASS`
- `pnpm build`
  - 结果：`PASS`

构建附注：

- 仍存在既有 warning：
  - `src/app/layout.tsx` custom font warning
  - `src/components/products/ManufacturerInfo.tsx` unused var warning
- 上述 warning 不由本次 Workspace shell boundary cleanup 引入

## 8. Risks

1. Workspace shell ownership 已明显收口，但 `/workspace/supplier` 仍作为独立 Supplier shell 存在，后续仍需持续依赖文档来约束其不演化为平行 Dashboard。
2. Git 工作树当前并非 clean；存在本任务之外的 M14.4 前端与文档改动，本次报告仅记录实际由本任务触碰的文件。
3. Next.js 构建仍带两条既有 warning，虽不阻塞本次任务，但仍属于后续发布整洁度风险。

## 9. Final Status

PASS WITH RISKS
