# M14.4.6 Dashboard Entry Normalization Report

## 1. Execution Context

- Repository Root: `F:\Desktop\VISNDT`
- Code Root: `F:\Desktop\VISNDT\VISNDT`
- Branch: `main`

## 2. Modified Files

- `VISNDT/apps/web/src/app/dashboard/page.tsx`
- `VISNDT/apps/web/src/app/workspace/dashboard/page.tsx`
- `VISNDT/apps/web/src/app/workspace/page.tsx`

## 3. Dashboard Entry Change

修改前：

- `/dashboard` -> `/workspace/dashboard`
- `/workspace/dashboard` -> `/dashboard/buyer`
- 最终固定进入 Buyer Dashboard

修改后：

- `/dashboard` -> 读取 `AuthProvider` 中的 `user.workspaceRole`
- `BUYER` -> `/dashboard/buyer`
- `SUPPLIER` -> `/dashboard/supplier`
- 未知角色 -> `/workspace`

说明：

- 未复制任何 Dashboard UI
- 未新增任何 Dashboard 页面
- `apps/web/src/app/workspace/dashboard/page.tsx` 保留为兼容入口，但仅重定向到 `/dashboard`

## 4. Workspace Boundary Review

审查文件：`apps/web/src/app/workspace/page.tsx`

审查结论：

- 修改前，该页面仍承担 summary-style 首页职责：
  - 使用 `demand.service`
  - 使用 `rfq.service`
  - 展示统计卡片、最近需求、最近询价
- 修改后，该页面仅保留 Workspace 入口与导航职责：
  - 展示当前 `workspaceRole`
  - 提供 Buyer / Supplier 对应入口导航
  - 不再承担 Dashboard 汇总职责
  - 不新增任何 Workspace API 聚合逻辑

## 5. Architecture Impact

- Frontend: `Yes`
  - 仅调整前端路由入口和 Workspace 页面职责边界
- Backend: `No`
- Database: `No`
- API: `No`

## 6. Freeze Check

- Backend untouched: `Yes`
- Database untouched: `Yes`
- Auth core untouched: `Yes`
- RBAC untouched: `Yes`

补充说明：

- 使用了现有 `AuthProvider` 上下文读取 `workspaceRole`
- 未修改 `apps/api/`
- 未修改 `database/`
- 未修改任何 Auth Backend / RBAC Backend / Workspace Backend

## 7. Validation

执行目录：

- `F:\Desktop\VISNDT\VISNDT\apps\web`

执行结果：

- `pnpm exec tsc --noEmit` -> `PASS`
- `pnpm build` -> `PASS`

Build 结果确认包含以下路由：

- `/dashboard`
- `/dashboard/buyer`
- `/dashboard/supplier`

附注：

- 构建过程中仍存在既有 warning：
  - `src/app/layout.tsx` custom font warning
  - `src/components/products/ManufacturerInfo.tsx` unused var warning
- 以上 warning 不属于本次任务修改范围，未在本任务中处理

## 8. Final Status

PASS
