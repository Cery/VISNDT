# 343_M14.2.4.5A_TypeContract_Fix_Report

## 1. 执行摘要

- 任务编号：`M14.2.4.5A-TypeContract-Fix`
- 任务目标：`完成 WorkspaceRole Contract 的 AuthUser 类型冻结`
- Git 仓库根目录：`F:\Desktop\VISNDT`
- 业务代码根目录：`F:\Desktop\VISNDT\VISNDT`
- 当前分支：`main`
- 本次禁止修改范围：
  - `apps/web`
  - `database`
  - `auth.service.ts`
  - `auth.controller.ts`
- 本次允许修改文件：
  1. [`VISNDT/apps/api/src/auth/dto/auth-response.dto.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts)
  2. [`VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts)

执行结论：

**本次已按要求将 `organization`、`organizationMember`、`workspaceRole` 从 optional 收敛为必含字段，并保留 `null` 作为唯一缺省语义，未新增字段，未修改 API 运行时逻辑，未修改 `workspaceRole` 映射，也未触达数据库。**

**结合 [`342_M14.2.4.5A_Final_Result_Audit_Report.md`](file:///F:/Desktop/VISNDT/docs/_review/342_M14.2.4.5A_Final_Result_Audit_Report.md) 中指出的唯一 A 类缺口，本次类型冻结修复已补齐该缺口。若仅按 A 类 `5A` 范围判断，当前已满足关闭条件。**

## 2. 修改文件

1. [`VISNDT/apps/api/src/auth/dto/auth-response.dto.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts)
2. [`VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts)
3. [`343_M14.2.4.5A_TypeContract_Fix_Report.md`](file:///F:/Desktop/VISNDT/docs/_review/343_M14.2.4.5A_TypeContract_Fix_Report.md)

## 3. 修改原因

根据 [`342_M14.2.4.5A_Final_Result_Audit_Report.md`](file:///F:/Desktop/VISNDT/docs/_review/342_M14.2.4.5A_Final_Result_Audit_Report.md#L87-L98) 的结论，当前 A 类 `5A` 唯一剩余缺口是：

1. `AuthUser` 关键字段仍被声明为 optional
2. 类型层仍保留 `undefined` 与 `null` 两套语义
3. 与 [`333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md`](file:///F:/Desktop/VISNDT/docs/_review/333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md#L277-L283) 中“字段必须显式存在、值允许为 null”的冻结要求不一致

因此本次仅对 DTO / Interface 执行最小类型收口，不扩展到运行时代码。

## 4. 冻结符合性

结论：**符合本次 `5A-TypeContract-Fix` 冻结要求。**

符合点如下：

1. `organization` 已改为必含字段，值类型为 `AuthOrganizationDto | null`
   - 证据：[`auth-response.dto.ts:L32-L37`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts#L32-L37), [`auth-request.interface.ts:L9-L13`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts#L9-L13)
2. `organizationMember` 已改为必含字段，值类型为 `AuthOrganizationMemberDto | null`
   - 证据：[`auth-response.dto.ts:L39-L44`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts#L39-L44), [`auth-request.interface.ts:L14-L16`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts#L14-L16)
3. `workspaceRole` 已改为必含字段，值类型为 `'SUPPLIER' | 'BUYER' | null`
   - 证据：[`auth-response.dto.ts:L46-L51`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts#L46-L51), [`auth-request.interface.ts:L17-L17`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts#L17-L17)
4. `nullable: true` 仍保留，保证未映射语义继续用 `null` 表达，而不是 `undefined`
5. 未新增任何字段，未修改现有字段名
6. 未修改 `auth.service.ts` 中的 `workspaceRole` 映射逻辑
7. 未修改 `auth.controller.ts`、`apps/web`、`database`

对应冻结依据：

- [`333_Specification:L160-L162`](file:///F:/Desktop/VISNDT/docs/_review/333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md#L160-L162)
- [`333_Specification:L227-L230`](file:///F:/Desktop/VISNDT/docs/_review/333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md#L227-L230)
- [`333_Specification:L277-L283`](file:///F:/Desktop/VISNDT/docs/_review/333_M14.2.4.4C_WorkspaceRole_Contract_Freeze_Specification.md#L277-L283)

## 5. API 影响

结论：**仅有类型契约声明层影响，无 API 运行时逻辑变化。**

具体说明：

1. Swagger DTO 中 `organization`、`organizationMember`、`workspaceRole` 从“可缺省”收敛为“必须存在但可为 null”
2. `AuthRequest.user` 类型同步收敛，避免守卫、控制器和服务侧继续消费 `undefined`
3. 未新增接口
4. 未删除接口
5. 未修改控制器返回结构
6. 未修改 `workspaceRole` 的取值集合与映射规则

## 6. Database 影响

结论：**无。**

说明：

1. 本次未触达 Prisma Schema
2. 本次未新增 migration
3. 本次未修改数据库字段、表结构或 enum

## 7. Build 结果

在业务代码根目录 `F:\Desktop\VISNDT\VISNDT` 执行：

1. `pnpm --filter @visndt/api build`
   - 结果：通过

## 8. 类型契约变化

本次仅发生以下 3 处类型冻结变化：

1. `AuthUserDto.organization`
   - 变更前：`organization?: AuthOrganizationDto | null`
   - 变更后：`organization: AuthOrganizationDto | null`

2. `AuthUserDto.organizationMember`
   - 变更前：`organizationMember?: AuthOrganizationMemberDto | null`
   - 变更后：`organizationMember: AuthOrganizationMemberDto | null`

3. `AuthUserDto.workspaceRole` / `AuthRequest.user.workspaceRole`
   - 变更前：`workspaceRole?: 'SUPPLIER' | 'BUYER' | null`
   - 变更后：`workspaceRole: 'SUPPLIER' | 'BUYER' | null`

同步结果：

1. `organization` 不再允许缺失
2. `organizationMember` 不再允许缺失
3. `workspaceRole` 不再允许缺失
4. 未映射或无值场景统一收敛为 `null`

## 9. 5A 关闭判断

结论：**若仅按 A 类 `5A` 允许范围判断，当前已满足关闭条件。**

判断依据：

1. [`342`](file:///F:/Desktop/VISNDT/docs/_review/342_M14.2.4.5A_Final_Result_Audit_Report.md#L152-L158) 中指出的唯一未满足项，是 `AuthUser` 关键字段未完全冻结为“必含且可为 null”
2. 本次该缺口已在 DTO 与 Interface 两侧完成收口：
   - [`auth-response.dto.ts:L32-L51`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/dto/auth-response.dto.ts#L32-L51)
   - [`auth-request.interface.ts:L9-L17`](file:///F:/Desktop/VISNDT/VISNDT/apps/api/src/auth/interfaces/auth-request.interface.ts#L9-L17)
3. 本次未引入新的越界修改，也未扩展到运行时逻辑

边界说明：

- 本报告仅判断当前 A 类 `5A` 类型冻结任务是否完成
- `B/D/E` 类历史变更不在本报告关闭结论中处理

## 10. Execution Result

1. 修改文件
   - `F:\Desktop\VISNDT\VISNDT\apps\api\src\auth\dto\auth-response.dto.ts`
   - `F:\Desktop\VISNDT\VISNDT\apps\api\src\auth\interfaces\auth-request.interface.ts`
   - `F:\Desktop\VISNDT\docs\_review\343_M14.2.4.5A_TypeContract_Fix_Report.md`
2. 修改原因
   - 按任务要求完成 AuthUser 类型冻结，将 `organization`、`organizationMember`、`workspaceRole` 从 optional 收敛为必含字段，并保留 `null` 语义
3. 冻结符合性
   - 符合；未新增字段，未修改映射、API 逻辑、数据库
4. API 影响
   - 仅类型契约声明层影响，无运行时逻辑变化
5. Database 影响
   - 无
6. Build 结果
   - `pnpm --filter @visndt/api build`：通过
7. 类型契约变化
   - `organization`、`organizationMember`、`workspaceRole` 已从 optional 收敛为必含且可为 `null`
8. 5A 关闭判断
   - 若仅按 A 类 `5A` 范围判断，当前已满足关闭条件
