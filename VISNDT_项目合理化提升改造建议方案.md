# VISNDT 项目合理化提升改造建议方案

**基准日期：2026-09-10 | 综合《VISNDT_平台架构设计审核报告》《VISNDT_前端
设计审计报告》及全部核实记录。本方案按"先止血、再修复、再统一、再决策、
最后做新功能"的顺序排列，不是按发现顺序排列。**

---

## 总体原则

这份方案不是一次性执行清单，是一个**分阶段、每阶段有明确出口条件**的
路线图。不建议跳阶段——比如在 Phase 0 的两个安全问题confirm生产环境
状态之前就去做 Phase 2 的设计系统统一，会导致优先级错配。

---

## Phase 0：本周内，止血类（不需要架构决策，直接执行）

| # | 事项 | 动作 | 出口条件 |
|---|---|---|---|
| 0.1 | 确认 org PATCH 越权 + workflow-events 未鉴权两处修复已在生产/预生产环境生效 | 如果已部署，重新走一遍越权测试用例确认；如果尚未部署，排期部署 | 生产环境实测通过 |
| 0.2 | 系统性权限守卫排查 | 写一个脚本（可复用本轮排查用的方法）扫描全部 48 个 controller 的写操作，逐一确认 Guard 配置意图正确，不只是"有 Guard"，而是"Guard 配得对" | 产出一份完整核对表，不再是抽查 |
| 0.3 | bcrypt 环境问题 | 视是否近期需要本地鉴权测试决定是否放开 `allowBuilds`（此前已给过判断标准） | 本地登录可验证，或明确记录"暂不需要" |
| 0.4 | 更正 CLAUDE.md 与技术文档里"apps/web 范围未定案"的错误结论 | 用本轮发现的 56 页面真实清单替换 | 文档与仓库一致 |

**Phase 0 出口条件**：两个已知漏洞确认生产环境状态、有了系统性（非
抽查）的权限核对表、文档不再包含已知错误结论。

---

## Phase 1：2-4 周，纯代码修复类（不涉及冻结 schema 决策部分）

| # | 事项 | 类型 | 备注 |
|---|---|---|---|
| 1.1 | e2e fixture 与 `Product.categoryId` 非空字段同步 | 测试基建 | 消除本轮 165 个用例里因测试数据过期造成的噪音，让真实业务通过率可信 |
| 1.2 | 追查 auth-invitation "期望 401 实收 400" 的断言差异 | 缺陷定位 | 确认是测试用例错了还是业务逻辑返回码不对 |
| 1.3 | 建立"写操作必须有 Guard"的 ESLint/CI 检查规则 | 工程防护 | 防止 Phase 0.2 发现的这类问题以后再次悄悄引入 |
| 1.4 | GET /files/:id 补全 | 功能缺口 | 低风险，纯新增端点 |
| 1.5 | ENUM 参数 Option 管理 UI | 功能缺口 | 优先级高于表面看起来的"前端小需求"——直接影响匹配引擎实际效果上限，建议提前到这个阶段而不是排在设计统一之后 |
| 1.6 | 排查是否还有其他 service 手写了类似 `checkOrganizationAccess` 这种容易出错的归属校验逻辑 | 安全 | Phase 0.2 是覆盖面排查，这一条是深挖每一处发现的手写权限逻辑本身对不对 |

**Phase 1 出口条件**：e2e 真实通过率可信、无已知遗留的高优先级功能
缺口、有了防止同类安全问题复发的自动化机制。

---

## Phase 2：中期（1-2 个月），需要产品/设计投入的统一化工作

**重要顺序调整**：根据前端审计报告的新发现，**web 端工作台/产品发现层
的设计统一，优先级应该高于 admin 端**——因为它覆盖 50 个页面（远多于
admin 的 30 个），而且是买卖双方真正完成业务的地方，此前完全没有被
评估过、风险被低估。

| # | 事项 | 范围 | 备注 |
|---|---|---|---|
| 2.1 | web 端工作台+产品发现层设计系统统一 | 约 50 个页面 | 决定是延伸 blueprint 体系覆盖到这些页面，还是为"产品操作场景"设计一套区别于"营销发现场景"的姊妹视觉语言（两种都合理，取决于产品判断，不是纯技术问题） |
| 2.2 | admin 端统一设计系统落地 | 30 个页面 | 设计稿已有（治理队列+统一页头+语义色板），代码未改 |
| 2.3 | admin 页头组件化 | 15+ 处重复代码 | 抽成共享 `PageHeader` 组件，为 2.2 打基础 |
| 2.4 | 状态色/标签映射集中化 | 全 admin 端 | 统一到 `StatusTag`/`resolveStatusTone`，移除各文件散落的 `*_LABEL_MAP`/`*_COLOR` |
| 2.5 | 标题语言统一为中文 | admin 端几个残留英文标题页面 | 低成本高一致性收益 |
| 2.6 | Home.tsx / OperationCenter.tsx 理念冲突收敛 | admin 首页 | 需要先有 2.7 的产品决策才能执行 |

**Phase 2 出口条件**：两端视觉语言各自内部一致，且有清晰文档说明
"营销/发现场景"与"业务操作场景"之间是否共享设计语言、如何共享。

---

## Phase 3：需要先拍板才能启动的架构/产品决策类

这些不属于"排期执行"的范畴，是必须先有人做决定的事项，做完决定后
再各自排入对应阶段执行：

| # | 决策 | 涉及范围 | 决策后归属阶段 |
|---|---|---|---|
| 3.1 | AuditLog.operatorId 外键设计如何修——SetNull / 用户软删除 / 其他 | 冻结 schema，需架构审批 | 决定后进入下一轮 Phase 1 |
| 3.2 | suppliers/matching 命名是否回溯改 | 冻结 schema，改动面大 | 决定后单独立项，不建议塞进任何常规阶段 |
| 3.3 | ORG_ADMIN 角色是否加入枚举 | 产品权限模型 | 决定后进入 Phase 1 |
| 3.4 | Home.tsx vs OperationCenter.tsx 到底留哪个/怎么整合 | 产品信息架构 | 决定后进入 Phase 2.6 |
| 3.5 | web 端设计统一走哪条路（延伸 blueprint 还是姊妹体系） | 产品/设计 | 决定后进入 Phase 2.1 |
| 3.6 | AI Agent 子系统（agent-runtime/rag/assistant/context/tool-registry）当前完成度与产品定位 | 未知范围，需先核实 | 核实之后才能判断该不该继续投入，还是先停下 |
| 3.7 | 是否需要正式的"平台级管理员"身份体系，替代当前靠 `PLATFORM_ORGANIZATION_ID` 环境变量白名单维持的跨组织治理权限 | 权限架构 | 目前的修复是"够用但不优雅"的应急方案，长期看这是一个真实的架构缺口 |

---

## Phase 4：长期健康度维护机制（不是一次性任务，是持续动作）

| # | 机制 | 触发频率 |
|---|---|---|
| 4.1 | 冻结区域定期复查节点 | 每个里程碑收尾时 |
| 4.2 | 报告治理新规则（禁止新建报告、只追加三份核心文档）执行情况回顾 | 下一个里程碑收尾时首次回顾 |
| 4.3 | 权限守卫的自动化检查（Phase 1.3 建立的机制）持续跑在 CI 里 | 每次提交 |
| 4.4 | 覆盖矩阵（`后端核实覆盖矩阵.md`）持续维护，任何工具真正核实过一个模块就更新对应行 | 随核实工作增量更新 |

---

## 总览：一张表看全部待办的阶段归属

```
Phase 0（本周）──→ 安全确认 + 系统性排查 + 文档更正
Phase 1（2-4周）──→ 测试基建同步 + 纯代码缺口 + CI 防护机制
Phase 3（先拍板）──→ 7 项决策，决策完成后回填到 Phase 1/2
Phase 2（1-2月）──→ 两端设计统一（web 工作台优先于 admin）
Phase 4（持续）──→ 治理/覆盖矩阵/CI 检查的长期维护
```

**唯一需要你现在就做的选择**：Phase 3 的 7 项决策不需要一次性拍完，
但建议按对下游阻塞程度排序去逐条处理——3.1（AuditLog FK）和 3.7
（平台管理员体系）阻塞的下游工作最多，建议优先拍板这两条。
