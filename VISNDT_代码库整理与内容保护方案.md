# VISNDT 代码库整理方法 + 内容保护安全措施补充

**基准日期：2026-09-10 | 依据：直接对仓库三个层级（仓库根/VISNDT代码根/
database）做的完整文件盘点。**

---

# 第一部分：代码库整理方法

## 一、问题全貌（这次盘点的真实数字）

| 位置 | 零散文件数 | 典型内容 |
|---|---|---|
| 仓库根目录 | 68 个 | 调试脚本(`-781_*`/`_79x_*`/`_80x_*`)、SQL 查询、浏览器截图 HTML、`cookies.txt`/`hash.txt`/`query` |
| `VISNDT/`（代码根） | ~60 个 | 类似调试脚本、`login_body.json`（真实账号密码）、`.trae-*.mjs`、误传的截图 PNG |
| `VISNDT/database/` | 134 个 | 大量 `_XXX_*.mjs/.sql/.ts/.txt` 一次性验证脚本、`_admin_cookies.txt` 等三个真实会话文件 |
| **合计** | **约 260+ 个** | 且其中 **396 处已经被 git 追踪、gitignore 规则加晚了不生效** |

这些不是"随手留在本地忘记删"的问题——是**每一轮 Trae 的验证/调试工作
都在项目根目录直接创建临时脚本，做完之后没有清理也没有集中存放**，
日积月累变成了现在这个规模。

## 二、分类处理原则——不是"全删"，是"归位"

不要用"看到零散文件就删"这种简单粗暴的方式，会有清理掉有价值验证
记录的风险。按下面四类分别处理：

### 类别 A：真实凭据/隐私数据——立即从历史彻底清除，不进任何归档

```
cookies.txt / hash.txt / query（仓库根）
login_body.json（VISNDT/）
_admin_cookies.txt / _buyer_cookies.txt / _supplier_cookies.txt（database/）
.cdp_profile/ / .edge-cdp-profile/ / .edge-cdp-profile2/ / _ux_verify/.../chrome_mobile/（浏览器 profile）
```
**处理方式**：`git filter-repo` 从全部历史里清除，不保留、不归档
（这些内容本身就不该存在，归档等于换个地方继续暴露）。

### 类别 B：一次性调试/验证脚本——保留价值判断后分流

```
数据库根目录下 134 个 _XXX_*.mjs/.sql/.ts
仓库根目录的 -781_*/_79x_*/_80x_* 系列
VISNDT/ 下的 _81x_*/_82x_* 系列
.trae-*.mjs
```
**处理方式**：
- 这些脚本对应的**验证结论**如果已经写进了 `ACTUAL_STATE.md`/
  `执行记录.md`，脚本本身可以直接删除，结论已经保留了，脚本是过程
  产物不是最终交付物
- 如果某个脚本本身有复用价值（比如反复要跑的健康检查逻辑），
  挪进一个新建的 `scripts/` 目录并给一个正式名字，不要留在根目录
  用编号命名
- 其余（大部分）直接删除

### 类别 C：真实有效的测试/构建产物——保留但归位

```
e2e_full_result.log（本次核实的原始证据）
e2e-test-image.png（测试用固定资源，需确认是否为 e2e 用例依赖）
```
**处理方式**：日志类挪进 `docs/_context/logs/`（如果还需要保留作
证据），或者接受"证据已经写进核实文档"后直接删除原始日志（保留
文档摘要即可，不需要 171KB 的原始日志永久留在仓库里）。

### 类别 D：已经算正式交付物的文档——归位到统一目录

```
CLAUDE.md（应留在仓库根，这个位置是约定俗成的，不用动）
VISNDT_项目全面技术文档.md / VISNDT_项目交接准备文档.md /
VISNDT_交接核实执行报告.md / 后端核实覆盖矩阵.md /
后端源码与关键结论独立验证.md / 给Trae的核实指令_第二轮.md /
服务与启动命令.md / 指令.md
```
**处理方式**：全部移进 `docs/_context/`，跟 `DECISIONS.md`/
`ACTUAL_STATE.md`/`执行记录.md` 放在一起，仓库根目录只留 `CLAUDE.md`
和 `README.md` 两个约定俗成的入口文件，其余交接类文档统一归位。

## 三、目标目录结构

```
VISNDT/（仓库根）
├── CLAUDE.md                    ← 唯一留在根目录的操作手册
├── README.md
├── docker-compose.yml
├── .gitignore                   ← 重写，覆盖三个层级
├── docs/
│   ├── _context/                ← 交接/核实/决策类文档统一归位于此
│   │   ├── DECISIONS.md
│   │   ├── ACTUAL_STATE.md
│   │   ├── 执行记录.md
│   │   ├── VISNDT_基线进度清单.md
│   │   ├── VISNDT_项目全面技术文档.md
│   │   ├── VISNDT_项目交接准备文档.md
│   │   ├── 后端核实覆盖矩阵.md
│   │   └── logs/                ← 需要保留的原始日志证据（非必要不留）
│   ├── _review/                 ← 历史 900+ 报告，冻结，不再新增
│   ├── VISNDT-Blueprint/        ← 蓝图文档，600_Frontend 已冻结
│   └── Content Management Guide/
├── scripts/                     ← 有复用价值的脚本，正式命名，非编号
├── VISNDT/                      ← pnpm workspace（业务代码，不受影响）
│   ├── apps/
│   ├── database/                ← 清理后只保留 seed/migration/package.json
│   │                                等正式内容，调试脚本全部清除
│   └── packages/
```

## 四、执行步骤（建议顺序）

```
1. 仓库私有化（已建议，优先级最高，立即执行）
2. 补全三层 .gitignore（根目录、VISNDT/、database/ 都要覆盖到）
3. 对类别 D 的文档做 git mv 归位到 docs/_context/
4. 对类别 B/C 逐一判断去留，git rm 清理
5. 对类别 A（凭据/隐私数据），用 git filter-repo 清除全部历史，
   这一步不是 git rm，是重写历史，需要之后强制推送
6. 强制推送后，仓库大小会显著变小（.cdp_profile 一项就有 211M）
7. 考虑联系 GitHub 支持清除侧边缓存（历史遗留的旧提交在私有化前
   已经暴露过，这一步是进一步止血，非必须但建议做）
```

## 五、补全后的 .gitignore 建议内容（三层合并版）

```gitignore
# 凭据/会话类，任何位置都不许提交
*cookies*.txt
*login_body*.json
hash.txt
*.pem
*.key

# 浏览器自动化 profile
.cdp_profile/
.edge-cdp-profile*/
_ux_verify/

# 一次性调试脚本（编号命名模式）
_[0-9]*.mjs
_[0-9]*.sql
_[0-9]*.ts
_[0-9]*.cjs
-[0-9]*.sql
-[0-9]*.mjs
_[0-9]*_*.html
_[0-9]*_*.json

# 测试原始日志
*.log
e2e_full_result.log

# 已有规则保留
node_modules/
.env
.env.local
```

**更重要的是行为约束，不只是规则**：把"任何脚本/临时文件不直接
`git add .`，只 add 明确要提交的文件"写进 CLAUDE.md 的工程规范里，
这比再完善的 .gitignore 规则更能从根源上防止复发。

---

# 第二部分：内容保护安全措施

## 六、你提到的"禁止复制"——建议不要作为主要防线，说明原因

**右键/复制禁用（`oncontextmenu` 拦截、CSS `user-select: none`）**
效果实际很有限：懂技术的人打开浏览器开发者工具、直接看网页源码、
截图，几秒钟就能绕过，几乎拦不住真正想复制内容的人。**反而会真实
伤害到正常用户**——比如想复制一段产品型号去粘贴搜索、想选中文字
用浏览器自带翻译、视障用户依赖的屏幕阅读器都可能受影响。

**建议**：不要把它当作内容保护的主力手段，如果非要做，可以做成
一个**很轻量的摩擦层**（比如复制时弹一个"内容版权归 VISNDT 所有"
的提示，但不真的阻止复制动作），象征意义大于实际防护意义。

## 七、真正值得投入的内容保护措施

### 7.1 图片水印——建议做，效果实在

产品图片加一个半透明的 VISNDT 品牌水印（角标或者贯穿式都可以），
成本低（上传时自动处理即可），能有效降低"别人直接盗图放自己平台"
的便利性，即使被盗用也能明确追溯来源，比"禁止复制"更实际。

### 7.2 文档水印——建议做，且能做到"可追溯泄露源"

供应商上传的规格书 PDF、平台生成的报价单/RFQ 相关文档，下载时
**动态生成带用户信息的水印**（比如"下载账号：xxx@xxx.com，下载
时间：2026-09-10"），成本比图片水印略高，但价值更大——**如果哪份
文档被泄露到平台外，水印能直接追溯是谁下载的**，这比单纯"防止
下载"更有威慑力，而且不影响正常业务使用（买家该看的技术资料还是
能正常看）。

### 7.3 文件下载链接——项目已经具备基础，建议延续

本轮核实确认 `file-asset` 模块的下载机制**已经是签名 URL**（不是
直接暴露文件公网路径），这个设计本身就是内容保护的一种，建议延续
这个模式，不需要额外引入新机制。**唯一观察到的例外**：
`GET :id/download` 对非 Content 关联文件目前没有额外授权门禁
（之前架构报告里提到过），如果这类文件里有需要保护的内容，建议
补上权限检查。

### 7.4 反爬虫/限流——项目已有基础

e2e 测试套件里已经出现 `rate-limit` 相关用例，说明限流机制已经在
代码里，这块不需要新建，只需要确认限流阈值是否覆盖了"恶意批量抓取
产品数据"这类场景（跟正常用户访问速率做区分）。

### 7.5 SEO 抓取 vs 内容保护——两者需要分层，不要一刀切

**这一点需要跟之前讨论的"参数释义/知识库内容要利于 AI/搜索引擎
抓取"放在一起看，是同一个问题的两面**：
- **应该完全开放抓取**：产品目录页、参数释义、知识库文章——这些
  正是你想被搜索引擎和 AI 工具收录、引流的内容
- **应该限制抓取/需要登录**：供应商详细规格书 PDF、买家/供应商
  工作台内的商业敏感数据（询价详情、报价金额）
- 用 `robots.txt` + 页面级别的登录门禁配合实现，不要为了"防抓取"
  把本该开放的营销/知识内容也一起挡住，那样反而伤害了你的 SEO/AI
  引流策略

### 7.6 低成本但真实有效的底线：版权声明+服务条款

网站页脚加版权声明，服务条款里明确写"未经授权禁止批量抓取/转载"，
成本几乎为零，虽然不能物理阻止别人复制，但**给了你之后追究责任的
法律依据**——没有这条声明，即使真的发生了内容盗用，维权依据也会
更弱。建议现在就补上，这是所有内容保护措施里性价比最高的一项。

## 八、优先级建议

```
现在就做（零/极低成本）：版权声明+服务条款、robots.txt 分层配置
短期做（低成本，明确收益）：图片水印
中期做（中等成本，价值高）：文档动态水印（可追溯泄露源）
不建议投入：右键/复制禁用这类前端拦截
已有基础，确认覆盖面即可：签名下载 URL、限流机制
```
