# VISNDT 平台架构设计审核报告

**基准日期：2026-09-10 | 依据：直接克隆并读取 `github.com/Cery/VISNDT`（commit
`bc9effd`）源码，非转述。本报告仅覆盖架构层面，功能缺陷清单见
《VISNDT_项目全面技术文档.md》，改造建议见《项目合理化提升改造建议方案》。**

---

## 一、总体架构评价

Monorepo（api / admin / web + database + packages），三端通过 REST 契约
解耦，是这个体量的项目里合理、成熟的选择，没有过度设计（比如没有盲目上
微服务），也没有欠设计（三端职责清晰，没有把治理逻辑塞进客户端）。

**模块划分**：`apps/api/src` 下 **46 个 NestJS module**，基本遵循单一
职责——但有几处命名/边界值得注意：

- `admin/` 目录下的 `admin-*.controller.ts` 是一批"治理专用视图"
  （`admin-demand`、`admin-inquiry`、`admin-matching`、
  `admin-product-category-knowledge-mapping` 等），跟同名的基础模块
  （`demands`、`inquiries`）并存。这个"业务模块 + 治理视图模块"分离
  的模式是合理的（治理端有自己的鉴权边界、返回字段可以不同），但如果
  没有清晰的文档说明这个约定，容易让新加入的开发者误以为是重复模块
- 新发现（此前所有轮核实都未覆盖到）：**`ai/` 目录下有一整套 Agent
  子系统**——`agent-runtime.service.ts`、`rag.service.ts`、
  `assistant.service.ts`、`context.service.ts`、
  `tool-registry.service.ts`。这套东西的存在此前从未被任何一版文档
  提及，说明"核心功能模块"清单实际上是不完整的。**这是本报告唯一
  一处我没有把握给出评价的模块**——只看到了文件结构，没有读过实现，
  不知道这套 Agent/RAG 系统的完成度、是否已接入实际业务流程、还是
  处于实验阶段。**建议单独排一轮核实**，因为"AI Agent 能做什么"
  直接关系到产品对外的能力边界描述是否准确

---

## 二、安全架构评价

### 2.1 全局安全基础设施——评价：扎实，好于同体量项目的平均水平

`main.ts` 里的启动配置包含：
- `helmet()`——标准安全响应头
- **CSRF 双提交 Cookie 模式**——手写实现（`CsrfService` + 中间件），
  对非安全方法（POST/PATCH/PUT/DELETE）强制校验 `X-CSRF-Token` 头
  与 `csrf_token` Cookie 一致，登录/注册/刷新三个端点显式跳过（合理，
  这几个端点本来就是在建立会话）
- 全局 `ValidationPipe({ whitelist: true, transform: true })`——防止
  多余字段注入
- 全局异常过滤器 + 日志拦截器
- CORS 显式配置 `origin` 白名单 + `credentials: true`

这套组合拳说明团队在"通用 Web 安全"这个维度是有意识、有investment的，
不是随手拼凑的项目。

### 2.2 但业务层访问控制存在系统性风险——这才是真正需要关注的地方

**本轮直接核实发现两个真实的访问控制漏洞**（均已确认修复并推送，
commit `bc9effd`）：

1. `organizations.service.ts` 的 `checkOrganizationAccess`——原逻辑
   "只要是任意一个组织的 ADMIN，就能修改任意其他组织"，是跨租户权限
   提升
2. `workflow-events.controller.ts` 的 `GET /` 与 `GET /:id`——完全没有
   鉴权守卫，任何人无需登录即可读取全平台业务流转记录

**这两个漏洞的共同特征值得写进架构层面的结论，而不是当作孤立 bug**：
都不是"忘了写权限判断"，而是**权限判断写了，但写错了**——第一个是
查询条件漏了组织限定，第二个是漏挂了装饰器。这说明当前架构里**没有
任何机制能在开发阶段就拦住这类错误**（比如没有针对"写操作必须有
Guard"的 lint 规则、没有针对"跨组织操作必须验证组织归属"的统一
中间件/装饰器）。48 个 controller、275 个端点里只抽查出这两个，
不代表只有这两个——**这是抽查发现的下限，不是上限**。

**架构建议**：不要满足于"这两个改完了"，应该建立系统性的防护，
比如：
- 写一条自定义 ESLint 规则，检测任何 `@Post/@Patch/@Put/@Delete`
  方法（类级别或方法级别）缺少 `@UseGuards` 时构建失败
- 把"跨组织资源访问校验"抽成一个统一的装饰器/守卫（比如
  `@RequireOrgAccess()`），而不是像现在这样每个 service 各自手写
  `checkXxxAccess` 私有方法——`organizations.service.ts` 这次改对了，
  但如果 `offers.service.ts`、`demands.service.ts` 里有类似的手写
  归属校验，谁能保证它们都写对了？这次核实没有专门去找"是否有其他
  service 也手写了类似容易出错的归属判断逻辑"，这是一个值得排期的
  后续检查项

### 2.3 认证机制评价

JWT 自研（无第三方 OAuth）+ HttpOnly Cookie（结合 CSRF 双提交模式看，
应该是把 token 放 Cookie 而不是纯 localStorage，这是防 XSS 窃取 token
的正确做法）+ refresh token 轮换机制（`auth.service.ts` 核实过存在）。
这套设计本身是合理的，选择不依赖第三方 OAuth 也符合"迁移自由度"的
既定架构目标。

**当前生产阻塞**：bcrypt 原生二进制缺失导致本地登录无法验证（详见
技术文档第七节），这是环境问题不是架构问题，但会直接卡住任何需要
真实鉴权验证的开发工作。

---

## 三、数据架构评价

### 3.1 Schema 设计——总体合理，两处设计缺陷

- **命名自洽**：`ParameterGroup`/`WorkflowEvent`/`RFQ`/`RFQResponse`
  与蓝图文档描述基本一致，蓝图里出现的
  `StandardProduct`/`ParameterTemplate`/`WorkflowInstance`/`RFQItem`
  在实际 schema 里不存在，是文档漂移不是代码问题
- **参数标准化体系设计合理**：`ParameterGroup → ParameterDefinition`
  的两层结构，配合 `DemandParameter`/`ProductParameterValue` 的精确值/
  范围值双模式，是支撑"工程参数比对"这个核心业务差异化能力的正确
  底层设计
- **🔴 设计缺陷 1：`AuditLog.operatorId` 外键约束设计不当**——引用
  `User` 且未设置 `onDelete: SetNull`，导致"删除一个曾经操作过任何
  东西的用户"必然触发外键冲突（P2003，已本地实测复现）。这是一个
  **审计需求与数据完整性约束之间的设计冲突**：如果 `operatorId`
  允许级联删除，审计记录会随用户一起消失，违背审计"即使当事人不在
  痕迹仍在"的本意；如果不允许，用户管理功能里的"删除用户"就是
  残缺的。**这类问题应该在 schema 设计阶段就用"软删除 + 保留外键
  但允许为空"的模式解决**，而不是等实现完之后才发现
- **⚠️ 观察：审计覆盖依赖 Prisma 中间件，隐式且不完整**——`$use`
  中间件自动覆盖所有 model 的 create/update/delete/upsert，但
  **不覆盖交互式事务** `$transaction(async tx => …)`（这是 Prisma 5
  的已知行为），也不映射 `STATUS_CHANGE`/`LOGIN` 这两类语义动作
  （需要显式调用）。这意味着**审计覆盖率高度依赖开发者是否用了
  正确的 Prisma 客户端调用方式**，任何一处误用交互式事务都会悄悄
  跳过审计，且不会有任何报错提示——这是一个脆弱的架构，建议要么
  改成显式审计（放弃"自动兜底"的便利性换取可预测性），要么在
  CI 里加一条检测"是否在交互式事务里做了写操作却没有显式审计"的
  静态检查

  **概念上可以进一步区分三类审计语义**（这个提炼有价值，值得采纳
  为设计原则，即使目前 schema 不需要为此新增字段）：`Data Mutation`
  （中间件自动覆盖的 create/update/delete）、`Business State
  Transition`（`STATUS_CHANGE`，比如产品从 REVIEWING→APPROVED，
  这是业务事件而不只是字段变了）、`Authentication Event`
  （`LOGIN`，目前从未被记录）。当前实现把这三者混在同一张表、同一套
  隐式/显式规则里，容易在排查问题时误判"这个操作到底有没有被审计"。

### 3.2 冻结治理机制评价

数据库 schema/migration 冻结、改动需架构审批的做法本身合理，但
"冻结不等于完美"这条原则需要真正落地——第 3.1 节的两处设计缺陷
恰好说明了冻结区域也会有问题，建议把"冻结项定期复查"从原则变成
实际的复查节点（比如每个里程碑收尾时的固定检查项），而不是停留
在文档里的一句话。

---

## 四、匹配引擎架构评价

**评价：设计合理，实现细节比早期文档描述的更精细**。

- 加权平均计分，必填参数硬性淘汰（一票否决制），数值型参数按偏离度
  给部分分而非二元判断——这是恰当的工程判断，避免了"差一点点就完全
  不匹配"这种对用户不友好的行为
- 编排层（`matching.service.ts`）与算分层（`scoring.service.ts`）
  分离，职责清晰
- `DemandMatch` 状态机 `PENDING→MATCHED→REVIEWED→ACCEPTED/REJECTED/
  EXPIRED`——比早期简化描述多一个 MATCHED 中间态，状态机设计完整

**唯一的架构性隐患**：匹配精度完全依赖 `ParameterDefinition` 体系的
完整性，而 ENUM 类型参数当前缺少可视化的选项管理界面（技术债，见
技术文档），这意味着**匹配引擎的实际效果上限被一个看似"前端小功能
缺失"的问题卡住**——这是一个值得在优先级排序时纳入考虑的架构耦合
关系：不要把 ENUM Option UI 当作纯前端小需求来评估优先级，它实际上
关系到核心业务能力的可用性。

### 4.1 开放式架构问题：匹配候选对象是 Product+Offer，还是应该是 SupplierProduct

**这是一个真实存在、值得你做判断的架构问题，不是已确认的缺陷**——
直接读 `matching.service.ts` 源码确认：

```ts
// 查询候选产品（ACTIVE + 有 ACTIVE Offer + Category Filter）
where: { status: 'ACTIVE', ...categoryFilter, offers: { some: { status: 'ACTIVE' } } }
...
const bestOffer = candidate.offers[0] ?? null;  // 选择最佳 Offer（第一个 ACTIVE）
```

也就是说：**当前匹配引擎用"这个 Product 是否挂着至少一个 ACTIVE 状态
的 Offer"来判断"这个产品是否有供给"**，选中的"最佳 Offer"只是按创建
时间取第一个，不是真正的商业最优选择。

这里有一个值得思考的角色混淆：`Offer`（报价）本来的语义应该是"针对
某次具体商业请求的响应"，但在匹配阶段被拿来当作"这个产品是否真的有
供应商在供应"的判断依据——这两件事目前耦合在一起。**如果未来
`SupplierProduct`（供应商型号）想成为"谁在提供这个产品"的权威判断
来源（而不是 Offer），匹配候选逻辑需要调整为查询 `SupplierProduct`
状态而不是查询 Offer 状态。**

这不是必须现在改的 bug，是一个**产品/架构决策**：取决于你希望"一个
产品是否可询价"这件事由 `SupplierProduct` 的存在与审核状态决定，
还是继续由"是否有人报过价"这个更偶然的信号决定。建议列入第十节的
待拍板事项。



---

## 五、测试架构评价

**e2e 测试基建本身存在与 schema 演进不同步的问题**：`Product.categoryId`
已改为非空字段，但测试 fixture（`createProductWithParams`）仍按旧
schema 构造数据，导致级联失败。这不是"测试写错了"这么简单，是
**schema 变更时没有配套更新测试基建**的流程缺口——建议在冻结区域的
审批流程里加一条"数据库字段级别的变更（尤其是从可选改为必填）必须
同步检查/更新 e2e fixture"，否则每次类似变更都会制造一批看起来是
"业务逻辑失败"、实际是"测试数据过期"的噪音，浪费排查时间（这次
165 个用例里 48 个失败，相当比例就是这类噪音，真实业务缺陷被淹没
在里面）。

真实业务通过率（排除已知噪音后）：核心主链路（产品、admin、rematch、
category-filter、rate-limit、workflow-permission）验证通过，但仍有
一条断言差异（auth 注册无邀请码场景期望 401 实际收到 400）未定论，
建议排期确认这是测试用例错了还是业务逻辑的错误状态码。

---

## 六、部署架构评价

Docker Compose 单机部署（postgres+api+admin+minio），规划路径清晰
（Oracle Cloud 免费层→阿里云香港 ECS），"中国可访问、无 ICP 备案、
自托管"这个约束贯穿始终、没有被违反（历史上曾有工具建议 Vercel，
已正确否决）。

**当前架构对"多实例/高可用"没有设计**，这对 MVP 阶段是合理的取舍，
但如果第二阶段迁移到阿里云 ECS 时用户量有实质增长，需要提前评估
Session/JWT 在多实例场景下是否需要额外处理（目前 JWT 无状态本身对
水平扩展友好，但 CSRF token 校验、文件上传到本地磁盘还是 MinIO 等
细节需要确认是否已经是"无状态友好"的设计——这个本轮没有单独核实，
标记为待确认项）。

---

## 七、项目治理架构评价（不是代码架构，但是这个项目独有的、必须提的一环）

900+ 份分散的执行报告 + 命名重复编号 + 历史文档与代码脱节，这个
"项目治理债务"的规模和积累速度，本质上跟代码技术债是同一类问题，
只是发生在文档层面而非代码层面。2026-09-09 建立的新治理原则（结论
必须有源码/命令证据、不再新建报告文件、只追加维护三份核心文档）是
正确方向，但**这套新规则本身还没有经过足够长时间的检验**——建议
在下一个里程碑收尾时回顾一次，确认这套新流程是否真的被坚持了，
还是又悄悄退回到"生成新报告"的旧习惯。

---

## 八、架构问题优先级汇总（供改造方案参考）

| 优先级 | 问题 | 类型 |
|---|---|---|
| P0 | AuditLog FK 设计缺陷导致用户删除功能残缺 | 数据架构 |
| P0 | bcrypt 原生二进制缺失阻塞本地鉴权验证 | 环境/工程 |
| P1 | 业务层访问控制缺乏系统性防护机制（这次只抽查出 2 个，不代表只有 2 个） | 安全架构 |
| P1 | 审计中间件不覆盖交互式事务，覆盖率不可预测 | 数据架构 |
| P1 | e2e fixture 与 schema 变更不同步的流程缺口 | 工程流程 |
| P2 | AI Agent 子系统完全未被文档化，能力边界不明 | 文档/架构盲区 |
| P2 | ENUM 参数 Option 管理 UI 缺失，实际卡住匹配引擎效果上限 | 功能/架构耦合 |
| P2 | 匹配候选对象应以 Product+Offer 还是 SupplierProduct 状态为准（见第 4.1 节） | 产品/架构决策 |
| P2 | 平台管理员与组织管理员应分属两个不同的权限域，而不是靠 `PLATFORM_ORGANIZATION_ID` 白名单临时维持（呼应第八节结论） | 权限架构 |
| P3 | admin-* 治理视图模块与基础模块并存的约定未文档化 | 架构可维护性 |

---

## 九、关于外部审核意见的取舍说明

本报告修订时参考过一份 ChatGPT 生成的架构审核报告。其中"匹配候选
对象应重新考虑""AuditLog 应区分数据变更/业务事件/鉴权事件三类语义"
"平台管理员应与组织管理员分离"这三点已核实合理并采纳进本报告（第
4.1 节、第 3.1 节、第八节）。**该报告里提出的 `SupplierProfile`、
`Membership`、`SalesMember`、`Lead`、付费排序/Sponsored Placement
等一整套商业模型概念，本报告不予采纳**——这些概念在当前代码库中
完全不存在，是外部报告基于通用 B2B 平台经验自行提出的商业模式设想，
不是对现有架构的审核结论。这些设想已单独整理进《VISNDT_商业化产品
设计方案》作为待你判断的产品提案，不应与本报告的架构事实混为一谈。
