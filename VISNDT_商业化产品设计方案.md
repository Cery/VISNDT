# VISNDT 商业化产品设计方案

**基准日期：2026-09-10 | 文档性质：产品/商业设计提案，非架构审核结论。**

> **重要说明**：这份文档跟《VISNDT_平台架构设计审核报告》性质不同——
> 那份只写"代码里已经是什么样"，这份写"商业模式可以往哪个方向设计"。
> 本文档里每一节都会明确标注：**【现状】**=已核实的代码事实，
> **【提案】**=参考 ChatGPT 报告筛选后、结合项目现状提出的设计选项，
> 需要你判断取舍，不是既定结论。提案部分如果被采纳，才会进入架构
> 报告和 CLAUDE.md 变成"待实现的决策"。

---

## 一、产品定位收敛提案

**【现状】** 当前定位表述："面向中国大陆市场的工业内窥镜/工业无损
检测能力发现平台"，核心对象是 `Product`（平台产品/标准产品）。

**【提案】** 把定位收敛得更精确一点：

```
VISNDT = NDT 结构化产品发现平台
       = 结构化工程数据 + 供应网络 + 需求匹配 + RFQ 转化
```

这不是推翻现有方向，是把"能力发现平台"这个偏抽象的表述，换成一个
更能指导后续设计决策的具体框架。**如果 VISNDT 只停留在"产品+供应商+
联系方式+付费展示"，跟 Thomas、DirectIndustry 这类传统工业黄页网站
没有本质区别；真正的差异化只能来自"结构化的检测工程数据"这一层**——
也就是现在已经在做的参数标准化体系（`ParameterGroup`/
`ParameterDefinition`）。这个提案的实际意义是：**告诉你未来资源该
往哪投**——投参数字典的完整度、投知识库跟产品的关联深度，而不是
投更多产品分类页面的数量。

是否采纳这个收敛表述，不影响任何代码，纯粹是对外沟通/内部对齐用的
定位语言，可以随时调整，风险很低。

---

## 二、领域概念澄清提案（不涉及 schema 改动）

**【现状】** 当前核心对象关系：`Product`（平台产品）→
`SupplierProduct`（供应商型号）→ `Organization`（组织）。这套结构
本身是合理的（详见架构报告第三节），问题出在**概念职责的清晰度**，
不是结构本身。

**【提案】** 用一句话分别定义四个核心对象的职责边界，作为团队内部
（包括未来接手的 Claude Code）共同理解的基准，不需要改代码：

| 对象 | 职责（提案措辞） |
|---|---|
| `Product` | 回答"这是什么"——平台对某类检测设备的标准化定义 |
| `SupplierProduct` | 回答"谁在提供、提供的具体型号是什么" |
| `Offer` | 回答"针对一次具体商业请求，报价是多少、条件是什么"——**不应该同时承担"这个产品是否有供给"的判断**（呼应架构报告 4.1 节的开放问题） |
| `Capability` | 如果未来要做语义理解层（比如用户搜"焊缝检测"，系统理解成"缺陷检测能力+超声/涡流方法+相关产品分类"），这应该是一个**查询/理解层**，不应该重新变成一级商业实体去承担"能力型号""能力供应商"这类概念——历史上（本对话早期核实过）项目已经用"Product"替代了"Capability"作为商业主实体，这个方向是对的，不建议走回头路 |

这份职责澄清如果你认可，建议直接写进 CLAUDE.md，作为以后任何人（含
Claude Code）设计新功能时的概念护栏，防止"Capability"这类历史概念
借着某次需求又重新变成商业实体。

---

## 三、商业模式设计提案——这是本文档最需要你判断的部分

**【现状】** 当前 schema 和代码里**完全没有**付费、会员、销售联系人、
Lead 分配、付费排序这类概念。平台目前是纯粹的"发现+撮合"工具，没有
任何商业化机制。

**【提案，供你判断是否要往这个方向发展】** 如果未来需要商业化
（这是一个产品决策，不是技术必然），可以考虑分层设计，但**现在不
建议直接改 schema 去实现它们**，原因见第五节：

```
供应商与平台的商业关系（是否付费、什么等级）
        ≠
供应商是否是真实供应商（Organization.type = SUPPLIER 已经表达了这个）
        ≠
供应商内部谁负责对接商务（未来如果需要，可以是 OrganizationMember 的
一个角色扩展，不需要单独实体）
```

**一条需要现在就想清楚的商业治理原则（这条建议无论是否做商业化都要
先定下来）**：

> **付费只能改变"谁被看见、看见的顺序"，不能改变"技术事实"。**

也就是说，如果未来做付费曝光/置顶，**匹配结果的技术评分（matchScore）
不能因为付费而改变**，只能在"同等匹配度下，付费方展示优先"这个层面
起作用。这条原则现在写进 CLAUDE.md 不需要任何代码改动，但能防止将来
在压力下（比如销售想促成某个付费客户成交）被要求"让付费供应商匹配分
更高"这种会破坏平台可信度的需求。**建议现在就拍板这条原则，不管商业
化具体方案是什么样。**

**是否要做商业化、做成什么样（会员制/按曝光收费/按 Lead 收费/纯免费
靠其他方式变现），这是一个需要你结合实际业务判断的决策，本文档不替
你做这个决定**——只是提醒你：如果决定要做，"付费不改变技术事实"这条
护栏应该在设计阶段就定下来，而不是等实现完了再补。

---

## 四、SEO / 内容可发现性提案

**【现状】** 已经有知识库（`KnowledgeDomain/Category/Entry`）+
`ProductCategoryKnowledgeMapping` 关联产品的机制，方向正确。

**【提案】** 随着近期 AI 搜索（豆包、DeepSeek、ChatGPT 这类工具）
越来越多地替代传统搜索引擎，产品页面/知识条目的结构化程度会直接影响
"能不能被 AI 工具引用和推荐"。这不是要求你现在就大改，而是建议在
**新增内容/产品页面时**，保持规范的 URL 结构、结构化元数据、稳定的
命名——这是一个**长期习惯**，不是一次性项目，成本很低，收益是长期的。
不建议现在为此立项做大改造，作为日常开发的一条约定即可。

---

## 五、为什么现在不建议大规模改 schema 去实现上述提案

即使你认可第二、三节的方向，**现在也不建议立刻**：

```
新增 Capability 独立表 / Membership 表 / SalesMember 表 /
大幅重构 Organization / 大幅重构 Product / 大幅重构 Matching
```

理由很直接：数据库处于冻结状态是有原因的（架构报告第三节），
而且**当前更紧迫的是先把已确认的真实缺陷和安全问题处理完**
（AuditLog FK、bcrypt、访问控制系统性排查）——这些是"现有功能是否
可靠"的问题，商业化扩展是"要不要长出新功能"的问题，优先级顺序不该
颠倒。

**建议的正确顺序**：

```
先完成《项目合理化提升改造建议方案》里的 Phase 0/1
        ↓
本文档第二节的"概念职责澄清"可以现在就写进 CLAUDE.md（零代码成本）
        ↓
本文档第三节"付费不改变技术事实"这条原则可以现在就拍板（零代码成本）
        ↓
是否要做商业化、做成什么样——你自己判断节奏，不需要现在就决定
        ↓
真正要动 schema 时，先补一份《权责矩阵》（谁能创建/编辑/审核/发布
每个对象），再评估 schema 影响，而不是直接建表
```

---

## 六、交给 Claude Code 的建议

如果你后续用 Claude Code 接手改造工作，建议按这个顺序喂给它：

1. 先给它《VISNDT_平台架构设计审核报告》（事实基准）和《后端核实
   覆盖矩阵》（哪些模块已核实、哪些没有）
2. 再给它《项目合理化提升改造建议方案》（Phase 0-4 的执行顺序）
3. **本文档（商业化产品设计方案）先不要给它**，除非你已经对第三节
   的商业化方向做出了明确决定——在你决定之前把这份提案文档喂给
   Claude Code，它可能会把"提案"误当成"待实现的需求"直接开始写
   `Membership`/`SalesMember` 相关代码，那就是在没有产品决策的情况下
   把一个假设性的商业模型变成了真实的 schema 改动，这正是本文档
   开头强调"提案不是结论"的原因

等你对商业化方向有了判断（哪怕只是"暂时不做，先把现有产品做扎实"
这个判断本身也算数），回来告诉我，我再帮你把决定的部分转化成可以
交给 Claude Code 执行的具体任务清单。
